// Completed tasks for the current goal. \
// Format: [x] <timestamp> <task> \
// Example: [x] 2026-04-05T20:39:27Z Verify the completed-task log is sorted newest-first using 24-hour HH:MM:SS UTC timestamps. \

[x] 2026-07-04T01:26:13Z Implement and verify CMUX-compatible remaining App catalog config parity from current upstream CMUX `ceb9f9368d8a7ea8c56bcbec39fa0ea8b3a33da8`: local `config get/set` and host settings load/save now support `app.language`, `app.appIcon`, `app.minimalMode`, `app.globalFontMagnification`, `app.confirmQuit`, `app.fileDropDefaultBehavior`, and `app.titlebarControlsStyle` in addition to the existing App catalog keys; enum values match upstream value files; invalid values fail loudly; and `app.globalFontMagnification` rounds/clamps to CMUX's 50..200 percent 10-point step behavior. Verified by `cargo test -p limux-cli config_app_scalar -- --nocapture`, `cargo test -p limux-host-linux app_scalar -- --nocapture`, touched-crate clippy, `cargo fmt --check`, `git diff --check`, and full `./scripts/check.sh`. \
[x] 2026-07-04T01:08:13Z Implement and verify CMUX-compatible spawned terminal locale sanitization from current upstream CMUX `ceb9f9368d8a7ea8c56bcbec39fa0ea8b3a33da8`: terminal creation now clears malformed effective `LC_ALL` and `LC_CTYPE` values, including inherited or startup-env CLDR/BCP-47 identifiers, before Ghostty spawns the shell so valid `LANG` can keep UTF-8 locale behavior; valid POSIX locales, `C`, and `POSIX` remain untouched. Verified by `cargo test -p limux-host-linux locale_ -- --nocapture`, `cargo clippy -p limux-host-linux --all-targets -- -D warnings`, `cargo fmt --check`, `git diff --check`, and full `./scripts/check.sh`. \
[x] 2026-07-04T00:25:02Z Implement and verify CMUX-compatible automation/mobile settings config parity from current upstream CMUX `7f94c9352f5267c2c01319d1aaa73b1e2207890e`: local `config get/set` now supports non-secret `automation.socketControlMode`, `automation.claudeCodeIntegration`, `automation.claudeBinaryPath`, `automation.workspaceAutoNaming`, `automation.autoNamingAgent`, `automation.ripgrepBinaryPath`, `automation.suppressSubagentNotifications`, `automation.ampIntegration`, `automation.cursorIntegration`, `automation.geminiIntegration`, `automation.kiroIntegration`, `automation.kiroNotificationLevel`, `automation.portBase`, `automation.portRange`, `mobile.iOSPairingHost.enabled`, `mobile.iOSPairingHost.port`, and `mobile.iOSPairingHost.displayName`; host settings load/save strictly validates and round-trips the same JSON sections; and `automation.socketPassword` remains separate secret-file work rather than a printable JSON scalar. Verified by `cargo test -p limux-cli config_automation -- --nocapture`, `cargo test -p limux-cli config_mobile -- --nocapture`, `cargo test -p limux-host-linux automation_and_mobile -- --nocapture`, `cargo test -p limux-host-linux invalid_automation -- --nocapture`, `cargo test -p limux-host-linux invalid_mobile -- --nocapture`, `cargo test -p limux-host-linux save_to_path_writes_terminal_custom_sidebar_and_beta_settings -- --nocapture`, and `cargo fmt --check`. \
[x] 2026-07-03T23:28:07Z Implement and verify CMUX-compatible pane chrome settings parity from current upstream CMUX `7f94c9352f5267c2c01319d1aaa73b1e2207890e`: local `config get/set` now supports root `paneBorderColor` and `activePaneBorderColor`; color values accept CMUX hex strings with or without `#`, normalize to uppercase `#RRGGBB`, and reject malformed CSS colors loudly; JSON `null` clears to default; host settings load/save round-trips both root keys; and GTK pane roots apply configured normal and focused pane border CSS. Verified by `cargo test -p limux-cli config_pane_chrome -- --nocapture`, `cargo test -p limux-host-linux pane_chrome -- --nocapture`, `cargo test -p limux-host-linux build_window_css_uses_resolved_background_opacity -- --nocapture`, touched-crate clippy, `cargo fmt --check`, `git diff --check`, and `./scripts/check.sh`. \
[x] 2026-07-03T22:47:40Z Implement and verify CMUX-compatible App catalog scalar settings parity: local `config get/set` now supports `app.windowTitleTemplate`, `app.menuBarOnly`, `app.preferredEditor`, `app.openSupportedFilesInCmux`, `app.openMarkdownInCmuxViewer`, `app.iMessageMode`, `app.reorderOnNotification`, `app.sendAnonymousTelemetry`, `app.warnBeforeQuit`, `app.warnBeforeClosingTab`, `app.warnBeforeClosingTabXButton`, `app.hideTabCloseButton`, `app.renameSelectsExistingName`, `app.commandPaletteSearchesAllSurfaces`, `app.workspaceButtonFade`, `app.workspaceTitlebarVisibility`, `app.systemWideHotkeyEnabled`, and `app.devWindowDisplay` with CMUX defaults from upstream `AppCatalogSection.swift`; host settings load/save round-trips the same keys while preserving unrelated settings; and invalid booleans/enums/strings fail loudly. Verified by `cargo test -p limux-cli config_app_scalar -- --nocapture`, `cargo test -p limux-host-linux app_scalar -- --nocapture`, `cargo test -p limux-host-linux save_writes_gtk_and_ghostty_color_schemes -- --nocapture`, touched-crate clippy, `cargo fmt --check`, `git diff --check`, and `./scripts/check.sh`. \
[x] 2026-07-03T21:16:45Z Implement and verify CMUX-compatible Markdown settings parity: local `config get/set` now supports `markdown.fontSize`, `markdown.fontFamily`, and `markdown.maxWidth` with upstream defaults and strict validation; host settings load/save preserves the nested `markdown` section; and `markdown.open` responses include configured effective viewer defaults when no per-command font size is supplied. Verified by `cargo test -p limux-cli config_markdown -- --nocapture`, `cargo test -p limux-host-linux markdown_settings -- --nocapture`, `cargo test -p limux-host-linux markdown_response_metadata -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, `git diff --check`, and `./scripts/check.sh`. \
[x] 2026-07-03T20:51:22Z Implement and verify CMUX-compatible `surface resume` / `surface-resume` binding parity: CLI set/show/get/clear forms now map to `surface.resume.set`, `surface.resume.get`, and `surface.resume.clear`; the live GTK bridge advertises and routes the methods; terminal tabs persist optional resume binding metadata in session JSON; set/clear schedule session saves and publish lifecycle events; and show/get render either the stored command or `No resume binding`. Verified by `cargo test -p limux-cli surface_resume -- --nocapture`, `cargo test -p limux-host-linux surface_resume -- --nocapture`, `cargo test -p limux-host-linux terminal_tab_state_round_trips_restorable_agent_metadata -- --nocapture`, `cargo test -p limux-host-linux capabilities_include_surface_ports_kick_method -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, `git diff --check`, and `./scripts/check.sh`. \
[x] 2026-07-03T19:44:36Z Implement and verify CMUX-compatible `trigger-flash` / `surface.trigger_flash` parity: CLI `trigger-flash` now maps workspace and surface/panel selectors to the live RPC, the GTK bridge advertises and routes `surface.trigger_flash`, and the host marks the addressed or focused live surface's workspace as needing attention while returning `flashed` metadata and publishing `surface.flash_triggered` lifecycle events. Verified by `cargo test -p limux-cli cmux_surface_aliases_map_to_limux_methods -- --nocapture`, `cargo test -p limux-host-linux surface_trigger_flash_route_accepts_cmux_surface_refs -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, `git diff --check`, and `./scripts/check.sh`; direct Xvfb live smoke was blocked because `xvfb-run` is not installed. \
[x] 2026-07-02T23:51:22Z Implement and verify CMUX-compatible Markdown viewer parity: `markdown open <path>`, `markdown <path>`, and live `markdown.open` now validate/canonicalize existing files, accept CMUX routing, direction, focus, and bounded font-size options, open a real WebKit browser split through an encoded `file://` URL, and return canonical path/URL markdown metadata. Verified by `cargo test -p limux-cli cmux_markdown_open -- --nocapture` and `cargo test -p limux-host-linux markdown_open_route_queues_browser_pane -- --nocapture`. \
[x] 2026-07-02T21:05:22Z Implement and verify CMUX-compatible JSON custom-sidebar `workspace.create` parity used by upstream Swift sidebar buttons: actions now support no-param background workspace creation, optional `name`/`title`/`template` title seeds, optional `cwd`/`working_directory`, optional `command` startup injection, strict boolean `focus`, real GTK workspace model creation, session persistence, CMUX workspace payloads, and loud validation for malformed focus params. \
[x] 2026-07-02T20:59:39Z Implement and verify CMUX-compatible JSON custom-sidebar `tab.create` parity used by upstream Swift sidebar plus buttons: actions now parse optional workspace targets from `workspace_id`, `workspace`, `param`, or `value`, default to the active workspace only when omitted, create a real terminal surface in the target workspace focused pane, optionally run `command`/`text` startup content, publish `surface.created` lifecycle events with `origin=tab.create`, persist the session, and fail loudly for explicit unknown workspaces or missing panes. \
[x] 2026-07-02T20:56:01Z Implement and verify live GTK bridge plus JSON custom-sidebar `window.focus` parity used by upstream CMUX Swift sidebar context menus: the live bridge now advertises and routes `window.focus`, explicit `window_id`/`window`/`id` params validate against Limux's current single GTK host window, no-id sidebar calls focus the current host window, unsupported window refs fail loudly, and responses include CMUX-shaped `window_id`/`window_ref` data. \
[x] 2026-07-02T20:51:04Z Implement and verify CMUX-compatible JSON custom-sidebar `surface.run` parity used by upstream Swift sidebar command buttons: actions now parse required `command` text plus optional `surface_id`/`tab_id`, run the command in the addressed terminal or focused terminal when omitted, send a trailing newline through the existing Ghostty terminal injection path, publish redacted surface input events, and fail loudly for missing commands or missing terminal surfaces. \
[x] 2026-07-02T20:46:47Z Implement and verify CMUX-compatible JSON custom-sidebar `clipboard.write(text:)` parity used by upstream Swift sidebar corpus examples: sidebar actions now resolve copy text from `message`, `text`, `param`, or `value`, show copy previews in tooltips, write both GTK standard and primary clipboards when a display is available, and fail loudly when text or display is missing. \
[x] 2026-07-02T20:43:36Z Implement and verify CMUX-compatible JSON custom-sidebar URL action aliases used by upstream Swift sidebar corpus examples: `openURL(param:)`, `open.url(url:)`, and `link.open(url:)` now resolve URLs from `message`, `url`, `param`, or `value`, expose the resolved URL in tooltips, and open through the existing GTK URI launcher while preserving loud missing-URL feedback. \
[x] 2026-07-02T20:40:25Z Implement and verify CMUX-compatible JSON custom-sidebar aliases used by upstream Swift sidebar corpus examples: `selectWorkspace(param)` now routes to workspace selection, while `tab.focus(param)` and `tab.close(param)` parse tab/surface ids and route through existing surface focus/close host helpers with the same strict missing-id validation as other dispatcher actions. \
[x] 2026-07-02T20:38:36Z Implement and verify CMUX-compatible JSON custom-sidebar `tab.togglePin` dispatch used by the upstream two-column cockpit sidebar: static JSON sidebar buttons now parse `tab_id`, `surface_id`, `param`, or `value` handles, resolve the owning workspace/tab, toggle pinned tab state through the existing pane tab-action mutation path, persist the session, and return CMUX-shaped tab action payloads. \
[x] 2026-07-02T20:33:45Z Implement and verify CMUX-compatible JSON custom-sidebar workspace dispatcher actions used by upstream CMUX corpus sidebars: `workspace.focus(param)` aliases workspace selection, `workspace.pin` and `workspace.togglePin` toggle the existing Limux pinned/favorite workspace order, `workspace.markRead` clears workspace notifications through the existing host notification path, `workspace.markAllRead` clears all unread notification state, and workspace/surface parameters accept CMUX `param`/`value` aliases while preserving loud validation for missing IDs. \
[x] 2026-07-02T20:03:04Z Extend and verify CMUX-compatible JSON custom-sidebar `workspace.reorder` dispatch to all single-target forms: actions now accept exactly one of `index`, `before_workspace_id`, or `after_workspace_id`, reject missing/conflicting targets loudly, resolve before/after workspace refs into absolute order indexes, and preserve the live reorder/event/response behavior added for the index form. \
[x] 2026-07-02T19:58:07Z Implement and verify CMUX-compatible JSON custom-sidebar `workspace.reorder(workspace_id,index)` dispatch: actions now validate the required workspace id/ref and non-negative target index, clamp index targets like CMUX, mutate live workspace order, publish retained `workspace.reordered` events on changed order, return CMUX-shaped `plan`/`events` response metadata, and preserve loud errors for invalid params. \
[x] 2026-07-02T19:51:18Z Implement and verify CMUX-compatible parameterized JSON custom-sidebar dispatcher actions for documented `workspace.select(workspace_id)` and `surface.focus(surface_id)`: JSON actions now parse `params` or direct CMUX parameter keys, validate missing/malformed params loudly, and dispatch through existing live host workspace selection and surface focus helpers. \
[x] 2026-07-02T19:43:08Z Implement and verify runtime enforcement of CMUX `customSidebars.beta.enabled`: live `sidebar.custom.validate` and `sidebar.custom.reload` now expose `beta_enabled`/`disabled` state and hide sidebars while disabled, and `sidebar.custom.select`/`sidebar.custom.open` reject loudly with the beta-disabled reason instead of rendering or opening custom sidebars when the feature is off. \
[x] 2026-07-02T19:37:12Z Implement and verify CMUX-compatible static styling fields for JSON custom sidebars: the GTK renderer now parses and applies `spacing`, `alignment`, `padding`, `font`, `weight`, `color`, `background`, and `size` for declarative JSON nodes, uses CMUX font-size/weight/color token mappings where possible, sizes spacer nodes, and rejects malformed numeric style fields loudly. \
[x] 2026-07-02T19:31:41Z Implement and verify CMUX-compatible no-parameter dispatcher actions for JSON custom-sidebar buttons: non-`log`/`openURL` action types now map supported CMUX no-param methods through existing host helpers for `workspace.next`, `workspace.previous`, `workspace.last`, and `notification.jump_to_unread`, while parameterized dispatcher methods such as `workspace.select` and `surface.focus` remain loud unsupported paths instead of silently doing nothing. \
[x] 2026-07-02T19:25:33Z Implement and verify CMUX-compatible JSON custom-sidebar runtime selection: `sidebar select/open` now routes through the live socket, the GTK bridge advertises/parses `sidebar.custom.validate`, `sidebar.custom.reload`, `sidebar.custom.select`, and `sidebar.custom.open`, and the host validates `~/.config/cmux/sidebars` before rendering static declarative JSON sidebars in the right sidebar while rejecting Swift runtime rendering and pane-open requests loudly. \
[x] 2026-07-02T19:11:22Z Implement and verify CMUX-compatible config parity for remaining Linux-meaningful terminal, custom-sidebar, and beta-feature settings: local `config get/set` supports nested terminal performance/text-box/resume/memory keys, `customSidebars.renderer`, `customSidebars.beta.enabled`, `rightSidebar.beta.feed.enabled`, `rightSidebar.beta.dock.enabled`, `extensions.beta.enabled`, and `remoteTmux.beta.enabled` with strict boolean/integer/decimal/string/enum/JSON-array validation, while host settings load/save validates and round-trips the same keys without dropping unrelated siblings. \
[x] 2026-07-02T17:23:26Z Implement and verify CMUX-compatible shell working-directory/state report parity: the live bridge advertises/routes `surface.report_pwd` and `surface.report_shell_state` plus `report_pwd`/`report_shell_state` aliases, `report_pwd` normalizes `file://` path reports and updates the addressed terminal surface cwd, `report_shell_state` strictly records `prompt|running` in pruned in-memory per-surface metadata, and CLI aliases map path/state plus workspace/surface/panel flags with Limux/CMUX environment fallbacks. \
[x] 2026-07-02T17:17:26Z Implement and verify CMUX-compatible shell port metadata parity: the live bridge advertises/routes `surface.report_tty`, `surface.report_ports`, and `surface.clear_ports` plus `report_tty`/`report_ports`/`clear_ports` aliases, records per-surface TTY and manually reported port metadata in bounded in-memory state pruned against live surfaces, merges reported ports into `sidebar-state` without duplicates, and CLI aliases map workspace/surface/panel flags plus Limux/CMUX environment fallbacks with strict port validation. \
[x] 2026-07-02T17:05:49Z Implement and verify CMUX-compatible `surface.ports_kick` parity: the live bridge advertises/routes `surface.ports_kick` plus dash/underscore aliases, validates workspace and surface/panel refs, returns focused/selected surface metadata with current on-demand procfs port rows, and CLI `ports-kick`/`ports_kick` maps workspace/surface/panel/reason flags plus Limux/CMUX environment fallbacks. \
[x] 2026-07-02T16:57:59Z Implement and verify CMUX-compatible sidebar port text/policy parity: discovered port JSON rows now expose the configured `sidebar.openPortLinksInCmuxBrowser` browser-open policy, and non-JSON `sidebar-state` renders CMUX-style comma-separated `ports=...` output for parser compatibility. \
[x] 2026-07-02T16:53:53Z Implement and verify CMUX-compatible sidebar port discovery: `sidebar-state` now discovers workspace-attributed child process listening TCP ports through one on-demand `/proc` scan, returns CMUX-style port/address/url/process rows, preserves `sidebar.showPorts` and `sidebar.hideAllDetails` gating, fails loudly on procfs read/parse errors, and avoids background polling or disk writes. \
[x] 2026-07-02T16:42:36Z Implement and verify CMUX-compatible left-sidebar branch/directory row rendering for `sidebar.branchLayout`: workspace rows now include detected Git branch metadata in the branch/directory detail row, `vertical` renders branch and directory on separate lines, `inline` renders them on one line, non-Git workspaces keep the directory-only row, and branch discovery reads `.git/HEAD` metadata directly instead of spawning `git` during sidebar refresh. \
[x] 2026-07-02T16:37:33Z Implement and verify CMUX-compatible `sidebar.branchLayout` configuration parity: local `config get/set` supports the nested string key with CMUX `vertical` default and strict `vertical|inline` validation, host settings load/save/defaults preserve the enum, and malformed persisted values fail loudly. \
[x] 2026-07-02T16:32:40Z Implement and verify CMUX-compatible configuration parity for remaining sidebar boolean keys `sidebar.makePullRequestsClickable`, `sidebar.openPullRequestLinksInCmuxBrowser`, `sidebar.openPortLinksInCmuxBrowser`, and `sidebar.showSSH`: local `config get/set` supports all four nested booleans with CMUX true defaults, host settings load/save/defaults preserve them, and malformed persisted values fail loudly. \
[x] 2026-07-02T16:28:19Z Implement and verify CMUX-compatible `sidebar.showPorts` visibility setting parity: local `config get/set` supports the nested boolean with CMUX true default, host settings load/save/defaults preserve it, malformed persisted values fail loudly, and sidebar-state port rows are gated by `sidebar.showPorts` plus the master `sidebar.hideAllDetails` detail visibility switch so future detected port rows cannot leak when disabled. \
[x] 2026-07-02T16:21:43Z Implement and verify CMUX-compatible `sidebar.watchGitStatus` and `sidebar.showPullRequests` parity: local `config get/set` supports both nested booleans with CMUX true defaults, host settings load/save/defaults preserve them, malformed persisted values fail loudly, terminal startup env now emits managed `CMUX_NO_GIT_WATCH` and `CMUX_NO_PR_WATCH` using CMUX's truth table, and pane-create startup environments cannot override those managed policy vars. \
[x] 2026-07-02T16:15:10Z Implement and verify CMUX-compatible `sidebar.rightMaxWidth` parity: local `config get/set` supports the nested numeric key with CMUX default `1200`, host settings load/save accepts optional `rightMaxWidth`, malformed or non-positive values fail loudly, configured values round and clamp to CMUX's `276..4096` settings range, and the GTK right-sidebar shell applies the CMUX width policy on startup and config reload while reserving terminal content width. \
[x] 2026-07-02T16:01:51Z Implement and verify CMUX-compatible right-sidebar metadata visibility settings parity: local `config get/set` supports `sidebar.showCustomMetadata`, `sidebar.showProgress`, and `sidebar.showLog` with CMUX true defaults, host settings load/save and default settings generation preserve them, malformed sidebar booleans fail loudly, and right-sidebar Status, Progress, and Log sections follow the configured visibility flags. \
[x] 2026-07-02T15:55:57Z Implement and verify CMUX-compatible sidebar detail settings parity: local `config get/set` supports `sidebar.hideAllDetails`, `sidebar.wrapWorkspaceTitles`, and `sidebar.showWorkspaceDescription` with CMUX defaults, host settings load/save and default settings generation preserve them, malformed sidebar booleans fail loudly, live config reload resyncs open workspace sidebar rows, workspace title wrapping follows `wrapWorkspaceTitles`, workspace descriptions follow `showWorkspaceDescription`, and `hideAllDetails` suppresses sidebar description/path/notification detail rows while preserving unread dots. \
[x] 2026-07-02T15:49:27Z Implement and verify CMUX-compatible sidebar display settings parity: local `config get/set` supports `sidebar.showNotificationMessage` and `sidebar.showBranchDirectory` with CMUX true defaults, host settings load/save and default settings generation preserve them, malformed sidebar booleans fail loudly, unread sidebar notification text follows `showNotificationMessage`, and workspace directory rows follow `showBranchDirectory` for new/restored workspaces. \
[x] 2026-07-02T15:38:52Z Implement and verify CMUX-compatible `notifications.command` and `notifications.hooksMode` parity: local `config get/set` supports both keys with CMUX defaults, host settings load/save and default settings generation preserve them, malformed `hooksMode` values fail loudly, notification hook policy can set `effects.command`, and delivered notifications spawn the configured command with CMUX/Limux title, subtitle, and body environment variables while reaping the child process. \
[x] 2026-07-02T15:29:28Z Implement and verify CMUX-compatible notification visual settings parity: local `config get/set` supports `notifications.dockBadge`, `notifications.showInMenuBar`, `notifications.unreadPaneRing`, and `notifications.paneFlash` with CMUX true defaults, host settings load/save and default settings generation preserve the keys, malformed `unreadPaneRing` values fail loudly, unread workspace visuals follow `unreadPaneRing`, and notification hook policy payloads/effects use the configured `paneFlash` value. \
[x] 2026-07-02T15:15:53Z Implement and verify CMUX-compatible `notifications.suppressOnlyFocusedSurface` parity: local `config get/set` supports the CMUX boolean key with default false, host settings load/save and default settings generation preserve the key, malformed stored values fail loudly, and desktop notification suppression now switches between workspace-level suppression and exact focused-surface suppression according to the setting. \
[x] 2026-07-02T15:06:08Z Implement and verify CMUX-compatible sampled CPU-percent `top` parity: live `system.top` now samples `/proc` twice over a bounded `sample_ms` window, emits `cpu_percent` for the app and child process groups, accepts validated `sample_ms` over RPC and CLI, sorts `top --sort cpu` by sampled percent when present, and renders CMUX-shaped TSV CPU percent values instead of cumulative tick counts. \
[x] 2026-07-02T14:55:23Z Implement and verify live CMUX advanced tab-action parity: `tab.action` now accepts CMUX aliases for `mark-read`, `mark-unread`, `reload`, `duplicate`, `close-left`, `close-right`, and `close-others`, routes browser-only duplicate/reload through real browser surfaces, applies relative close actions while skipping pinned tabs, marks Limux workspaces read/unread for tab read-state actions, and returns CMUX-shaped created/closed/skipped-pinned/reloaded/unread metadata. \
[x] 2026-07-02T14:42:21Z Implement and verify live CMUX tab metadata action parity: `tab.action` now routes `rename`, `clear-name`, `pin`, and `unpin` through the GTK host for addressed tabs, CLI `tab-action` serializes `--surface`, `--window`, and `--focus true|false`, `rename-tab` accepts surface aliases and surface env fallbacks, and unsupported live tab actions such as `duplicate` remain loud errors until their real host semantics are implemented. \
[x] 2026-07-02T14:27:52Z Implement and verify CMUX-compatible `move-tab-to-new-workspace` parity: top-level `move-tab-to-new-workspace` now wraps `tab.action move-to-new-workspace`, defaults `--focus` to false, accepts tab/surface/workspace/window/title flags, rejects caller-supplied `--action`, standalone control state moves the requested surface into a newly created workspace, and the live GTK bridge routes the CMUX action through the existing tab-to-workspace host path with tab/surface alias normalization. \
[x] 2026-07-02T14:14:05Z Implement and verify CMUX-compatible `terminal.autoResumeAgentSessions` parity: `config get/set terminal.autoResumeAgentSessions` now uses the CMUX `true` default, writes nested terminal settings JSON, rejects invalid boolean values loudly, host settings load/save preserves the key, and disabled hosts restore saved agent panes without automatically running their resume commands. \
[x] 2026-07-02T14:03:16Z Implement and verify CMUX-compatible `app.focusPaneOnFirstClick` parity: `config get/set app.focusPaneOnFirstClick` now uses the CMUX runtime `false` default, writes nested app settings JSON, rejects invalid boolean values loudly, host settings load/save preserves the key, and terminal mouse press click-to-focus is gated by the setting. \
[x] 2026-07-02T13:54:58Z Implement and verify CMUX-compatible `app.workspaceInheritWorkingDirectory` parity: `config get/set app.workspaceInheritWorkingDirectory` now uses the CMUX `true` default, writes nested app settings JSON, rejects invalid boolean values loudly, host settings load/save preserves the key, explicit workspace cwd remains authoritative, enabled hosts inherit the active workspace folder/cwd for new workspaces, and disabled hosts leave new workspace cwd unset so terminal defaults can apply. \
[x] 2026-07-02T13:45:05Z Implement and verify CMUX-compatible `app.keepWorkspaceOpenWhenClosingLastSurface` parity: `config get/set app.keepWorkspaceOpenWhenClosingLastSurface` now uses the CMUX `false` default, writes nested app settings JSON, rejects invalid boolean values loudly, host settings load/save preserves the key, and enabled hosts keep the workspace alive after the final surface closes by creating a fresh terminal surface. \
[x] 2026-07-02T13:35:05Z Implement and verify CMUX-compatible `app.appearance` config parity: `config get/set app.appearance` now reads the CMUX `system` default, writes nested settings JSON while preserving unrelated app and notification keys, rejects invalid values loudly, and the GTK host now prefers `app.appearance` for the app color scheme while keeping the legacy Limux appearance section synced on save. \
[x] 2026-07-02T13:29:37Z Implement and verify CMUX-compatible notification sound config parity: `config get/set notifications.sound` now supports CMUX documented sound presets plus existing Linux aliases without silent fallback, `config get/set notifications.customSoundFilePath` writes the custom sound path, host config load/save preserves both settings, invalid sound values fail loudly, and `custom_file` emits a Linux `sound-file` desktop notification hint when configured. \
[x] 2026-07-02T13:21:17Z Implement and verify CMUX-compatible local `config get/set` support for workspace placement settings: `app.newWorkspacePlacement` and `workspaceGroups.newWorkspacePlacement` now read the CMUX `afterCurrent` default, write nested settings JSON while preserving unrelated app, notification, and `workspaceGroups.byCwd` data, and reject invalid placement values loudly. \
[x] 2026-07-02T13:12:59Z Implement and verify CMUX-compatible local `config get/set` support for agent notification settings: `notifications.agentPermissionPrompt`, `notifications.agentTurnComplete`, and `notifications.agentIdleReminder` now read CMUX defaults, write nested `notifications` JSON while preserving unrelated settings, and reject invalid boolean or turn-complete policy values loudly. \
[x] 2026-07-02T13:06:35Z Implement and verify CMUX-compatible agent notification gating from upstream `128be4c1ac`: Limux now loads/saves `notifications.agentPermissionPrompt`, `notifications.agentTurnComplete`, and `notifications.agentIdleReminder`, classifies Claude hook notifications as turn-complete/needs-permission/idle-reminder, detects pending Claude background work from `session_crons` and running `background_tasks`, accepts snake_case and camelCase agent notification metadata over `notification.create`, and applies the CMUX per-category suppression/delivery decision table. \
[x] 2026-07-02T12:50:18Z Implement and verify local CMUX custom sidebar parity in `limux-cli`: `docs sidebars` documents `~/.config/cmux/sidebars`, `sidebar validate/reload` validate `.swift` and `.json` sources with Swift precedence and JSON output, and `sidebar select/open` validate the requested source before failing loudly for the missing runtime custom-sidebar renderer. \
[x] 2026-07-02T12:42:43Z Implement and verify CMUX-compatible local `top --sort cpu` diagnostics: Limux now reads cumulative user+system CPU ticks from `/proc/<pid>/stat`, aggregates `cpu_ticks` per direct-child process group, sorts local `top` rows by CPU ticks, and renders measured TSV CPU values instead of a placeholder. \
[x] 2026-07-02T12:39:39Z Implement and verify native live-bridge CMUX `system.top` parity for local Limux process diagnostics: the bridge now advertises/routes `system.top`, top-level `top` calls it directly, `--workspace` filters process groups using Limux child-process workspace attribution, `--window window:1` validates the current single GTK host window, and `--all` requests the unfiltered local process view. \
[x] 2026-07-02T12:31:12Z Implement and verify additional local CMUX `top` parity in `limux-cli`: `top --sort mem|proc` now reorders local process groups, and `top --flat` / `top --format tsv` render CMUX-shaped TSV rows from Limux's current process-group diagnostics while scoped top and CPU sorting remain explicit unsupported paths. \
[x] 2026-07-02T12:24:13Z Implement and verify native CMUX topology parity in the live GTK bridge: `window.list` and `system.tree` are now advertised/routed by the bridge, `system.tree` returns a live window/workspace/pane/surface tree tagged `limux_live_system_tree`, and top-level `tree` now calls the native method instead of rebuilding topology client-side. \
[x] 2026-07-02T12:15:50Z Implement and verify CMUX `docs dock` parity in `limux-cli`: the docs index now lists `dock`, `docs dock` prints dock/right-sidebar custom-control guidance, and `doc`/`controls`/`right-sidebar`/`dock-json` aliases normalize like CMUX. \
[x] 2026-07-02T12:11:05Z Implement and verify CMUX-compatible top-level `tree` and partial `top` routing in `limux-cli`: `tree` now assembles window/workspace/pane/surface topology from existing list APIs with text/JSON output, and `top` routes to Limux `system.memory` diagnostics while failing loudly for scoped/sorted/TSV CMUX `system.top` behavior that is not implemented yet. \
[x] 2026-07-02T11:57:56Z Implement and verify CMUX-compatible Ghostty theme catalog parity in `limux-cli`: theme listing now discovers Ghostty theme files from Linux resource, XDG data, repo, and user theme directories; JSON/text output reports CMUX-style theme rows with current light/dark badges; and `themes set` canonicalizes/rejects names against the discovered catalog while preserving arbitrary-name behavior when no catalog is available. \
[x] 2026-07-02T11:51:49Z Implement and verify additional CMUX/tmux local command parity in `limux-cli`: `pipe-pane` now accepts trailing shell command text as well as `--command`, `wait-for` uses CMUX's 30-second default with fractional non-negative timeout parsing and loud invalid-timeout failures, and `find-window` now emits CMUX-shaped multi-match/no-match text with case-insensitive title matching plus `--content` and `--select` routing. \
[x] 2026-07-02T11:45:50Z Implement and verify exact CMUX/tmux `split-window -l` live sizing: Limux now validates `-l` without using the old 100-cell approximation, resolves target pane geometry through `pane.list`/`surface.list`, computes the initial divider from real rows/columns with the CMUX clamp formula, and fails loudly when explicit sizing lacks workspace/pane geometry. \
[x] 2026-07-02T11:40:29Z Implement and verify CMUX/tmux pane geometry exposure foundation: Ghostty terminal health now carries cell pixel dimensions, host `pane.list` and `surface-health` expose active terminal rows/columns/pixel/cell metrics, and CLI tmux format rendering consumes real `pane_width`, `pane_height`, and cell-size fields when present. \
[x] 2026-07-02T11:35:44Z Implement and verify CMUX/tmux `split-window -P -F` print-format parity in `limux-cli`: formatted split output now renders pane, surface, and workspace fields from the created split payload, falls back to the new pane handle, and `--format` no longer leaks into command text. \
[x] 2026-07-02T11:31:24Z Implement and verify CMUX/tmux split sizing request parity: `split-window -l <cells>` now serializes a bounded `initial_divider_position`, `surface.split` preserves that divider request, and the live host applies it as the initial split ratio while exact terminal-cell geometry remains tracked separately. \
[x] 2026-07-02T11:24:17Z Implement and verify CMUX/tmux detached split focus parity: `split-window -d` serializes `focus:false`, `surface.split` preserves detached focus requests, and host terminal launch activation honors the requested focus setting. \
[x] 2026-07-02T11:17:05Z Implement and verify CMUX/tmux local split/layout parity in `limux-cli`: `split-window`/`splitw` route to `surface.split` with tmux direction, cwd, target, command, and `-P` output handling, and `select-layout` routes to `workspace.equalize_splits` with main-layout orientation mapping. \
[x] 2026-07-02T11:10:36Z Implement and verify CMUX/tmux lifecycle alias parity in `limux-cli` for `neww`, `select-window`/`selectw`, `select-pane`/`selectp`, `kill-window`/`killw`, `kill-pane`/`killp`, and `renamew`, including no-socket help probes, `-t`/`--target` parsing, pane focus, workspace select/close, surface close, and rename title parsing. \
[x] 2026-07-02T10:56:34Z Implement and verify CMUX-compatible OpenCode fork-support version probing for `sessions list`, including minimum version `1.14.50`, local executable probing from saved launch metadata, missing absolute executable rejection, remote-like working-directory bypass, sanitized probe environment, per-command probe caching, focused session diagnostics tests, and matrix documentation. \
[x] 2026-07-02T10:48:24Z Implement and verify CMUX-compatible Claude workflow-container transcript resolution for `sessions list`, including unambiguous sibling transcript discovery under Claude project roots, resolved session-id/path diagnostics, fork-command gating on the resolved transcript, focused tests, CLI clippy, and SCPlus lint. \
[x] 2026-07-02T10:40:37Z Implement and verify CMUX-compatible session-list trust diagnostics in `limux-cli`, including native CMUX camelCase hook-store field aliases, out-of-range saved PID tolerance, Claude transcript-backed restorable gating from direct `transcriptPath` and `CLAUDE_CONFIG_DIR` project transcripts, `fork_unavailable_reason`, `fork_startup_input_available`, and stale-PID restore-block metadata. \
[x] 2026-07-02T10:30:42Z Implement and verify CMUX-compatible session fork-command diagnostics in `limux-cli`, including concrete `fork_command` JSON/text availability for Codex, Claude, OpenCode, Pi, and OMP saved launch records, safe launch-flag/environment preservation, credential stripping, focused sessions/fork-command tests, and CLI clippy. \
[x] 2026-07-02T10:23:25Z Implement and verify the public Codex Teams production launcher flow in `limux-cli`, including private Codex app-server startup/readiness probing, root Codex `--remote` launch, hidden watcher process startup, Feed approval/subagent bridge integration, socket auth propagation, private helper logs, process-group cleanup for wrapper-spawned descendants, direct launcher smoke, focused tests, clippy, and full workspace checks. \
[x] 2026-07-02T10:12:52Z Implement and verify the hidden Codex Teams watcher stream in `limux-cli`, including `__codex-teams-watch` dispatch, required argument validation, Codex app-server initialize/backfill/subscribe loop, Feed approval forwarding and response mapping, bounded related approval item cache, subagent thread state tracking, managed `surface.split` opening, tab rename/layout polish calls, subscription de-duplication, and focused tests/clippy. \
[x] 2026-07-02T10:07:12Z Implement and verify the Codex Teams app-server websocket JSON-RPC client foundation in `limux-cli`, including pinned websocket dependencies, URL validation, `initialize`/`initialized` handshake, interleaved notification handling, request/response id matching, JSON-RPC success/error responses back to Codex, ping/pong handling, invalid-frame failures, and a local websocket protocol test. \
[x] 2026-07-02T10:03:27Z Implement and verify the Codex Teams watcher model and managed subagent split-plan foundation in `limux-cli`, including Codex app-server thread/subagent-spawn parsing, attachability status checks, CMUX-positioned root `--remote` args, shell-quoted subagent resume command construction, one-shot startup script generation, CMUX Teams startup environment metadata, `surface.split` params, and focused tests. \
[x] 2026-07-02T09:58:17Z Implement and verify the pure Rust Codex Teams app-server approval bridge foundation in `limux-cli`, including CMUX-shaped Feed event construction, Feed decision extraction, app-server approval response mapping for command/file/permissions requests, bounded related-item snapshots, explicit Codex `-C`/`--cd`/`--cwd` resolution and validation, and focused tests ported from CMUX `FeedCoordinatorTests`. \
[x] 2026-07-02T09:50:44Z Implement and verify CMUX `surface.split`/`pane.create` Codex watcher startup-field parity, including `initial_command`, `working_directory`, and `startup_environment` parsing, strict startup env validation, launch-time terminal cwd/env wiring, and focused bridge tests. \
[x] 2026-07-02T09:40:03Z Implement and verify CMUX-compatible Codex app-server Feed permission action capability policy for right-sidebar and native notification decisions, including filtering `once`/`always`/`all` actions from app-server `available_decisions` and amendment payloads while preserving existing non-app-server Feed actions. \
[x] 2026-07-02T09:33:00Z Implement and verify explicit CMUX `codex-teams` CLI surface handling, including local help text, loud `not_supported` execution failure for the missing Codex app-server watcher/subagent orchestration, focused CLI tests, direct smoke commands, clippy, and parity matrix updates. \
[x] 2026-07-02T09:32:00Z Implement and verify CMUX-compatible Linux inline Feed notification actions for pending permission, exit-plan, and question rows, including freedesktop action mapping, shared Feed reply routing, focused Feed/notification tests, clippy, and parity matrix updates. \
[x] 2026-07-02T09:31:18Z Implement and verify CMUX-compatible native Feed notifications for pending actionable permission, exit-plan, and question rows, including workspace/surface targeting, telemetry suppression, focused host tests, clippy, and parity matrix updates. \
[x] 2026-07-02T09:09:12Z Implement and verify CMUX-compatible pid-anchored Codex transcript diagnostics for wrapper-started sessions, including `/proc/<pid>/fd` transcript discovery under the configured Codex home, native Codex session-id/path reporting, strict sessions-root JSONL validation, focused tests, SCPlus lint, and CLI clippy. \
[x] 2026-07-02T08:55:16Z Implement and verify CMUX-compatible Codex wrapper-owned session-start emission for hand-typed `codex` in Limux terminals, including `LIMUX_CLI`/`CMUX_CLI` terminal env wiring, wrapper session/pid env metadata, and hook persistence support for wrapper-provided session id and pid values. \
[x] 2026-07-02T08:42:24Z Implement and verify CMUX-compatible Codex per-surface wrapper shim injection for terminal tabs, including shim directory creation, `PATH` prepending, `CMUX_CODEX_WRAPPER_SHIM`/`LIMUX_CODEX_WRAPPER_SHIM` exports, and Codex launch metadata preservation for real Codex hook events. \
[x] 2026-07-02T08:35:24Z Implement and verify CMUX-compatible JSONC preprocessing for `config doctor`, including line comments, block comments, trailing commas, top-level object validation after sanitization, and preservation of comment-like text inside strings. \
[x] 2026-07-02T08:31:28Z Implement and verify CMUX-compatible `config doctor --path` and project config doctor target parity, including custom target parsing, default project `.cmux/cmux.json`/`cmux.json` inclusion, top-level object validation, key listing, and missing/error statuses. \
[x] 2026-07-02T08:26:54Z Implement and verify CMUX-compatible project `cmux.json` workspace environment loading for `new-workspace` and `workspace create`, including upward `.cmux/cmux.json`/`cmux.json` search order, `newWorkspaceCommand` workspace env extraction, protected-key rejection, and CLI override precedence. \
[x] 2026-07-02T07:23:28Z Implement and verify CMUX-compatible `new-workspace` and `workspace create` name/title flag serialization so `--name` and `--title` reach the live `workspace.create` bridge alongside cwd, command, and workspace env params. \
[x] 2026-07-02T07:17:14Z Implement and verify CMUX-compatible Kiro Feed denial exit-status parity: Kiro deny and malformed permission decisions fail closed with exit status 2, allow modes exit 0 with `{}`, and generated Kiro Feed hook commands preserve denial status instead of masking it with a generic fallback. \
[x] 2026-07-02T06:49:44Z Implement and verify CMUX OMP extension hook setup, lifecycle-only event bridge, base64 launch-argv capture compatibility, native session restore command, and protected extension uninstall parity. \
[x] 2026-07-02T06:40:48Z Implement and verify CMUX Rovo Dev YAML hook setup, lifecycle-only event bridge, native restore command, and safe marked-block uninstall parity. \
[x] 2026-07-02T06:33:51Z Implement and verify CMUX Antigravity hook group setup, Feed PreToolUse/PostToolUse bridge, Antigravity decision JSON rendering, and replay-style session restore parity. \
[x] 2026-07-02T06:29:28Z Implement and verify CMUX Kiro CLI agent hook setup, Feed preToolUse/postToolUse bridge, Kiro side-effect approval classification, and native resume parity. \
[x] 2026-07-02T06:23:57Z Implement and verify CMUX Cursor CLI flat hook setup, Feed beforeShellExecution bridge, and native resume parity. \
[x] 2026-07-02T06:20:12Z Implement and verify CMUX nested JSON hook setup, Feed PreToolUse bridge, and native resume parity for Copilot, CodeBuddy, Factory, and Qoder. \
[x] 2026-07-02T06:15:56Z Implement and verify CMUX Grok hook setup, Feed PreToolUse bridge, and native Grok resume command parity. \
[x] 2026-07-02T06:08:15Z Implement and verify CMUX Feed hook setup wiring for Claude and Gemini JSON hook integrations. \
[x] 2026-07-02T06:03:00Z Implement and verify direct right-sidebar Feed exit-plan and question controls for pending CMUX requests. \
[x] 2026-07-02T05:57:12Z Implement and verify direct right-sidebar Feed permission controls for pending Codex/CMUX requests. \
[x] 2026-07-02T05:51:07Z Implement and verify CMUX `feed tui [--opentui|--legacy]` routing to Limux's live Feed right-sidebar UI surface. \
[x] 2026-07-02T05:47:22Z Implement and verify CMUX `feed clear [--yes|-y]` local workstream history deletion plus live `feed.clear` retained Feed clearing. \
[x] 2026-07-02T05:40:41Z Implement and verify explicit CMUX top-level `feed tui` and `feed clear` `not_supported` surfacing while preserving `feed --help` and `hooks feed` behavior. \
[x] 2026-07-02T05:38:17Z Implement and verify explicit CMUX `remote`/`remotes` cloud device-registry command `not_supported` surfacing while preserving no-socket help probes. \
[x] 2026-07-02T05:31:48Z Implement and verify CMUX-compatible socket password auth semantics for CLI `--password`, `CMUX_SOCKET_PASSWORD`, password file lookup, password socket mode, JSON `auth.login`, and legacy `auth <password>`. \
[x] 2026-07-02T05:21:10Z Implement and verify CMUX `settings open [target]` target alias parsing, bridge validation, activate parsing, and target echo output. \
[x] 2026-07-02T05:12:19Z Implement and verify CMUX `themes list/set/clear` Ghostty managed theme block parity with light/dark selections and JSON output. \
[x] 2026-07-02T05:06:48Z Implement and verify CMUX `settings open` live host settings dialog parity through the `settings.open` bridge method. \
[x] 2026-07-02T04:59:27Z Implement and verify CMUX workspace-group collapsed sidebar row hiding while keeping anchors and active workspaces visible. \
[x] 2026-07-02T04:55:46Z Implement and verify mode-specific rendered GTK right-sidebar panels for Files, Find, Vault, Sessions, Feed, and Dock previews. \
[x] 2026-07-02T04:51:03Z Implement and verify a rendered GTK right-sidebar metadata panel for CMUX mode/status/progress/log visibility. \
[x] 2026-07-02T04:43:27Z Implement and verify explicit CMUX Remote/SSH CLI `not_supported` surfacing while preserving no-socket help probes. \
[x] 2026-07-02T04:40:48Z Implement and verify CMUX sidebar metadata command help and non-JSON `sidebar-state` output parity. \
[x] 2026-07-02T04:37:18Z Implement and verify CMUX sidebar status/progress/log command state parity in CLI, live bridge, host state, and retained events. \
[x] 2026-07-02T04:25:15Z Implement and verify CMUX `right-sidebar` visibility, mode, focus, and state command parity in CLI and live bridge paths. \
[x] 2026-07-02T04:14:32Z Implement and verify CMUX command-position `--json` and `--id-format` presentation flag parity outside browser commands. \
[x] 2026-07-02T04:10:06Z Implement and verify no-socket CMUX command help probes for socket-backed commands and legacy aliases. \
[x] 2026-07-02T04:05:28Z Implement and verify CMUX `sessions list/debug` and `session-debug` no-socket session diagnostics with stale saved-PID reporting. \
[x] 2026-07-02T03:50:01Z Implement and verify CMUX tmux `list-windows`/`list-panes` format output parity plus `lsw`/`lsp` aliases. \
[x] 2026-07-02T03:45:39Z Implement and verify CMUX tmux short alias parity for `capturep`, `display/displayp`, `resizep`, `respawnp`, `setb`, `pasteb`, and `show-buffer/showb` with `-b` buffer names. \
[x] 2026-07-02T03:41:46Z Implement and verify CMUX tmux `display-message -p/-F/-t` format expansion parity with live Limux context, unknown-key stripping, trim, and fallback behavior. \
[x] 2026-07-02T02:00:00Z Implement and verify live bridge CMUX browser frame selection/main reset plus download wait CLI/API parity. \
[x] 2026-07-02T01:51:37Z Implement and verify CMUX workspace environment parity for `new-workspace --env/--env-file`, `workspace.create workspace_env`, terminal inheritance, and `workspace env --mask`. \
[x] 2026-07-02T01:39:20Z Implement and verify CMUX Feed `~/.cmuxterm/workstream.jsonl` audit persistence with bounded rotation. \
[x] 2026-07-02T01:35:42Z Implement and verify CMUX surface-targeted notification metadata parity for `created_at`, surface/pane refs, and `tab_title` rows/events. \
[x] 2026-07-02T01:30:11Z Implement and verify CMUX durable bounded `~/.cmuxterm/events.jsonl` event logging with 16 MiB rotation and oversized event-frame truncation. \
[x] 2026-07-02T01:27:16Z Implement and verify CMUX config `sidebar-font-size` and `surface-tab-bar-font-size` get/set parity while preserving unrelated Limux settings JSON. \
[x] 2026-07-02T01:05:00Z Refresh the CMUX audit clone to current `manaflow-ai/cmux` main commit `871aa710b30d0775f5f18f0bf98f57cdb42047f4` and verify the Limux fork is ahead of `am-will/main` without pushing to `am-will`. \
[x] 2026-07-02T01:05:00Z Implement and verify CMUX retained `events.stream` parity slice with replay ack metadata, category/name filters, heartbeat frames, and live socket takeover. \
[x] 2026-07-02T01:05:00Z Implement and verify CMUX Feed and agent event publication for `feed.item.received`, `feed.item.completed`, `feed.item.resolved`, and `agent.hook.<HookEventName>`. \
[x] 2026-07-02T01:05:00Z Implement and verify CMUX notification event publication for created, read, removed, and cleared notification lifecycle changes. \
[x] 2026-07-02T01:05:00Z Implement and verify `limux events --reconnect` resumes from the last processed sequence instead of failing before socket streaming. \
[x] 2026-07-02T00:58:43Z Implement and verify CMUX tmux `surface.respawn`/`respawn-pane`, stricter buffer/hook/display-message behavior, and explicit unsupported placeholder reporting. \
[x] 2026-07-02T00:44:15Z Implement and verify live bridge CMUX tmux `pane.swap` and `swap-pane` parity for exchanging selected surfaces between panes. \
[x] 2026-07-02T00:38:55Z Implement and verify live bridge CMUX tmux `pane.break` and `break-pane` parity for moving surfaces into new workspaces. \
[x] 2026-07-02T00:33:35Z Implement and verify live bridge CMUX tmux `pane.join` and `join-pane` parity for moving surfaces into target panes. \
[x] 2026-07-02T00:28:44Z Implement and verify live bridge CMUX tmux `pane.resize` and `resize-pane` parity by mutating persisted GTK split ratios. \
[x] 2026-07-02T00:22:28Z Implement and verify live bridge CMUX tmux `surface.clear_history` and `clear-history` parity for addressed or focused terminal surfaces. \
[x] 2026-07-02T00:16:33Z Implement and verify live bridge CMUX tmux `pane.last` and `last-pane` parity using live pane focus history. \
[x] 2026-07-02T00:11:57Z Implement and verify live bridge CMUX tmux workspace navigation parity for `next-window`, `previous-window`, and `last-window`. \
[x] 2026-07-02T00:05:19Z Implement and verify CLI and live bridge CMUX `surface.drag_to_split`, `split-off`, and `drag-surface-to-split` parity for moving surfaces into new split panes. \
[x] 2026-07-02T00:00:48Z Implement and verify CLI and live bridge CMUX `surface.split` and `new-split` parity for creating split terminal panes from surfaces. \
[x] 2026-07-01T23:57:14Z Implement and verify CLI and live bridge CMUX `surface.refresh` and `refresh-surfaces` parity for terminal surface redraws. \
[x] 2026-07-01T23:47:48Z Implement and verify CLI and live bridge CMUX `surface.reorder` and `reorder-surface` parity for reordering surfaces within panes. \
[x] 2026-07-01T23:41:59Z Implement and verify CLI and live bridge CMUX `surface.move` and `move-surface` parity for moving surfaces between panes. \
[x] 2026-07-01T23:38:20Z Implement and verify live bridge CMUX `surface.close` and `close-surface` parity for GTK surfaces. \
[x] 2026-07-01T23:33:49Z Implement and verify live bridge and CLI CMUX browser visible, enabled, and checked state checks. \
[x] 2026-07-02T03:53:00Z Implement and verify CMUX browser persisted element-ref parity for snapshot/find refs used by later actions and getters. \
[x] 2026-07-02T03:37:00Z Implement and verify retained CMUX browser console/error diagnostic buffers for live WebKit browser surfaces. \
[x] 2026-07-02T03:21:00Z Implement and verify live bridge CMUX browser dialog accept/dismiss CLI/API parity for WebKit JavaScript dialogs. \
[x] 2026-07-02T02:49:37Z Implement and verify CMUX surface I/O event emission for redacted text sends and key sends. \
[x] 2026-07-02T02:40:37Z Implement and verify CMUX workspace lifecycle event emission for create, select, close, rename, and reorder live-host mutations. \
[x] 2026-07-02T02:30:56Z Implement and verify CMUX notification hook policy effects for record, unread, desktop, and sound delivery decisions. \
[x] 2026-07-01T23:30:01Z Implement and verify live bridge CMUX browser state save and load parity for WebKit browser surfaces. \
[x] 2026-07-01T23:25:27Z Implement and verify live bridge CMUX browser tab list, new, switch, and close parity for WebKit browser surfaces. \
[x] 2026-07-01T23:14:21Z Implement and verify live bridge CMUX browser screenshot capture for WebKit browser surfaces. \
[x] 2026-07-01T22:59:57Z Implement and verify live bridge CMUX browser locator routing for WebKit browser surfaces. \
[x] 2026-07-01T22:54:12Z Implement and verify live bridge CMUX browser highlight, cookies, and Web Storage routing for WebKit browser surfaces. \
[x] 2026-07-01T22:47:41Z Implement and verify live bridge CMUX browser injection and console/error diagnostic routing for WebKit browser surfaces. \
[x] 2026-07-01T22:42:33Z Implement and verify explicit `not_supported` handling for CMUX-documented unsupported browser APIs in CLI and live bridge paths. \
[x] 2026-07-01T22:37:38Z Implement and verify Codex CMUX Feed hook setup wiring for installing and uninstalling `hooks feed --source codex` entries. \
[x] 2026-07-01T19:43:29Z Implement and verify the CMUX `hooks feed --source` CLI forwarding and decision-rendering parity slice for Feed permission, plan, and question events. \
[x] 2026-07-01T19:32:46Z Implement and verify the first CMUX Feed backend parity slice: `feed.push`, `feed.list`, `feed.permission.reply`, `feed.question.reply`, and `feed.exit_plan.reply`. \
[x] 2026-07-01T19:25:22Z Implement and verify the CMUX browser DOM action and selector-aware getter parity slice for live WebKit browser surfaces. \
[x] 2026-07-01T19:15:55Z Implement and verify the CMUX browser `snapshot` and bounded-polling `wait` parity slice for live WebKit browser surfaces. \
[x] 2026-07-01T19:06:02Z Implement and verify CMUX workspace-group mutation CLI/API parity for create, ungroup, delete, rename, collapse, expand, pin, unpin, add, remove, set-anchor, new-workspace, set-color, set-icon, move, and focus. \
[x] 2026-07-01T18:57:54Z Implement and verify the CMUX workspace-group read/persistence parity slice: persisted group metadata, workspace `group_id`, live `workspace.group.list`, `list-workspace-groups`, and `workspace-group list`. \
[x] 2026-07-01T18:13:19Z Convert the current CMUX and Limux inventories into a durable CMUX parity matrix with verification evidence and exact Limux files/tests for implemented or partial features. \
[x] 2026-07-01T18:13:19Z Build a Limux feature inventory from the current fork and map every CMUX feature to implemented, partial, missing, intentionally incompatible, or blocked. \
[x] 2026-07-01T18:13:19Z Build a complete current-CMUX feature inventory from commit 2313855c4988ea20e065a9a9e87413f014777f46, including CLI commands, server APIs, UI flows, workspace/session behavior, browser automation, notifications, agents, memory diagnostics, tmux/remote support, configuration, and persistence. \
[x] 2026-07-01T15:50:37Z Move completed TODO items to TODO_COMPLETED.md, commit verified changes, and push only to the fork remote. \
[x] 2026-07-01T15:50:37Z Verify with active-output benchmarks, targeted functionality checks, repository checks, and no pushes to am-will. \
[x] 2026-07-01T15:50:37Z Implement or patch missing CMUX parity functionality selected from the audit. \
[x] 2026-07-01T15:50:37Z Implement the highest-confidence additional performance or leak fixes found in the audit. \
[x] 2026-07-01T15:50:37Z Identify missing high-value CMUX functionality or behavioral gaps that can be migrated without breaking existing Limux workflows. \
[x] 2026-07-01T15:50:37Z Pull or inspect the current CMUX codebase and build a concrete CMUX-to-Limux functionality parity matrix. \
[x] 2026-07-01T15:50:37Z Re-audit Limux runtime code paths for additional active-usage CPU, memory, GPU, disk-write, latency, and leak bottlenecks. \
[x] 2026-07-01T13:58:15Z Move completed active-output TODO entries to TODO_COMPLETED.md, commit verified changes, and push only to the fork remote. \
[x] 2026-07-01T13:58:15Z Re-run active-output profiling and repository checks to verify runtime efficiency, stability, and no continuous disk writes. \
[x] 2026-07-01T13:58:15Z Patch the verified active-runtime bottleneck while preserving terminal functionality. \
[x] 2026-07-01T13:58:15Z Capture active-output baseline metrics for the triple workload. \
[x] 2026-07-01T13:58:15Z Extend the mixed workload profiler to measure active-output CPU, RSS, disk writes, GPU state, and stability separately from creation and idle metrics. \
[x] 2026-07-01T13:58:15Z Define an active-output workload with many open terminals repeatedly printing text while Limux stays running. \
[x] 2026-07-01T13:34:55Z Move completed triple-workload TODO entries to TODO_COMPLETED.md, commit the verified changes, and push only to the fork remote. \
[x] 2026-07-01T13:34:55Z Run repository verification after any code/script/docs changes. \
[x] 2026-07-01T13:34:55Z Re-run the optimized triple workload and verify whether the 80% creation-latency target is achieved or blocked by a measured constraint. \
[x] 2026-07-01T13:34:55Z Patch the dominant triple-workload bottleneck while preserving existing Limux behavior and hard-failing unsafe split batching. \
[x] 2026-07-01T13:34:55Z Measure the triple sequential baseline for latency, CPU, RSS, disk writes, GPU state, pane count, surface count, and stability. \
[x] 2026-07-01T13:34:55Z Define the triple mixed workload as 12 workspaces, 48 split panes, and 120 terminal surfaces. \
[x] 2026-07-01T12:59:37Z Move completed mixed-workload TODO entries to TODO_COMPLETED.md, commit the verified changes, and push only to the fork remote. \
[x] 2026-07-01T12:59:37Z Re-run repository checks and the mixed-workload profiler to verify stability and resource behavior. \
[x] 2026-07-01T12:59:37Z Patch the verified mixed-workload bottleneck without removing existing Limux functionality. \
[x] 2026-07-01T12:59:37Z Measure the existing sequential multi-workspace split-pane path and identify the dominant bottleneck. \
[x] 2026-07-01T12:59:37Z Add a repeatable mixed-workload profiler that measures creation latency, host CPU, RSS, disk writes, GPU state, pane count, surface count, and workspace count. \
[x] 2026-07-01T12:59:37Z Define and document a mixed Limux workload with multiple workspaces, multiple split panes per workspace, and multiple terminal surfaces per workspace. \
[x] 2026-07-01T12:19:05Z Run repository verification and move completed 40-terminal efficiency TODO entries to TODO_COMPLETED.md. \
[x] 2026-07-01T12:19:05Z Migrate high-value cmux surface/tab functionality into Limux with live surface.create and surface.create_many. \
[x] 2026-07-01T12:19:05Z Re-run the 40-terminal benchmark and prove the 85% target with measured batch creation results. \
[x] 2026-07-01T12:19:05Z Patch verified 40-terminal inefficiencies while preserving existing Limux functionality. \
[x] 2026-07-01T12:19:05Z Identify eager activation and repeated CLI/socket calls as dominant 40-terminal bottlenecks. \
[x] 2026-07-01T12:19:05Z Capture baseline 40-terminal latency, host CPU, RSS, GPU, and disk-write behavior. \
[x] 2026-07-01T12:19:05Z Build a repeatable 40-terminal Limux stress benchmark with isolated runtime state. \
[x] 2026-07-01T12:19:05Z Audit current cmux functionality and map it against Limux features. \
[x] 2026-07-01T12:19:05Z Create an 85%-efficiency 40-terminal stress ExecPlan with benchmark definition and acceptance criteria. \
[x] 2026-07-01T11:18:21Z Move completed performance-goal items to TODO_COMPLETED.md and leave TODO.md clean. \
[x] 2026-07-01T11:18:21Z Re-run profiling and tests to prove measurable improvements and document the Xvfb privilege blocker. \
[x] 2026-07-01T11:18:21Z Patch the verified Ghostty mailbox polling inefficiency while preserving functionality and hard-cutout behavior. \
[x] 2026-07-01T11:18:21Z Identify Ghostty's unconditional 8 ms mailbox timer as the measured runtime hotspot. \
[x] 2026-07-01T11:18:21Z Compare patched Limux measurements against a reproducible pre-performance git baseline. \
[x] 2026-07-01T11:18:21Z Run live-display runtime profiling for disk writes, memory growth, CPU, and NVIDIA GPU behavior. \
[x] 2026-07-01T11:18:21Z Establish the installed/current-version baseline and build profileable optimized Limux binaries. \
[x] 2026-07-01T11:02:53Z Create a reproducible performance and leak profiling plan for Limux. \
[x] 2026-07-01T10:54:59Z Move completed work items to TODO_COMPLETED.md and leave TODO.md clean. \
[x] 2026-07-01T10:54:59Z Verify the patched behavior with the strongest local checks available. \
[x] 2026-07-01T10:54:59Z Compare Limux with similar terminal multiplexers and document practical missing functions. \
[x] 2026-07-01T10:54:59Z Evaluate whether a language conversion is useful now and keep existing functionality intact. \
[x] 2026-07-01T10:54:59Z Patch validated safety issues without adding silent fallbacks. \
[x] 2026-07-01T10:54:59Z Audit high-risk leak, socket, filesystem, process, and terminal-control paths that could expose data or damage user files. \
[x] 2026-07-01T10:46:00Z Baseline the project build/test state after updating from upstream. \
[x] 2026-07-01T10:45:49Z Fetch and integrate the latest upstream limux changes without discarding local fork work. \
[x] 2026-04-30T08:36:47Z Verify Limux loads and renders the recovered workspace list through the control socket. \
[x] 2026-04-30T08:36:47Z Apply the session recovery and GTK CSS parser fixes without adding silent runtime fallbacks. \
[x] 2026-04-30T08:36:47Z Identify that an empty canonical session.json was masking the non-empty legacy workspaces.json file. \
[x] 2026-04-30T08:36:47Z Reproduce the Limux startup and empty workspace/session state on this machine. \
[x] 2026-04-15T20:42:40Z Move completed tasks into TODO_COMPLETED.md and push the git change to the user's fork. \
[x] 2026-04-15T20:41:49Z Verify that `limux` now resolves to the updated installed version rather than the old `0.1.12` package. \
[x] 2026-04-15T20:41:49Z Upgrade the installed Debian Limux package on this Ubuntu 24.04 machine to the freshly built `0.1.13` artifact from this repo. \
[x] 2026-04-15T20:41:49Z Ignore `.scplus/` in the repo and prepare the git change for commit. \
[x] 2026-04-15T20:36:22Z Move completed TODO items into TODO_COMPLETED.md, leave only unfinished work in TODO.md, and commit and push the verified changes to the user's fork. \
[x] 2026-04-15T20:34:21Z Verify the requested outcome directly on this Ubuntu 24.04 environment by running the new bootstrap script and the repository quality/build checks that exercise the exact flow. \
[x] 2026-04-15T20:34:21Z Update repository build/package/docs wiring so the scripted flow succeeds on Ubuntu 24.04 without hidden manual steps. \
[x] 2026-04-15T20:34:21Z Add a single Ubuntu 24.04 bootstrap/build shell script that installs or provisions the required toolchain and runs the full build/package flow from a fresh checkout. \
[x] 2026-04-15T20:34:21Z Reproduce the current Ubuntu 24.04 build and packaging path, document concrete blockers, and identify the minimum repo changes required for compatibility. \
[x] 2026-07-02T02:56:15Z Implement and verify CMUX `events.stream` slow-consumer cutoff at 1,024 pending live events with an explicit `slow_consumer` error frame. \
[x] 2026-07-02T03:02:31Z Implement and verify retained CMUX surface lifecycle events for successful create, focus, close, move, reorder, and drag-to-split mutations. \
[x] 2026-07-02T03:11:16Z Implement and verify live CMUX `config reload`/`reload-config` parity with settings and shortcut reload, terminal appearance/font application, and retained `config.reloaded` event emission. \
[x] 2026-07-02T03:24:41Z Implement and verify retained CMUX browser navigation, interaction, and redacted input event emission for successful live browser actions. \
[x] 2026-07-02T03:38:12Z Implement and verify CMUX `read-screen`/`capture-pane` `--lines` and `--scrollback` request plumbing, last-N visible text limiting, and explicit scrollback capability metadata. \
[x] 2026-07-02T03:48:03Z Implement and verify CMUX `workspace reconnect`/`workspace disconnect` CLI/API routing with explicit `not_supported` errors until remote daemon support exists. \
[x] 2026-07-02T06:58:31Z Implement and verify CMUX-compatible Pi hook parity: first-class `pi` agent kind, native `pi --session <id>` resume command, protected Pi extension install/uninstall, lifecycle hook bridge, Feed tool telemetry bridge, launch argv capture, docs update, focused tests, and CLI clippy. \
[x] 2026-07-02T07:03:41Z Implement and verify CMUX-compatible Amp hook parity: first-class `amp` agent kind, native `amp threads continue <id>` resume command, protected Amp plugin install/uninstall, lifecycle hook bridge, live status/log bridge, launch argv capture without `AMP_API_KEY`, docs update, focused tests, isolated install smoke, and CLI clippy. \
[x] 2026-07-02T07:11:27Z Implement and verify CMUX-compatible Hermes Agent hook parity: first-class `hermes-agent` agent kind, native `hermes --resume <id>` resume command, protected YAML hook install/uninstall, shell hook allowlist install/uninstall, lifecycle and Feed hook wiring, Hermes Feed nonblocking tool telemetry classification, isolated install smoke, focused tests, and CLI clippy. \
[x] 2026-07-02T07:29:24Z Implement and verify CMUX-compatible workspace create focus parity: `new-workspace --focus <true|false>` and `workspace create --focus <true|false>` serialize CMUX boolean aliases, invalid CLI values fail loudly, live `workspace.create` accepts boolean `focus`, default background creation stays false, and focused creation activates only when explicitly requested. \
[x] 2026-07-02T07:37:59Z Implement and verify CMUX-compatible workspace create layout parity: `new-workspace --layout <json>` and `workspace create --layout <json>` require JSON objects, serialize CMUX layout params, translate CMUX split/pane/surface layout topology into Limux layouts, launch terminal surface commands from layout entries, seed browser URLs, and skip the top-level `--command` when a layout is supplied. \
[x] 2026-07-02T08:06:42Z Implement and verify CMUX-compatible workspace create description and group-placement parity: `new-workspace --description`, `--group`, `--group-placement top|end|afterCurrent`, and `--group-reference` serialize into `workspace.create`, live creation validates group/reference targets before mutation, descriptions persist and appear in workspace payloads/events, and grouped workspaces are placed at the requested top/end/after-reference position. \
[x] 2026-07-02T08:21:38Z Implement and verify CMUX-compatible `workspace.group.new_workspace` placement parity: the live bridge validates `placement`, rejects invalid values before dispatch, and the host applies top/end/afterCurrent insertion order using the group anchor as the CLI/header reference. \
[x] 2026-07-02T08:43:12Z Implement and verify CMUX-compatible workspace group placement config parity: `workspaceGroups.newWorkspacePlacement` and longest-match `workspaceGroups.byCwd[*].newWorkspacePlacement` load from settings JSON, invalid placement values fail loudly, settings saves preserve supported byCwd fields, and `workspace.group.new_workspace` uses the configured default when no explicit placement is supplied. \
[x] 2026-07-02T08:49:46Z Implement and verify CMUX-compatible Cmd-N folder workspace group placement parity: folder-dialog workspace creation detects the active workspace group, resolves configured placement from the group anchor cwd, uses the active workspace as the `afterCurrent` reference, and keeps ungrouped active workspaces unchanged. \
[x] 2026-07-02T08:57:37Z Implement and verify CMUX-compatible `app.newWorkspacePlacement` parity: settings JSON loads and saves app-level top/end/afterCurrent placement, invalid values fail loudly, unrelated app keys are preserved, and normal Cmd-N/sidebar-button folder workspace creation honors CMUX pinned-prefix insertion rules. \
[x] 2026-07-02T17:35:21Z Implement and verify CMUX-compatible PR/review sidebar report parity: `surface.report_pr`, `surface.report_review`, `surface.clear_pr`, `surface.report_pr_action`, and CLI aliases validate strict PR metadata, store per-surface in-memory rows, expose `pull_requests` in `sidebar-state`, render compact non-JSON PR output, and avoid background polling or disk writes. \
[x] 2026-07-02T18:05:49Z Implement and verify CMUX-compatible clickable GTK sidebar PR and port row parity: reported PRs and ports now render as workspace sidebar actions, respect `sidebar.makePullRequestsClickable`, `sidebar.openPullRequestLinksInCmuxBrowser`, and `sidebar.openPortLinksInCmuxBrowser`, open in a Limux browser split when configured, otherwise use the system URI handler, and refresh from in-memory metadata without polling or disk writes. \
[x] 2026-07-02T18:32:00Z Implement and verify CMUX-compatible workspace group sidebar header rendering: grouped sidebar order emits group header rows, anchor workspaces are represented by the header, collapsed groups skip child rows through the shared render plan, restored sessions rebuild headers, and selected group headers focus their anchor workspace. Verified by `cargo test -p limux-host-linux workspace_sidebar_render_items -- --nocapture` and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T18:46:00Z Implement and verify CMUX-compatible workspace group header controls: GTK group header chevrons dispatch collapse/expand actions, header plus buttons dispatch `workspace.group.new_workspace` through the existing host action path using configured placement resolution, and action failures are reported loudly. Verified by `cargo test -p limux-host-linux workspace_group_header -- --nocapture` and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T19:18:00Z Implement and verify CMUX-compatible grouped workspace sidebar drag-order semantics: visible GTK group headers now drag by anchor workspace id, header drops route through the existing workspace reorder path, top-level grouped row planning keeps group runs contiguous and anchor-first, and grouped child drops into top-level space explicitly promote the child out of the group. Verified by `cargo test -p limux-host-linux workspace_reorder_uses_top_level_rows -- --nocapture`, `cargo test -p limux-host-linux workspace_top_level_ids -- --nocapture`, `cargo test -p limux-host-linux workspace_group -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T19:32:00Z Implement and verify CMUX-compatible workspace group delete confirmation UX: right-clicking a GTK group header exposes group actions, destructive UI deletion shows the CMUX-shaped member-count confirmation dialog, confirmed deletion dispatches the existing `workspace.group.delete` action, cancellation leaves the group untouched, and socket/CLI deletion remains unconditional. Verified by `cargo test -p limux-host-linux workspace_group_delete_confirmation -- --nocapture`, `cargo test -p limux-host-linux workspace_group -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T20:05:00Z Implement and verify CMUX-compatible `detach-tab` alias parity: the CLI now exposes no-socket help for `detach-tab`, builds the same strict `tab.action move-to-new-workspace` request as `move-tab-to-new-workspace`, rejects caller-provided `--action`, preserves surface/workspace/window/title/focus parameters, and live tab-action alias tests cover the canonical and CMUX spelling variants. Verified by `cargo test -p limux-cli detach_tab_alias -- --nocapture` and `cargo test -p limux-host-linux tab_action_aliases_route_to_live_command -- --nocapture`. \
[x] 2026-07-02T20:35:00Z Implement and verify CMUX-compatible `remote`/`remotes` registry command routing and local registry storage: `list|ls`, `add <name> --route ... [--tag ...]`, and `remove|rm|delete <target>` now parse to `remotes.list`, `remotes.add`, and `remotes.remove` socket requests with host:port/bracketed-IPv6 route parsing, port-range checks, loopback rejection, command-position `--json`, sanitized table rendering, live bridge routing, and strict persisted non-secret local registry rows under the Limux config directory. Verified by `cargo test -p limux-cli cmux_remotes -- --nocapture`, `cargo test -p limux-cli cmux_remote_ssh -- --nocapture`, `cargo test -p limux-host-linux remote_registry -- --nocapture`, `cargo test -p limux-host-linux remote_workspace_methods_fail_before_dispatch -- --nocapture`, and `cargo test -p limux-host-linux capabilities_include_remote_workspace_methods -- --nocapture`. \
[x] 2026-07-02T21:15:00Z Implement and verify CMUX-compatible JSON `sidebar.custom.open` pane surfaces: the live bridge now validates named JSON sidebars, opens or focuses `customSidebar` pane tabs with CMUX workspace/surface/tab refs, prefers a right split for focused opens, persists/restores the custom-sidebar tab kind by name, and keeps Swift interpreted sidebars on the explicit unsupported path. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar_open_route_accepts_focus_and_targets -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar_tab_round_trips_through_session_json -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T21:45:00Z Implement and verify CMUX-compatible JSON custom-sidebar dispatcher close actions: `workspace.close(workspace_id)` and `surface.close(surface_id)` now parse strict required ids, route through existing live host close helpers, emit close lifecycle events, persist state, and expose CMUX dispatcher tooltips. Verified by `cargo test -p limux-host-linux custom_sidebar_dispatcher_actions_accept_documented_cmux_params -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T21:50:00Z Implement and verify CMUX-compatible JSON custom-sidebar noop actions: `noop` now accepts ignored payload fields such as `value`, shows a supported noop tooltip, and executes without mutating host state or requiring dispatcher state, matching upstream Swift sidebar no-op usage. Verified by `cargo test -p limux-host-linux custom_sidebar_dispatcher_actions_accept_documented_cmux_params -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T21:55:00Z Implement and verify CMUX-compatible JSON custom-sidebar workspace branch copy actions: `workspace.copyBranch(workspace_id)` now resolves CMUX workspace handles, reads the workspace folder/cwd branch through the existing no-spawn `.git/HEAD` parser, writes GTK standard and primary clipboards, and fails loudly when no branch or GTK display exists. Verified by `cargo test -p limux-host-linux custom_sidebar_dispatcher_actions_accept_documented_cmux_params -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T22:00:00Z Implement and verify CMUX-compatible JSON custom-sidebar workspace sort actions: `workspace.sort(param: "unread"|"recent")` now validates strict modes, reorders live workspaces by pinned tier plus unread or latest retained notification activity, preserves active workspace selection, keeps group runs normalized, emits reorder events when changed, and persists changed order. Verified by `cargo test -p limux-host-linux custom_sidebar_dispatcher_actions_accept_documented_cmux_params -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T21:26:58Z Implement and verify CMUX-compatible JSON custom-sidebar `pr.open(url:)` parity used by upstream PR review queue sidebars: actions now resolve PR URLs through the existing URL action parser, expose the resolved URL in tooltips, and launch through the same GTK URI handler as `openURL`, `open.url`, and `link.open` while preserving loud missing-URL feedback. Verified by `cargo test -p limux-host-linux custom_sidebar_dispatcher_actions_accept_documented_cmux_params -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T21:33:48Z Implement and verify CMUX-compatible `workspace-action` metadata parity: CLI parsing now accepts `--action` or positional actions plus workspace/window/title/description/color flags, the live bridge advertises and routes `workspace.action`, and GTK host mutations support `pin`, `unpin`, `rename`, `clear-name`, `set-description`, and `clear-description` with CMUX-shaped payloads, lifecycle events, session persistence, and loud rejection for unsupported sub-actions. Verified by `cargo test -p limux-cli cmux_workspace_action -- --nocapture`, `cargo test -p limux-host-linux workspace_action -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T21:46:45Z Complete and verify remaining CMUX `workspace.action` parity for movement, close scopes, read state, and color: live GTK actions now support `move-up`, `move-down`, `move-top`, `close-others`, `close-above`, `close-below`, `mark-read`, `mark-unread`, `set-color`, and `clear-color` with CMUX-shaped payloads, existing reorder/close/read event paths, persisted workspace `customColor`, and visible sidebar title color. Verified by `cargo test -p limux-cli cmux_workspace_action -- --nocapture`, `cargo test -p limux-host-linux workspace_action -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar_tab_round_trips_through_session_json -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`. \
[x] 2026-07-02T21:52:24Z Implement and verify true CMUX `read-screen`/`capture-pane --scrollback` parity for the live GTK bridge: terminal reads now choose Ghostty viewport bounds for default reads and Ghostty screen bounds for scrollback reads, which includes available scrollback plus the active buffer, and response metadata reports `scrollback_included=true` only when the scrollback path is used. Verified by `cargo test -p limux-host-linux terminal_read_selection -- --nocapture`. \
[x] 2026-07-02T21:58:16Z Implement and verify CMUX/tmux short placeholder alias parity: `bind` now canonicalizes to `bind-key`, `unbind` canonicalizes to `unbind-key`, both no-socket help probes are available, and all unsupported placeholder spellings route to the same explicit `not supported` path instead of unknown-command handling. Verified by `cargo test -p limux-cli tmux_ -- --nocapture`. \
[x] 2026-07-02T22:02:55Z Implement and verify CMUX `config path|paths --json` parity: local config commands now honor the global JSON flag, emit CMUX-shaped path metadata for `config path --json` and `config paths --json`, and fail loudly on path-resolution errors instead of returning text when JSON was requested. Verified by `cargo test -p limux-cli config_path_outputs_cmux_json_shape -- --nocapture`, `cargo build -p limux-cli --bin limux-cli`, `XDG_CONFIG_HOME=$(mktemp -d) ./target/debug/limux-cli --json config paths`, and `XDG_CONFIG_HOME=$(mktemp -d) ./target/debug/limux-cli config paths --json`. \
[x] 2026-07-02T22:10:27Z Implement and verify CMUX `settings` dispatch parity: no-argument `settings`, `settings open [target]`, and direct `settings <target>` aliases now route to the live `settings.open` RPC, while only `settings path|paths|docs|--help` remain local no-socket probes; `settings paths --json` now emits CMUX-shaped path metadata. Verified by `cargo test -p limux-cli settings_open -- --nocapture`, `cargo build -p limux-cli --bin limux-cli`, and `XDG_CONFIG_HOME=$(mktemp -d) ./target/debug/limux-cli settings paths --json`. \
[x] 2026-07-02T22:18:15Z Implement and verify CMUX docs JSON parity: `docs --json`, `docs list|all --json`, `docs <topic> --json`, and `settings docs --json` now render CMUX-shaped local docs metadata without requiring a running Limux host. Verified by `cargo test -p limux-cli cmux_docs -- --nocapture`, `cargo test -p limux-cli local_docs_command -- --nocapture`, and `cargo test -p limux-cli settings_open -- --nocapture`. \
[x] 2026-07-02T22:24:32Z Implement and verify CMUX config/settings local arity and docs JSON parity: `config help` now behaves like CMUX local help, `config docs --json` emits the same settings docs payload as `docs settings --json`, and extra arguments for `config path/docs` and `settings path/docs` now fail loudly with CMUX-style usage instead of being silently ignored. Verified by `cargo test -p limux-cli config_path_outputs_cmux_json_shape -- --nocapture` and `cargo test -p limux-cli settings_open -- --nocapture`. \
[x] 2026-07-02T22:34:13Z Implement and verify CMUX default hook setup agent parity: bare `limux hooks setup` now considers every implemented CMUX agent integration instead of only Codex/Claude/Gemini, while matching CMUX's safe default skip behavior by requiring available binaries and required config roots before writing; explicit per-agent hook installs remain supported. Verified by `cargo test -p limux-cli default_hook_setup_covers_implemented_cmux_agents -- --nocapture`, `cargo test -p limux-cli hook_install -- --nocapture`, `cargo test -p limux-cli pi -- --nocapture`, `cargo test -p limux-cli amp -- --nocapture`, `cargo test -p limux-cli hermes -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and isolated `limux-cli --json hooks setup` smokes for no available agents and fake available Codex. \
[x] 2026-07-02T22:41:54Z Implement and verify CMUX Browser catalog config parity: `limux config get/set` now supports upstream `browser.defaultSearchEngine`, custom search name/template, search suggestions, browser theme, hidden WebView discard controls, download prompt, terminal-link routing flags, embedded/external URL allowlists, insecure embedded HTTP host allowlist, React grab version, browser disabled override, and import-hint flags with CMUX defaults/enums and strict invalid-value rejection. Verified by `cargo test -p limux-cli config_browser_scalar_settings -- --nocapture`, `cargo build -p limux-cli --bin limux-cli`, and isolated `limux-cli config get/set browser.defaultSearchEngine` plus `browser.theme` smokes. \
[x] 2026-07-02T22:49:23Z Implement and verify CMUX theme-list arity parity: `limux themes list extra` now fails loudly with the same positional-argument contract as CMUX instead of silently ignoring extra arguments. Verified by `cargo test -p limux-cli themes_list_rejects_extra_arguments_like_cmux -- --nocapture`. \
[x] 2026-07-02T22:57:09Z Implement and verify CMUX split/new-tab cwd inheritance parity from upstream `5d1d14ce3d`: new terminal tabs, command-created tabs, `pane.create`, and `surface.split` now prefer the source pane's active terminal cwd before falling back to pane/workspace cwd, while explicit launch `working_directory` remains authoritative. Verified by `cargo test -p limux-host-linux inherited_terminal_working_directory_prefers_active_terminal_cwd -- --nocapture`, `cargo test -p limux-host-linux pane_create_contract -- --nocapture`, and `cargo test -p limux-host-linux surface_split -- --nocapture`. \
[x] 2026-07-02T23:11:00Z Implement and verify CMUX workspace reorder CLI/API parity: `reorder-workspace` now routes to live `workspace.reorder` with `--workspace`/positional refs, `--index`, `--before`, `--after`, `--window`, and `--dry-run`; `reorder-workspaces` routes to live/core `workspace.reorder_many` with strict `--order` validation; live host batch reorder places requested workspaces first while preserving unlisted order and emits existing reorder events/persistence on mutation. Verified by `cargo test -p limux-cli cmux_reorder_workspace_commands_build_params -- --nocapture`, `cargo test -p limux-host-linux workspace_reorder_routes_parse_cmux_params -- --nocapture`, and `cargo test -p limux-core dispatcher_workspace_reorder_many_moves_leading_order -- --nocapture`. \
[x] 2026-07-02T23:21:46Z Implement and verify CMUX `move-workspace-to-window` parity for the current live Limux window: CLI parsing now maps `move-workspace-to-window --workspace <id|ref|index> --window <id|ref|index>` to `workspace.move_to_window`, rejects missing flags loudly, the live GTK bridge routes the CMUX method/command, and the host accepts `window:1` while rejecting unsupported windows until true multi-window GTK support exists. Verified by `cargo test -p limux-cli cmux_window_and_workspace_aliases_map_to_limux_methods -- --nocapture`, `cargo test -p limux-host-linux workspace_move_to_window_route_queues_command -- --nocapture`, and `cargo test -p limux-core dispatcher_handles_workspace_and_window_flow -- --nocapture`. \
[x] 2026-07-02T23:30:43Z Implement and verify CMUX `simulate-sidebar-drag` debug parity for the current live Limux window: CLI parsing maps `--window`, `--from`, `--to`, positive `--duration-ms`, and positive `--steps` to `debug.sidebar.simulate_drag`; the live GTK bridge advertises/routes the CMUX method and alias with strict validation; and the host returns a non-mutating `simulated=true`, `committed=false` payload with edge, timing, window, and workspace metadata. Verified by `cargo test -p limux-cli cmux_simulate_sidebar_drag_builds_debug_params -- --nocapture` and `cargo test -p limux-host-linux simulate_sidebar_drag_route_queues_command -- --nocapture`. \
[x] 2026-07-02T23:36:12Z Implement and verify CMUX `current-window` / `window.current` live parity and explicit single-host window create/close handling: CLI parsing still maps `new-window`/`current-window`/`close-window` to window RPCs but now rejects missing `close-window --window`; the live GTK bridge advertises/routes `window.current`, `window.create`, and `window.close`; `window.current` returns current `window:1` metadata; and create/close return explicit `not_supported` until true multi-window GTK support exists. Verified by `cargo test -p limux-cli cmux_window_and_workspace_aliases_map_to_limux_methods -- --nocapture` and `cargo test -p limux-host-linux window_current_create_and_close_routes_are_explicit -- --nocapture`. \
[x] 2026-07-03T21:50:31Z Implement and verify CMUX-compatible FileEditor and Canvas catalog settings parity: `limux config get/set` now supports upstream `fileEditor.wordWrap`, `canvas.paneGap`, and `canvas.snappingEnabled` with CMUX defaults, strict invalid-value rejection, nested JSON load/save, and preservation of unrelated config keys. Verified by `cargo test -p limux-cli config_file_editor_and_canvas -- --nocapture`, `cargo test -p limux-host-linux file_editor_and_canvas -- --nocapture`, `cargo test -p limux-host-linux save_to_path_writes_terminal_custom_sidebar_and_beta_settings -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, `cargo fmt --check`, `git diff --check`, and `./scripts/check.sh`. \
[x] 2026-07-03T22:05:30Z Implement and verify CMUX-compatible Account catalog settings parity: `limux config get/set` now supports upstream `account.piiDisplayMode`, `account.selectedTeamID`, and `account.welcomeShown` with CMUX defaults, strict enum/string/boolean validation, nested JSON load/save, and preservation of unrelated config keys. Verified by `cargo test -p limux-cli config_account_scalar -- --nocapture`, `cargo test -p limux-host-linux account_settings -- --nocapture`, `cargo test -p limux-host-linux save_to_path_writes_terminal_custom_sidebar_and_beta_settings -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, `cargo fmt --check`, `git diff --check`, and `./scripts/check.sh`. \
[x] 2026-07-03T22:28:16Z Implement and verify CMUX-compatible Integrations catalog settings parity: `limux config get/set` now supports upstream `integrations.claudeCode.hooksEnabled`, `integrations.claudeCode.customClaudePath`, `integrations.codex.hooksEnabled`, `integrations.amp.hooksEnabled`, `integrations.cursor.hooksEnabled`, `integrations.gemini.hooksEnabled`, `integrations.kiro.hooksEnabled`, `integrations.kiro.notificationLevel`, `integrations.ripgrep.customBinaryPath`, and `integrations.suppressSubagentNotifications` with CMUX defaults, strict enum/string/boolean validation, nested JSON load/save, subsection object validation, and preservation of unrelated config keys. Verified by `cargo test -p limux-cli config_integrations_scalar -- --nocapture`, `cargo test -p limux-host-linux integrations_settings -- --nocapture`, `cargo test -p limux-host-linux malformed_integration_subsections -- --nocapture`, `cargo test -p limux-host-linux save_to_path_writes_terminal_custom_sidebar_and_beta_settings -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, `cargo fmt --check`, `git diff --check`, and `./scripts/check.sh`. \
[x] 2026-07-04T00:47:47Z Implement and verify CMUX-compatible sidebar appearance and workspace color settings parity from current upstream CMUX `7f94c9352f5267c2c01319d1aaa73b1e2207890e`: local `config get/set` now supports `sidebarAppearance.matchTerminalBackground`, `sidebarAppearance.tintColor`, `sidebarAppearance.lightModeTintColor`, `sidebarAppearance.darkModeTintColor`, `sidebarAppearance.tintOpacity`, `sidebarAppearance.blurOpacity`, `sidebarAppearance.cornerRadius`, `sidebarAppearance.preset`, `sidebarAppearance.material`, `sidebarAppearance.blendMode`, `sidebarAppearance.state`, `workspaceColors.indicatorStyle`, `workspaceColors.selectionColor`, `workspaceColors.notificationBadgeColor`, `workspaceColors.colors`, `workspaceColors.paletteOverrides`, and `workspaceColors.customColors`; host settings load/save strictly validates and round-trips the same JSON sections; legacy workspace indicator aliases normalize to modern values; and color arrays/maps require explicit JSON with normalized `#RRGGBB` values. Verified by `cargo test -p limux-cli workspace_color -- --nocapture`, `cargo test -p limux-cli sidebar_appearance -- --nocapture`, `cargo test -p limux-host-linux workspace_colors -- --nocapture`, `cargo test -p limux-host-linux sidebar_appearance -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`. \
