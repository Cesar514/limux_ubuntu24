# CMUX Full Parity Matrix

Source CMUX commit: `9374b139e1e60c4f5f17cb3e64f11ab8a6bceb47` in `/tmp/cmux-audit`.

Status values:

- Implemented: Limux has a verified equivalent in this branch.
- Partial: Limux has a usable subset, but named CMUX behavior remains missing.
- Missing: no equivalent found in Limux.
- Blocked: implementation needs external infrastructure or a platform facility not available in this local verification pass.
- Not supported by CMUX: upstream CMUX documents the API as unsupported.

| Area | CMUX commands/APIs | Limux status | Evidence and remaining gap |
| --- | --- | --- | --- |
| Notification lifecycle | `notify`, `list-notifications`, `dismiss-notification`, `mark-notification-read`, `open-notification`, `jump-to-unread`, `clear-notifications`; `notification.create/list/dismiss/mark_read/open/jump_to_unread/clear` | Implemented | Implemented live host store, CLI/API aliases, retained event publication for `notification.created`, `notification.read`, `notification.removed`, and `notification.cleared`, CMUX-style `created_at`, surface/pane refs, `tab_title` metadata for surface-targeted notifications, configured notification hooks, and hook-controlled `record`, `markUnread`, `desktop`, and `sound` policy effects in `rust/limux-host-linux/src/app_config.rs`, `rust/limux-host-linux/src/control_bridge.rs`, `rust/limux-host-linux/src/window.rs`, `rust/limux-host-linux/src/event_bus.rs`, and `rust/limux-cli/src/main.rs`. Verified by `cargo test -p limux-host-linux notification -- --nocapture`, `cargo test -p limux-host-linux app_config -- --nocapture`, and `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`. |
| Browser open split | `browser open`, `browser open-split`, `browser new`; `browser.open_split` | Implemented | Live bridge accepts `pane.create type=browser` and `browser.open_split`, seeded through WebKit browser pane state and CLI browser open/new aliases. Verified by `cargo test -p limux-host-linux browser_open_split_route_queues_browser_pane -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, and `./scripts/check.sh`. |
| Browser navigation and automation | `goto/navigate`, `back/forward/reload`, `snapshot`, `eval`, `wait`, `click`, `fill`, `type`, `select`, `get`, `find`, `frame`, `dialog`, `download`, `cookies`, `storage`, `tab`, `console`, `errors`, `highlight`, `state` | Implemented | Live bridge now supports `browser.navigate`, `browser.url.get`, `browser.back`, `browser.forward`, `browser.reload`, DOM actions `browser.click/dblclick/fill/type/select/hover/focus/check/uncheck/press/keydown/keyup/scroll/scroll_into_view`, `browser.focus_webview`, `browser.is_webview_focused`, `browser.eval`, getters `browser.get.title/text/html/value/attr/count/box/styles`, DOM state checks `browser.is.visible/enabled/checked`, locator methods `browser.find.role/text/label/placeholder/alt/title/testid/first/last/nth`, persisted `@eN` element refs from snapshot/find through later actions and getters, `browser.frame.select/main` with persistent selected-frame evaluation context, `browser.dialog.accept/dismiss` for queued WebKit JavaScript dialogs, `browser.download.wait` for bounded local download path waits, real WebKit PNG capture through `browser.screenshot`, `browser.snapshot`, bounded-polling `browser.wait`, injection commands `browser.addscript/addinitscript/addstyle`, retained diagnostic buffers for `browser.console.list/clear` and `browser.errors.list/clear`, `browser.highlight`, `browser.cookies.get/set/clear`, `browser.storage.get/set/clear`, browser tab commands `browser.tab.list/new/switch/close`, state save/load commands `browser.state.save/load`, and retained CMUX browser events for successful navigation, interaction, and redacted input actions for addressed WebKit browser surfaces. CLI routes serialize the matching browser namespace and legacy focus/url aliases in `rust/limux-cli/src/main.rs`. Verified by `cargo test -p limux-host-linux browser_action_routes_validate_surface_and_url -- --nocapture`, `cargo test -p limux-host-linux browser_element_ref_scripts_store_and_resolve_refs -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, `cargo test -p limux-cli cmux_ -- --nocapture`, `cargo test -p limux-core dispatcher_browser_screenshot_writes_requested_png_path -- --nocapture`, `cargo test -p limux-host-linux browser_find -- --nocapture`, `cargo test -p limux-host-linux browser_state -- --nocapture`, `cargo test -p limux-host-linux browser_stateful -- --nocapture`, `cargo test -p limux-host-linux browser_diagnostics -- --nocapture`, `cargo test -p limux-host-linux browser_tab -- --nocapture`, `cargo test -p limux-host-linux event_bus -- --nocapture`, `cargo test -p limux-host-linux -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, `cargo test -p limux-control -- --nocapture`, and `./scripts/check.sh`. |
| Browser documented unsupported APIs | `browser.viewport.set`, `browser.geolocation.set`, `browser.offline.set`, `browser.trace.start/stop`, `browser.network.route/unroute/requests`, `browser.screencast.start/stop`, `browser.input_mouse/input_keyboard/input_touch` | Not supported by CMUX | Upstream documents these as WKWebView gaps in `/tmp/cmux-audit/skills/cmux-browser/references/commands.md`. Limux now returns explicit `not_supported` errors for the CMUX CLI forms and live bridge RPC methods instead of silently passing them through or misreporting them as unknown. Verified by `cargo test -p limux-cli cmux_browser_unsupported -- --nocapture` and `cargo test -p limux-host-linux browser_unsupported -- --nocapture`. |
| Workspace lifecycle | `workspace list/create/env/close/rename/select/reconnect/disconnect`, `list-workspaces`, `new-workspace`, `select-workspace`, `close-workspace`, `rename-workspace`, `current-workspace` | Partial | Core workspace list/create/select/rename/close exists. Added workspace environment storage, `new-workspace --env/--env-file`, `workspace.create workspace_env`, terminal inheritance for workspace env, `workspace env [--mask]` inspection with protected `CMUX_*`/`LIMUX_*` rejection, retained CMUX event emission for `workspace.created`, `workspace.selected`, `workspace.closed`, `workspace.renamed`, and `workspace.reordered` from live host model mutation paths, and explicit CMUX `workspace reconnect`/`disconnect` CLI-to-RPC mapping with live `workspace.remote.reconnect`/`workspace.remote.disconnect` `not_supported` errors until the remote daemon exists. Verified by `cargo test -p limux-cli workspace_env -- --nocapture`, `cargo test -p limux-cli workspace_remote_namespace_requests_map_to_cmux_methods -- --nocapture`, `cargo test -p limux-host-linux workspace_env -- --nocapture`, `cargo test -p limux-host-linux workspace_lifecycle -- --nocapture`, `cargo test -p limux-host-linux workspace_reordered -- --nocapture`, `cargo test -p limux-host-linux remote_workspace_methods_fail_before_dispatch -- --nocapture`, and `./scripts/check.sh`. Remaining: actual remote reconnect/disconnect behavior, moving/reordering across windows, project-config workspace env loading, and full CMUX argument compatibility. |
| Workspace groups | `workspace-group list/create/ungroup/delete/rename/collapse/expand/pin/unpin/add/remove/set-anchor/new-workspace/set-color/set-icon/move/focus`; `workspace.group.*` | Partial | Added CMUX-style persisted group model, per-workspace `group_id`, live `workspace.group.*` list and mutation APIs, `list-workspace-groups`, `workspace-group <subcommand>`, and `workspace group <subcommand>` CLI routes. Collapsed groups now resync the GTK sidebar and hide inactive non-anchor member workspaces while keeping the anchor and active workspace visible. Verified by focused workspace-group tests, `cargo test -p limux-host-linux workspace_group -- --nocapture`, `cargo test -p limux-host-linux collapsed_workspace_groups -- --nocapture`, `cargo clippy -p limux-host-linux --all-targets -- -D warnings`, and `./scripts/check.sh`. Remaining: true grouped sidebar/header rendering, drag-order semantics across grouped rows, delete confirmation UX, and exact new-workspace placement semantics beyond accepted params. |
| Pane and surface lifecycle | `new-split`, `list-panes`, `list-pane-surfaces`, `focus-pane`, `new-pane`, `new-surface`, `close-surface`, `move-surface`, `split-off`, `reorder-surface`, `drag-surface-to-split`, `refresh-surfaces`, `clear-history`, `surface-health` | Partial | Live bridge supports list/create/focus, terminal surface creation, `surface.split`/`new-split` to create a real split from a source surface, `surface.drag_to_split`/`split-off`/`drag-surface-to-split` to move a surface into a new split, `surface.close`/`close-surface` tab removal, `surface.move`/`move-surface` to another pane with optional index, `surface.reorder`/`reorder-surface` within a pane by index or before/after target, `surface.refresh`/`refresh-surfaces` terminal redraws, `surface.clear_history`/`clear-history` terminal scrollback clearing through Ghostty's live clear action, retained CMUX `surface.created`, `surface.focused`, `surface.closed`, `surface.moved`, and `surface.reordered` lifecycle events from successful live host mutations, and tmux pane resize/swap/join/break selected-surface movement, all with session-save scheduling where state mutates. Verified by `cargo test -p limux-host-linux surface_split -- --nocapture`, `cargo test -p limux-host-linux surface_drag_to_split -- --nocapture`, `cargo test -p limux-host-linux surface_close -- --nocapture`, `cargo test -p limux-host-linux surface_move -- --nocapture`, `cargo test -p limux-host-linux surface_reorder -- --nocapture`, `cargo test -p limux-host-linux surface_refresh -- --nocapture`, `cargo test -p limux-host-linux surface_clear_history -- --nocapture`, `cargo test -p limux-host-linux surface_lifecycle -- --nocapture`, `cargo test -p limux-host-linux pane_resize -- --nocapture`, `cargo test -p limux-host-linux pane_swap -- --nocapture`, `cargo test -p limux-host-linux pane_join -- --nocapture`, `cargo test -p limux-host-linux pane_break -- --nocapture`, `cargo test -p limux-cli cmux_surface_aliases -- --nocapture`, and `./scripts/check.sh`. Remaining: broader cross-window pane move semantics outside the current live host scope. |
| Terminal I/O | `read-screen`, `capture-pane`, `send`, `send-key`, `send-panel`, `send-key-panel` | Partial | Terminal read/send/key paths exist. Live socket text/key injection now emits retained CMUX `surface.input_sent` and `surface.key_sent` events with terminal text redacted from event payloads. `read-screen`/`capture-pane` now serialize CMUX `--lines` and `--scrollback` options, validate positive line counts, apply last-N line limiting to the terminal text Limux can currently read, and return explicit `scrollback_requested`, `scrollback_included`, and `source` metadata instead of silently claiming scrollback support. Verified by `cargo test -p limux-host-linux surface_io -- --nocapture`, `cargo test -p limux-host-linux surface_input -- --nocapture`, `cargo test -p limux-host-linux read_text_route_accepts_lines_and_scrollback_flags -- --nocapture`, `cargo test -p limux-host-linux limit_text_to_last_lines_preserves_line_endings -- --nocapture`, `cargo test -p limux-cli read_screen_params_include_lines_and_scrollback -- --nocapture`, and `cargo test -p limux-cli cmux_surface_aliases -- --nocapture`. Remaining: true beyond-viewport scrollback capture from Ghostty. |
| Events stream | `events.stream`; CLI `events --after --cursor-file --name --category --reconnect --limit --no-ack --no-heartbeat` | Implemented | Added a host-side retained event bus with 4,096-frame replay, ack resume metadata, category/name filters, heartbeat frames, live socket takeover, bounded event frames, live subscriber `slow_consumer` cutoff at 1,024 pending matching events, durable CMUX-compatible `~/.cmuxterm/events.jsonl` append, 16 MiB rotation to `events.jsonl.1`, Feed/agent/notification/workspace lifecycle/surface lifecycle/surface I/O/config reload/browser navigation/browser interaction/redacted browser input event publication, and CLI reconnect resume from the last processed `seq`. Verified by `cargo test -p limux-host-linux event_bus -- --nocapture`, `cargo test -p limux-host-linux live_stream_closes_slow_consumers_after_pending_queue_limit -- --nocapture`, `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`, `cargo test -p limux-host-linux workspace_lifecycle -- --nocapture`, `cargo test -p limux-host-linux workspace_reordered -- --nocapture`, `cargo test -p limux-host-linux surface_lifecycle -- --nocapture`, `cargo test -p limux-host-linux config_reload -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, `cargo test -p limux-host-linux surface_input -- --nocapture`, and `cargo test -p limux-cli events_ -- --nocapture`. |
| Feed and approvals | `feed.push`, `feed.permission.reply`, `feed.question.reply`, `feed.exit_plan.reply`; `feed tui`, `feed clear`, feed hooks | Partial | Added a live host Feed backend with bounded in-memory item ring, direct socket handling for `feed.push`, `feed.list`, `feed.clear`, `feed.permission.reply`, `feed.question.reply`, and `feed.exit_plan.reply`, CMUX-shaped resolved/acknowledged/timed_out result payloads, reply mode validation, blocking wait support beyond the normal 5s control-command timeout, `limux feed clear [--yes|-y]` local deletion of `~/.cmuxterm/workstream.jsonl` and `workstream.jsonl.1`, `limux feed tui [--opentui|--legacy]` routing to the live Feed right-sidebar UI with explicit `limux-right-sidebar` implementation reporting, direct right-sidebar permission controls for pending Codex/CMUX Feed requests (`once`, `always`, `bypass`, `deny`), direct right-sidebar exit-plan controls (`manual`, `autoAccept`, `bypassPermissions`, `ultraplan`, `deny`), direct right-sidebar question choice controls from CMUX/Claude question option payloads, `limux hooks feed --source <agent>` CLI forwarding, CMUX-style Claude/Codex/generic hook event classification, blocking/nonblocking timeout selection, decision JSON rendering for permission, plan, and question replies, Codex hook setup wiring that installs CMUX Feed hook entries for `PreToolUse`, `PermissionRequest`, `PostToolUse`, `PreCompact`, `SubagentStart`, and `SubagentStop`, Claude hook setup wiring that installs blocking Feed entries for `PreToolUse` and `PermissionRequest` with Claude's matcher shape, Gemini hook setup wiring that installs blocking Feed `PreToolUse`, Cursor CLI flat JSON hook setup wiring that installs lifecycle entries plus Feed `beforeShellExecution`, Kiro CLI agent JSON setup wiring that installs lifecycle entries plus Feed `preToolUse` and `postToolUse`, Antigravity hook group setup wiring that installs lifecycle entries plus Feed `PreToolUse` and `PostToolUse`, Rovo Dev YAML setup wiring that installs lifecycle-only `on_complete`, `on_error`, and `on_tool_permission` hooks, Grok hook setup wiring that installs lifecycle entries plus blocking Feed `PreToolUse` with CMUX second-based timeout units, and Copilot, CodeBuddy, Factory, and Qoder nested JSON hook setup wiring that installs lifecycle entries plus blocking Feed `PreToolUse`, retained `feed.item.received`, `feed.item.completed`, `feed.item.resolved`, `feed.cleared`, and `agent.hook.<HookEventName>` events, bounded CMUX-compatible `~/.cmuxterm/workstream.jsonl` audit persistence with rotation to `workstream.jsonl.1`, and a right-sidebar Feed preview while preserving `feed --help` and `hooks feed --source ...`. Verified by `cargo test -p limux-cli cmux_feed -- --nocapture`, `cargo test -p limux-cli antigravity -- --nocapture`, `cargo test -p limux-cli rovo -- --nocapture`, `cargo test -p limux-cli cursor -- --nocapture`, `cargo test -p limux-cli kiro -- --nocapture`, `cargo test -p limux-cli grok -- --nocapture`, `cargo test -p limux-cli nested_json_agents -- --nocapture`, `cargo test -p limux-cli hook_install_writes_feed_hooks -- --nocapture`, `cargo test -p limux-host-linux right_sidebar_feed -- --nocapture`, `cargo test -p limux-host-linux feed_sidebar_ -- --nocapture`, `cargo test -p limux-host-linux feed -- --nocapture`, `cargo test -p limux-host-linux right_sidebar -- --nocapture`, `cargo test -p limux-host-linux event_bus -- --nocapture`, and `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`. Remaining: exact CMUX Bun/OpenTUI terminal UI parity, hook setup wiring for additional CMUX agents beyond Codex/Claude/Grok/Gemini/OpenCode/Cursor/Kiro/Antigravity/RovoDev/Copilot/CodeBuddy/Factory/Qoder, native decision notifications, and broader agent-specific edge cases such as Kiro exit-code denial. |
| Right sidebar controls | `right-sidebar toggle/show/hide/focus/set/mode`, mode aliases `files/find/vault/sessions/feed/dock`; `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, `list-log`, `sidebar-state`; `right_sidebar`, `sidebar.*` | Partial | Added CMUX-compatible CLI parsing for `right-sidebar`, including `--workspace`, `--tab`, `--window`, `--no-focus`, mode aliases, silent success output for mutating commands, and JSON state output for `mode`. The live GTK bridge now exposes `right_sidebar`, validates modes, queues host-owned visibility/mode/focus state, and reports `visible`, `mode`, `focused`, `workspace_id`, and supported modes. Added workspace-scoped sidebar metadata parity for `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, `list-log`, and `sidebar-state`, backed by live GTK host state, CMUX alias bridge methods, retained `sidebar.metadata.*`, `sidebar.progress.*`, and `sidebar.log.*` events, bounded log retention, and a rendered GTK right-sidebar shell. The shell now renders mode-specific visible panels: Files uses a bounded non-recursive workspace-folder preview, Find shows the search root plus open surfaces, Vault shows non-secret metadata counts, Sessions shows pane and surface summaries, Feed shows newest retained Feed items, and Dock shows selected/open surfaces. Verified by `cargo test -p limux-cli cmux_right_sidebar -- --nocapture`, `cargo test -p limux-cli cmux_sidebar -- --nocapture`, `cargo test -p limux-host-linux right_sidebar -- --nocapture`, `cargo test -p limux-host-linux sidebar_ -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, and `./scripts/check.sh`. Remaining: full interactive files/find/vault/sessions/feed/dock workflows and window-specific routing beyond the current single GTK host window. |
| Agent session diagnostics | `sessions list`, `sessions debug`, `session-debug` | Partial | Added no-socket CLI inspection of saved Claude/Codex/Grok/OpenCode/Cursor/Kiro/Antigravity/RovoDev/Gemini/Copilot/CodeBuddy/Factory/Qoder hook-session stores, with `--agent`, `--session`, `--workspace`, `--surface`, `--cwd`, `--state-dir`, `--codex-home`, `--limit`, `--all`, and `--json` support; CMUX-shaped text/JSON rows; Codex session-index/transcript diagnostics; and stale saved-PID reporting so forkable sessions with dead PIDs remain visible instead of disappearing. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, direct `cargo run -p limux-cli --bin limux-cli -- sessions list --agent codex --state-dir /tmp/limux-empty-sessions --json`, and direct `cargo run -p limux-cli --bin limux-cli -- session-debug --agent claude --state-dir /tmp/limux-empty-sessions`. Remaining: exact CMUX Claude workflow transcript resolution and full fork command diagnostics/trust metadata from latest macOS session-list implementation. |
| Remote and SSH | `ssh`, `remote list/add/remove`, `remotes list/add/remove`, `remote-daemon-status`, `ssh-session-*`; `workspace.remote.*`; `cmuxd-remote` daemon RPC | Partial | Limux exposes explicit `not_supported` behavior for `workspace.remote.reconnect`/`workspace.remote.disconnect` live bridge methods and now fails top-level Remote/SSH CLI forms locally before socket routing when they require CMUX remote daemon support or CMUX cloud/team device-registry infrastructure (`ssh`, `ssh-tmux`, `ssh-pty-attach`, `ssh-session-list`, `ssh-session-attach`, `ssh-session-cleanup`, `ssh-session-end`, `remote-daemon-status`, `remote`, `remotes`, `vm-ssh-attach`) while preserving no-socket help probes. Verified by `cargo test -p limux-cli cmux_remote_ssh -- --nocapture`, `cargo test -p limux-host-linux remote_workspace_methods_fail_before_dispatch -- --nocapture`, and `./scripts/check.sh`. Remaining: CMUX-compatible remote daemon, SSH workspace proxy, lease/auth file protocol, remote PTY/session RPC, and cloud/team device registry-backed remote list/add/remove management. |
| tmux compatibility | `capture-pane`, `resize-pane`, `pipe-pane`, `wait-for`, `swap-pane`, `break-pane`, `join-pane`, `next-window`, `previous-window`, `last-window`, `last-pane`, `find-window`, `clear-history`, `list-windows`, `list-panes`, buffers/hooks/respawn/display-message | Partial | Several compatibility wrappers exist in CLI. Live bridge now supports `workspace.next`/`next-window`, `workspace.previous`/`previous-window`, and `workspace.last`/`last-window` against the real GTK workspace stack with previous-workspace tracking, `pane.last`/`last-pane` using live control-bridge pane focus history, `surface.clear_history`/`clear-history` for addressed or focused terminal surfaces, `pane.resize`/`resize-pane` by mutating persisted GTK split ratios, `pane.swap`/`swap-pane` by exchanging selected surfaces between panes, `pane.join`/`join-pane` by moving an addressed or focused surface into a target pane, `pane.break`/`break-pane` by moving an addressed or focused surface into a newly-created workspace, and `surface.respawn`/`respawn-pane` by replacing the addressed terminal tab process while preserving the surface ID. CLI-local tmux buffers, hooks, aliases, and display-message handling now match more CMUX defaults for the documented commands: default buffer name, `-b` buffer names, explicit missing-buffer errors for paste, empty output for missing show-buffer, size-reporting `list-buffers`, `set-hook --list/--unset`, `capturep`, `display/displayp`, `resizep`, `respawnp`, `setb`, `pasteb`, `show-buffer/showb`, `lsw`, `lsp`, `list-windows -F/--format`, `list-panes -F/--format`, explicit placeholder rejection for `popup`, `bind-key`, `unbind-key`, and `copy-mode`, plus `display-message -p/-F/-t` parsing with known `#{...}` expansion, unknown-key stripping, trim, and empty-render fallback behavior backed by live workspace/pane/surface context. Verified by `cargo test -p limux-host-linux workspace_navigation -- --nocapture`, `cargo test -p limux-host-linux pane_last -- --nocapture`, `cargo test -p limux-host-linux surface_clear_history -- --nocapture`, `cargo test -p limux-host-linux pane_resize -- --nocapture`, `cargo test -p limux-host-linux pane_swap -- --nocapture`, `cargo test -p limux-host-linux pane_join -- --nocapture`, `cargo test -p limux-host-linux pane_break -- --nocapture`, `cargo test -p limux-host-linux surface_respawn -- --nocapture`, `cargo test -p limux-core dispatcher_handles_pane_and_surface_flow -- --nocapture`, `cargo test -p limux-cli respawn_pane_request -- --nocapture`, `cargo test -p limux-cli tmux_ -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, `cargo test -p limux-host-linux ratio -- --nocapture`, and `./scripts/check.sh`. Remaining: broader tmux aliases not listed in the public contract. |
| tmux documented unsupported placeholders | `popup`, `bind-key`, `unbind-key`, `copy-mode` | Not supported by CMUX | Upstream CLI contract marks these as placeholders/unsupported. Limux should report explicit unsupported behavior if exposed. |
| Config/settings/themes/docs | `settings`, `config doctor/check/validate/path/paths/docs/reload/get/set`, `themes list/set/clear`, `docs settings/shortcuts/api/browser/agents` | Partial | Implemented no-socket CLI parity for `docs`, `settings path/docs`, `shortcuts`, `config path/paths/check/validate/doctor/docs/documentation`, `config get/set sidebar-font-size`, `config get/set surface-tab-bar-font-size`, direct `config sidebar-font-size [points]`, direct `config surface-tab-bar-font-size [points]`, CMUX-style `themes list/set/clear` management of a marked Ghostty `theme = ...` config block with light/dark conditional values and JSON output, live socket `config reload`/`reload-config`/`reload_config`/`config.reload` support that reloads settings, shortcuts, and Ghostty config, applies appearance and terminal font-size changes, and emits retained `config.reloaded`, and live host `settings open [target]` routing through `settings.open` with CMUX target aliases, bridge-side target validation, `activate` parsing, target echoing, and nearest-page GTK dialog presentation. Font-size values use CMUX implementation defaults/ranges from `/tmp/cmux-audit/CLI/CMUXCLI+Config.swift` and preserve unrelated Limux settings JSON. Verified by `cargo test -p limux-cli theme -- --nocapture`, isolated `XDG_CONFIG_HOME` direct `limux-cli --json themes set/list` smoke, `cargo test -p limux-cli config_font_size -- --nocapture`, `cargo test -p limux-cli config_reload -- --nocapture`, `cargo test -p limux-cli config_ -- --nocapture`, `cargo test -p limux-cli settings_open -- --nocapture`, `cargo test -p limux-host-linux config_reload -- --nocapture`, `cargo test -p limux-host-linux settings_open -- --nocapture`, and `./scripts/check.sh`. Remaining: config precedence beyond XDG Limux settings/shortcuts, bundled/interactive Ghostty theme catalog listing and validation parity, exact settings-page UI for every CMUX target beyond Limux's current General/Keybindings pages, and upstream `config get/set` keys beyond the two font-size settings found in current CMUX. |
| CLI globals and environment | `--socket`, `--password`, `--json`, `--id-format`, `--window`; `CMUX_SOCKET_PATH`, `CMUX_SOCKET`, `CMUX_SOCKET_PASSWORD`, `CMUX_WORKSPACE_ID`, `CMUX_SURFACE_ID`, `CMUX_TAB_ID` | Partial | Limux now supports `CMUX_SOCKET_PATH`/`CMUX_SOCKET` checked routing, global `--window` parsing for window aliases, global `--password` parsing, CMUX password precedence of explicit `--password`, `CMUX_SOCKET_PASSWORD`, then `~/.local/state/limux/socket-control-password`, `LIMUX_SOCKET_MODE=password`/`CMUX_SOCKET_MODE=password` startup validation, same-user peer gating before the password handshake, JSON `auth.login`/`auth.status`, CMUX legacy `auth <password>` text responses, no-socket `help`/`version`, no-socket CMUX command help probes for socket-backed and legacy alias commands, command-position `--json`/`--id-format` presentation flags outside browser commands, and CMUX workspace/surface/tab env aliases in CLI context lookup and spawned terminal env. Verified by `cargo test -p limux-cli client_sends_auth_login_before_request_when_password_is_set -- --nocapture`, `cargo test -p limux-control socket_password_resolver_prefers_explicit_then_env_then_file -- --nocapture`, `cargo test -p limux-host-linux password_mode_rejects_request_until_auth_login_succeeds -- --nocapture`, `cargo test -p limux-host-linux legacy_cmux_auth_command_returns_text_status -- --nocapture`, `cargo test -p limux-cli cmux_command_help -- --nocapture`, `cargo test -p limux-cli cmux_non_help_probe -- --nocapture`, `cargo test -p limux-cli cmux_command_position -- --nocapture`, direct `cargo run -q -p limux-cli --bin limux-cli -- sessions list --state-dir /tmp/limux-no-sessions --json`, direct `cargo run -p limux-cli --bin limux-cli -- list-panes --help`, direct `cargo run -p limux-cli --bin limux-cli -- list-windows --help`, focused touched-crate `cargo test -p limux-control -p limux-cli -p limux-host-linux`, and `./scripts/check.sh`. Remaining: full multi-window routing. |

Primary upstream references:

- `/tmp/cmux-audit/docs/cli-contract.md`
- `/tmp/cmux-audit/docs/events.md`
- `/tmp/cmux-audit/docs/feed.md`
- `/tmp/cmux-audit/docs/notifications.md`
- `/tmp/cmux-audit/docs/workspace-groups.md`
- `/tmp/cmux-audit/docs/remote-daemon-spec.md`
- `/tmp/cmux-audit/daemon/remote/README.md`
- `/tmp/cmux-audit/skills/cmux-browser/references/commands.md`
