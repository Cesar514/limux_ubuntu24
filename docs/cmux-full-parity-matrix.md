# CMUX Full Parity Matrix

Source CMUX commit: `7f94c9352f5267c2c01319d1aaa73b1e2207890e` in `/tmp/cmux-audit` `origin/main`.

Status values:

- Implemented: Limux has a verified equivalent in this branch.
- Partial: Limux has a usable subset, but named CMUX behavior remains missing.
- Missing: no equivalent found in Limux.
- Blocked: implementation needs external infrastructure or a platform facility not available in this local verification pass.
- Not supported by CMUX: upstream CMUX documents the API as unsupported.

| Area | CMUX commands/APIs | Limux status | Evidence and remaining gap |
| --- | --- | --- | --- |
| Notification lifecycle | `notify`, `list-notifications`, `dismiss-notification`, `mark-notification-read`, `open-notification`, `jump-to-unread`, `clear-notifications`; `notification.create/list/dismiss/mark_read/open/jump_to_unread/clear` | Implemented | Implemented live host store, CLI/API aliases, retained event publication for `notification.created`, `notification.read`, `notification.removed`, and `notification.cleared`, CMUX-style `created_at`, surface/pane refs, `tab_title` metadata for surface-targeted notifications, configured notification hooks, hook-controlled `record`, `markUnread`, `desktop`, `sound`, `command`, and `paneFlash` policy effects, CMUX `notifications.command` shell command delivery with CMUX/Limux notification environment variables, CMUX `notifications.suppressOnlyFocusedSurface` desktop suppression behavior, CMUX `notifications.unreadPaneRing` gating for unread workspace visuals, CMUX agent notification category metadata (`turn-complete`, `needs-permission`, `idle-reminder`, `other`), pending background-work gating, and Claude hook classification for turn-complete/permission/idle notifications in `rust/limux-host-linux/src/app_config.rs`, `rust/limux-host-linux/src/control_bridge.rs`, `rust/limux-host-linux/src/window.rs`, `rust/limux-host-linux/src/event_bus.rs`, and `rust/limux-cli/src/main.rs`. Verified by `cargo test -p limux-host-linux notification -- --nocapture`, `cargo test -p limux-host-linux app_config -- --nocapture`, `cargo test -p limux-host-linux agent_notification -- --nocapture`, `cargo test -p limux-host-linux notification_preferences -- --nocapture`, `cargo test -p limux-host-linux desktop_notifications_ -- --nocapture`, `cargo test -p limux-host-linux notification_policy -- --nocapture`, `cargo test -p limux-host-linux notification_command_env_uses_cmux_and_limux_names -- --nocapture`, `cargo test -p limux-host-linux unread_visual_gate_respects_notification_setting -- --nocapture`, `cargo test -p limux-host-linux notification_list_and_clear_routes_validate_params -- --nocapture`, `cargo test -p limux-cli claude_notification_gate -- --nocapture`, and `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`. |
| Browser open split | `browser open`, `browser open-split`, `browser new`; `browser.open_split` | Implemented | Live bridge accepts `pane.create type=browser` and `browser.open_split`, seeded through WebKit browser pane state and CLI browser open/new aliases. Verified by `cargo test -p limux-host-linux browser_open_split_route_queues_browser_pane -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, and `./scripts/check.sh`. |
| Markdown viewer | `markdown open <path>`, `markdown <path>`; `markdown.open` | Implemented | CLI parses CMUX long and shorthand forms with `--workspace`, `--surface`, `--window`, `--direction`, `--focus`, and bounded `--font-size`, canonicalizes existing files, rejects bad paths/options loudly, and live `markdown.open` queues a real WebKit browser split against an encoded `file://` URL while returning canonical path, URL, markdown marker, pane, and surface metadata. Verified by `cargo test -p limux-cli cmux_markdown_open -- --nocapture` and `cargo test -p limux-host-linux markdown_open_route_queues_browser_pane -- --nocapture`. |
| Browser navigation and automation | `goto/navigate`, `back/forward/reload`, `snapshot`, `eval`, `wait`, `click`, `fill`, `type`, `select`, `get`, `find`, `frame`, `dialog`, `download`, `cookies`, `storage`, `tab`, `console`, `errors`, `highlight`, `state` | Implemented | Live bridge now supports `browser.navigate`, `browser.url.get`, `browser.back`, `browser.forward`, `browser.reload`, DOM actions `browser.click/dblclick/fill/type/select/hover/focus/check/uncheck/press/keydown/keyup/scroll/scroll_into_view`, `browser.focus_webview`, `browser.is_webview_focused`, `browser.eval`, getters `browser.get.title/text/html/value/attr/count/box/styles`, DOM state checks `browser.is.visible/enabled/checked`, locator methods `browser.find.role/text/label/placeholder/alt/title/testid/first/last/nth`, persisted `@eN` element refs from snapshot/find through later actions and getters, `browser.frame.select/main` with persistent selected-frame evaluation context, `browser.dialog.accept/dismiss` for queued WebKit JavaScript dialogs, `browser.download.wait` for bounded local download path waits, real WebKit PNG capture through `browser.screenshot`, `browser.snapshot`, bounded-polling `browser.wait`, injection commands `browser.addscript/addinitscript/addstyle`, retained diagnostic buffers for `browser.console.list/clear` and `browser.errors.list/clear`, `browser.highlight`, `browser.cookies.get/set/clear`, `browser.storage.get/set/clear`, browser tab commands `browser.tab.list/new/switch/close`, state save/load commands `browser.state.save/load`, and retained CMUX browser events for successful navigation, interaction, and redacted input actions for addressed WebKit browser surfaces. CLI routes serialize the matching browser namespace and legacy focus/url aliases in `rust/limux-cli/src/main.rs`. Verified by `cargo test -p limux-host-linux browser_action_routes_validate_surface_and_url -- --nocapture`, `cargo test -p limux-host-linux browser_element_ref_scripts_store_and_resolve_refs -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, `cargo test -p limux-cli cmux_ -- --nocapture`, `cargo test -p limux-core dispatcher_browser_screenshot_writes_requested_png_path -- --nocapture`, `cargo test -p limux-host-linux browser_find -- --nocapture`, `cargo test -p limux-host-linux browser_state -- --nocapture`, `cargo test -p limux-host-linux browser_stateful -- --nocapture`, `cargo test -p limux-host-linux browser_diagnostics -- --nocapture`, `cargo test -p limux-host-linux browser_tab -- --nocapture`, `cargo test -p limux-host-linux event_bus -- --nocapture`, `cargo test -p limux-host-linux -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, `cargo test -p limux-control -- --nocapture`, and `./scripts/check.sh`. |
| Browser documented unsupported APIs | `browser.viewport.set`, `browser.geolocation.set`, `browser.offline.set`, `browser.trace.start/stop`, `browser.network.route/unroute/requests`, `browser.screencast.start/stop`, `browser.input_mouse/input_keyboard/input_touch` | Not supported by CMUX | Upstream documents these as WKWebView gaps in `/tmp/cmux-audit/skills/cmux-browser/references/commands.md`. Limux now returns explicit `not_supported` errors for the CMUX CLI forms and live bridge RPC methods instead of silently passing them through or misreporting them as unknown. Verified by `cargo test -p limux-cli cmux_browser_unsupported -- --nocapture` and `cargo test -p limux-host-linux browser_unsupported -- --nocapture`. |
| Workspace lifecycle | `workspace list/create/env/close/rename/select/reconnect/disconnect/action`, `workspace-action`, `list-workspaces`, `new-workspace`, `select-workspace`, `close-workspace`, `rename-workspace`, `current-workspace` | Partial | Core workspace list/create/select/rename/close exists. Added workspace environment storage, `new-workspace --env/--env-file`, `workspace.create workspace_env`, project `cmux.json` workspace env loading for `new-workspace`/`workspace create`, `new-workspace --name/--title`, `workspace create --name/--title`, CMUX-compatible `--focus <true|false>` parsing for `new-workspace`/`workspace create`, live `workspace.create focus` handling with background creation as the default, CMUX `--layout <json>` CLI validation, live CMUX layout translation for split/pane/surface topology, layout terminal startup commands, layout browser URLs, CMUX `--description` persistence/API payloads, CMUX `--group`, `--group-placement top|end|afterCurrent`, and `--group-reference` creation placement, app-level `app.newWorkspacePlacement` loading/saving for normal Cmd-N/sidebar-button folder creation with CMUX pinned-prefix placement semantics, terminal inheritance for workspace env, `workspace env [--mask]` inspection with protected `CMUX_*`/`LIMUX_*` rejection, CMUX-compatible `workspace-action` CLI parsing plus live `workspace.action` parity for `pin`, `unpin`, `rename`, `clear-name`, `set-description`, `clear-description`, `move-up`, `move-down`, `move-top`, `close-others`, `close-above`, `close-below`, `mark-read`, `mark-unread`, `set-color`, and `clear-color`, retained CMUX event emission for `workspace.created`, `workspace.selected`, `workspace.closed`, `workspace.renamed`, `workspace.reordered`, and `workspace.action` from live host model mutation paths, persisted workspace `customColor`, and explicit CMUX `workspace reconnect`/`disconnect` CLI-to-RPC mapping with live `workspace.remote.reconnect`/`workspace.remote.disconnect` `not_supported` errors until the remote daemon exists. Verified by `cargo test -p limux-cli project_workspace_env -- --nocapture`, `cargo test -p limux-cli project_cmux_config -- --nocapture`, `cargo test -p limux-cli new_workspace_params -- --nocapture`, `cargo test -p limux-cli workspace_env -- --nocapture`, `cargo test -p limux-cli cmux_workspace_action -- --nocapture`, `cargo test -p limux-cli workspace_remote_namespace_requests_map_to_cmux_methods -- --nocapture`, `cargo test -p limux-host-linux workspace_create_route -- --nocapture`, `cargo test -p limux-host-linux workspace_action -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar_tab_round_trips_through_session_json -- --nocapture`, `cargo test -p limux-host-linux workspace_insert_index_matches_cmux_app_placements_with_pins -- --nocapture`, `cargo test -p limux-host-linux app_new_workspace_placement -- --nocapture`, `cargo test -p limux-host-linux workspace_group_state_round_trips_through_session_json -- --nocapture`, `cargo test -p limux-host-linux workspace_lifecycle -- --nocapture`, `cargo test -p limux-host-linux workspace_reordered -- --nocapture`, `cargo test -p limux-host-linux remote_workspace_methods_fail_before_dispatch -- --nocapture`, and `./scripts/check.sh`. Remaining: actual remote reconnect/disconnect behavior, true multi-window workspace movement/routing once Limux exposes multiple live GTK windows, and broader CMUX argument compatibility. |
| Workspace groups | `workspace-group list/create/ungroup/delete/rename/collapse/expand/pin/unpin/add/remove/set-anchor/new-workspace/set-color/set-icon/move/focus`; `workspace.group.*` | Implemented | Added CMUX-style persisted group model, per-workspace `group_id`, live `workspace.group.*` list and mutation APIs, `list-workspace-groups`, `workspace-group <subcommand>`, and `workspace group <subcommand>` CLI routes. Grouped sidebar rendering now emits CMUX-style group header rows, represents anchor workspaces through the header instead of a duplicate workspace row, skips child rows while collapsed, resolves selected header rows back to their anchor workspace, wires header chevron/plus controls to the existing collapse/expand and configured-placement `workspace.group.new_workspace` host actions, makes group headers participate in workspace drag/drop with CMUX top-level row planning so group runs remain contiguous and grouped children are explicitly promoted when dropped into top-level space, and shows the CMUX-shaped destructive confirmation before UI group deletion while keeping socket/CLI delete unconditional. `workspace.group.new_workspace` validates `placement`, applies CMUX top/end/afterCurrent insertion order, and resolves omitted placement from `workspaceGroups.newWorkspacePlacement` plus longest-match `workspaceGroups.byCwd[*].newWorkspacePlacement` settings. Cmd-N/sidebar-button folder creation joins the active workspace group, resolves placement from the group anchor cwd, and uses the active member as the `afterCurrent` reference. Verified by focused workspace-group tests, `cargo test -p limux-host-linux workspace_reorder_uses_top_level_rows -- --nocapture`, `cargo test -p limux-host-linux workspace_top_level_ids -- --nocapture`, `cargo test -p limux-host-linux workspace_group_delete_confirmation -- --nocapture`, `cargo test -p limux-host-linux workspace_group -- --nocapture`, `cargo test -p limux-host-linux workspace_sidebar_render_items -- --nocapture`, `cargo test -p limux-host-linux workspace_group_mutation_routes_parse_cmux_params -- --nocapture`, `cargo test -p limux-host-linux workspace_group_insert_index_matches_cmux_placements -- --nocapture`, `cargo test -p limux-host-linux workspace_group_placement -- --nocapture`, `cargo clippy -p limux-host-linux --all-targets -- -D warnings`, and `./scripts/check.sh`. |
| Pane and surface lifecycle | `new-split`, `list-panes`, `list-pane-surfaces`, `focus-pane`, `new-pane`, `new-surface`, `close-surface`, `move-surface`, `split-off`, `reorder-surface`, `drag-surface-to-split`, `refresh-surfaces`, `trigger-flash`, `surface resume`, `surface-resume`, `surface.resume.set`, `surface.resume.get`, `surface.resume.clear`, `clear-history`, `surface-health` | Partial | Live bridge supports list/create/focus, terminal surface creation, `surface.split`/`new-split` to create a real split from a source surface, CMUX watcher startup fields for split-created terminal panes (`initial_command`, `working_directory`, and strictly validated `startup_environment`) so managed Codex subagent panes can launch with startup cwd/env instead of post-create text injection, `surface.drag_to_split`/`split-off`/`drag-surface-to-split` to move a surface into a new split, `surface.close`/`close-surface` tab removal, `surface.move`/`move-surface` to another pane with optional index, `surface.reorder`/`reorder-surface` within a pane by index or before/after target, `surface.refresh`/`refresh-surfaces` terminal redraws, `surface.trigger_flash`/`trigger-flash` attention marking for addressed or focused live surfaces with CMUX-shaped flashed metadata, `surface.resume.set`/`surface.resume.get`/`surface.resume.clear` plus `surface resume`/`surface-resume` CLI forms for persisted per-terminal resume bindings, `surface.clear_history`/`clear-history` terminal scrollback clearing through Ghostty's live clear action, retained CMUX `surface.created`, `surface.focused`, `surface.closed`, `surface.moved`, `surface.reordered`, and `surface.flash_triggered` lifecycle events from successful live host mutations, and tmux pane resize/swap/join/break selected-surface movement, all with session-save scheduling where state mutates. Verified by `cargo test -p limux-host-linux pane_create_contract_accepts_cmux_startup_fields -- --nocapture`, `cargo test -p limux-host-linux pane_create_contract_rejects_invalid_startup_fields -- --nocapture`, `cargo test -p limux-host-linux surface_split_route_queues_cmux_startup_fields -- --nocapture`, `cargo test -p limux-host-linux surface_split -- --nocapture`, `cargo test -p limux-host-linux surface_drag_to_split -- --nocapture`, `cargo test -p limux-host-linux surface_close -- --nocapture`, `cargo test -p limux-host-linux surface_move -- --nocapture`, `cargo test -p limux-host-linux surface_reorder -- --nocapture`, `cargo test -p limux-host-linux surface_refresh -- --nocapture`, `cargo test -p limux-host-linux surface_trigger_flash_route_accepts_cmux_surface_refs -- --nocapture`, `cargo test -p limux-host-linux surface_resume -- --nocapture`, `cargo test -p limux-cli surface_resume -- --nocapture`, `cargo test -p limux-host-linux surface_clear_history -- --nocapture`, `cargo test -p limux-host-linux surface_lifecycle -- --nocapture`, `cargo test -p limux-host-linux pane_resize -- --nocapture`, `cargo test -p limux-host-linux pane_swap -- --nocapture`, `cargo test -p limux-host-linux pane_join -- --nocapture`, `cargo test -p limux-host-linux pane_break -- --nocapture`, `cargo test -p limux-cli cmux_surface_aliases -- --nocapture`, and `./scripts/check.sh`. Remaining: broader cross-window pane move semantics outside the current live host scope. |
| Surface port scan and shell reports | `surface.ports_kick`, `ports-kick`, `ports_kick`, `surface.report_tty`, `surface.report_ports`, `surface.clear_ports`, `surface.report_pwd`, `surface.report_shell_state`, `report_tty`, `report_ports`, `clear_ports`, `report_pwd`, `report_shell_state` | Implemented | Live bridge advertises and routes CMUX `surface.ports_kick` plus dash/underscore aliases, accepts workspace and surface/panel refs, preserves `reason`, defaults omitted surface targets to the focused/selected workspace surface, rejects explicit unknown surfaces with `not_found`, and returns current on-demand workspace port rows from the procfs discovery backend. Shell report parity now routes `surface.report_tty`, `surface.report_ports`, `surface.clear_ports`, `surface.report_pwd`, and `surface.report_shell_state` plus legacy aliases, records per-surface TTY, manually reported ports, and prompt/running state in in-memory state pruned against live surfaces, updates addressed terminal cwd from normalized `report_pwd` paths, merges reported ports into `sidebar-state` without duplicate procfs rows, renders clickable GTK sidebar port buttons, opens ports in a Limux browser split or the system URI handler according to `sidebar.openPortLinksInCmuxBrowser`, and never starts a background scanner or writes shell metadata to disk. CLI `ports-kick`/`ports_kick`, `report_tty`, `report_ports`, `clear_ports`, `report_pwd`, and `report_shell_state` map workspace/surface/panel flags while honoring Limux/CMUX workspace and surface environment fallbacks with strict port/state validation. Verified by `cargo test -p limux-host-linux surface_ports_kick -- --nocapture`, `cargo test -p limux-host-linux surface_report_routes -- --nocapture`, `cargo test -p limux-host-linux reported_ports_merge_without_duplicate_sidebar_rows -- --nocapture`, `cargo test -p limux-host-linux capabilities_include_surface_ports_kick_method -- --nocapture`, `cargo test -p limux-cli ports_kick_params -- --nocapture`, `cargo test -p limux-cli shell_report_params -- --nocapture`, and `./scripts/check.sh`. |
| Pull request sidebar reports | `surface.report_pr`, `surface.report_review`, `surface.clear_pr`, `surface.report_pr_action`, `report_pr`, `report_review`, `clear_pr`, `report_pr_action`; `sidebar-state` PR rows | Implemented | Live bridge advertises and routes CMUX PR/review sidebar telemetry, accepts workspace and surface/panel refs, validates positive PR/MR numbers, `http(s)` URLs, `open|merged|closed` states, and CMUX action hints `merge|close|reopen|create|checkout|ready|edit|view`, stores per-surface PR metadata in pruned in-memory host state, records action hints without launching external tools, exposes sorted `pull_requests` rows through `sidebar-state`, renders clickable GTK sidebar PR rows gated by `sidebar.makePullRequestsClickable`, opens PR URLs in a Limux browser split or the system URI handler according to `sidebar.openPullRequestLinksInCmuxBrowser`, and keeps the non-JSON sidebar-state output compact with `pull_requests=...`. CLI `report_pr`, `report_review`, `clear_pr`, and `report_pr_action` map workspace/surface/panel flags while honoring Limux/CMUX workspace and surface environment fallbacks, with strict URL/state/action validation. Verified by `cargo test -p limux-cli pr_report_params_accept_report_review_clear_and_actions -- --nocapture`, `cargo test -p limux-cli cmux_sidebar_state_text_includes_metadata_sections -- --nocapture`, `cargo test -p limux-host-linux surface_pr_routes_accept_cmux_sidebar_metadata -- --nocapture`, `cargo test -p limux-host-linux reported_pull_requests_render_sidebar_rows -- --nocapture`, and `./scripts/check.sh`. |
| Move tab to new workspace | `move-tab-to-new-workspace`, `detach-tab`; `tab.action` with `action=move-to-new-workspace` | Implemented | Top-level `move-tab-to-new-workspace` and its CMUX `detach-tab` alias now wrap `tab.action` with CMUX-compatible `--tab`/`--surface`, `--workspace`, `--window`, `--title`, and `--focus true|false` parsing, default detached focus to false, and reject caller-supplied `--action`. The standalone dispatcher creates a new workspace, moves the tab surface into it, names it from `--title` or the tab title, and preserves current focus when detached. The live GTK bridge advertises `tab.action`, routes `move-to-new-workspace` into the existing tab-to-workspace host path, accepts `tab:` and `surface:` aliases, supports title override and focus policy, and rejects unsupported live `tab.action` actions loudly. Verified by `cargo test -p limux-cli detach_tab_alias -- --nocapture`, `cargo test -p limux-core dispatcher_handles_surface_and_tab_actions_with_workspace_resolution -- --nocapture`, and `cargo test -p limux-host-linux tab_action_move_to_new_workspace_routes_to_live_command -- --nocapture`. |
| Tab actions | `tab-action rename/clear-name/pin/unpin/mark-read/mark-unread/reload/duplicate/close-left/close-right/close-others`; `rename-tab`; `detach-tab` | Implemented | The live GTK bridge routes CMUX `tab.action` metadata actions for `rename`, `clear-name`, `pin`, and `unpin`; read-state actions `mark-read`/`mark-unread`; browser-only `reload`/`duplicate`; relative tab closure actions `close-left`, `close-right`, and `close-others`; and move-to-workspace detach requests to the addressed tab in the resolved workspace. Responses include CMUX-shaped workspace/surface/tab refs plus pinned, unread, created, closed, skipped-pinned, and reload metadata where relevant. CLI `tab-action` serializes CMUX `--surface`, `--window`, and `--focus true|false` flags, `rename-tab` accepts `--surface` plus `LIMUX_SURFACE_ID`/`CMUX_SURFACE_ID` context fallbacks, and `detach-tab` is a strict alias for `move-tab-to-new-workspace`. Browser-only `duplicate` and `reload` still fail loudly for terminal/keybind surfaces, matching CMUX rather than spawning fake terminal clones. Verified by `cargo test -p limux-cli cmux_tab_action_accepts_surface_window_and_focus_aliases -- --nocapture`, `cargo test -p limux-cli detach_tab_alias -- --nocapture`, `cargo test -p limux-host-linux tab_action_metadata_routes_and_validates_live_command -- --nocapture`, `cargo test -p limux-host-linux tab_action_aliases_route_to_live_command -- --nocapture`, and `cargo test -p limux-host-linux tab_action_payload_includes_created_closed_and_unread_metadata -- --nocapture`. |
| Terminal I/O | `read-screen`, `capture-pane`, `send`, `send-key`, `send-panel`, `send-key-panel` | Implemented | Terminal read/send/key paths exist. Live socket text/key injection emits retained CMUX `surface.input_sent` and `surface.key_sent` events with terminal text redacted from event payloads. `read-screen`/`capture-pane` serialize CMUX `--lines` and `--scrollback` options, validate positive line counts, apply last-N line limiting, and return explicit `scrollback_requested`, `scrollback_included`, and `source` metadata. The live GTK bridge now selects Ghostty `GHOSTTY_POINT_SCREEN` bounds for scrollback reads, which Ghostty defines as available scrollback plus the active buffer, and keeps viewport reads on `GHOSTTY_POINT_VIEWPORT`. Verified by `cargo test -p limux-host-linux terminal_read_selection -- --nocapture`, `cargo test -p limux-host-linux surface_io -- --nocapture`, `cargo test -p limux-host-linux surface_input -- --nocapture`, `cargo test -p limux-host-linux read_text_route_accepts_lines_and_scrollback_flags -- --nocapture`, `cargo test -p limux-host-linux limit_text_to_last_lines_preserves_line_endings -- --nocapture`, `cargo test -p limux-cli read_screen_params_include_lines_and_scrollback -- --nocapture`, and `cargo test -p limux-cli cmux_surface_aliases -- --nocapture`. |
| Topology and resource diagnostics | `tree`, `top`, `memory`; `system.tree`, `system.top`, `system.memory` | Partial | Limux now exposes live GTK bridge `window.list`, native `system.tree`, and native `system.top`; top-level `tree` routes to `system.tree`, which snapshots the current GTK host window with workspace, pane, and surface children and returns JSON tagged with `source=limux_live_system_tree`. Top-level `top` routes to native `system.top`, supports `--workspace` filtering against Limux child-process workspace attribution, validates `--window window:1` for the current single GTK host window, accepts `--all`, samples `/proc` over a bounded `sample_ms` window to expose `cpu_percent`, supports local text sorting by sampled CPU percent, memory, or process count with `--sort cpu|mem|proc`, and supports CMUX-shaped TSV text output through `--flat`/`--format tsv` for the current local process-group diagnostics. Verified by `cargo test -p limux-host-linux memory_diagnostics -- --nocapture`, `cargo test -p limux-host-linux system_top -- --nocapture`, `cargo test -p limux-cli cmux_top -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, `cargo test -p limux-host-linux system_tree -- --nocapture`, `cargo test -p limux-cli cmux_tree -- --nocapture`, and `./scripts/check.sh`. Remaining: broader multi-window process attribution if Limux gains multiple live GTK windows. |
| Events stream | `events.stream`; CLI `events --after --cursor-file --name --category --reconnect --limit --no-ack --no-heartbeat` | Implemented | Added a host-side retained event bus with 4,096-frame replay, ack resume metadata, category/name filters, heartbeat frames, live socket takeover, bounded event frames, live subscriber `slow_consumer` cutoff at 1,024 pending matching events, durable CMUX-compatible `~/.cmuxterm/events.jsonl` append, 16 MiB rotation to `events.jsonl.1`, Feed/agent/notification/workspace lifecycle/surface lifecycle/surface I/O/config reload/browser navigation/browser interaction/redacted browser input event publication, and CLI reconnect resume from the last processed `seq`. Verified by `cargo test -p limux-host-linux event_bus -- --nocapture`, `cargo test -p limux-host-linux live_stream_closes_slow_consumers_after_pending_queue_limit -- --nocapture`, `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`, `cargo test -p limux-host-linux workspace_lifecycle -- --nocapture`, `cargo test -p limux-host-linux workspace_reordered -- --nocapture`, `cargo test -p limux-host-linux surface_lifecycle -- --nocapture`, `cargo test -p limux-host-linux config_reload -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, `cargo test -p limux-host-linux surface_input -- --nocapture`, and `cargo test -p limux-cli events_ -- --nocapture`. |
| Feed and approvals | `feed.push`, `feed.permission.reply`, `feed.question.reply`, `feed.exit_plan.reply`; `feed tui`, `feed clear`, feed hooks | Partial | Added a live host Feed backend with bounded in-memory item ring, direct socket handling for `feed.push`, `feed.list`, `feed.clear`, `feed.permission.reply`, `feed.question.reply`, and `feed.exit_plan.reply`, CMUX-shaped resolved/acknowledged/timed_out result payloads, reply mode validation, blocking wait support beyond the normal 5s control-command timeout, `limux feed clear [--yes|-y]` local deletion of `~/.cmuxterm/workstream.jsonl` and `workstream.jsonl.1`, `limux feed tui [--opentui|--legacy]` routing to the live Feed right-sidebar UI with explicit `limux-right-sidebar` implementation reporting, direct right-sidebar permission controls for pending Codex/CMUX Feed requests (`once`, `always`, `bypass`, `deny`), CMUX-compatible Codex app-server permission action capability filtering for right-sidebar and native notification action buttons (`once`/`always`/`all` only when app-server `available_decisions` and amendment payloads can map back to valid approval responses), public `limux codex-teams` launcher/watcher orchestration for Codex app-server approvals and managed subagent splits, direct right-sidebar exit-plan controls (`manual`, `autoAccept`, `bypassPermissions`, `ultraplan`, `deny`), direct right-sidebar question choice controls from CMUX/Claude question option payloads, native host notifications for pending actionable Feed permission, exit-plan, and question rows with workspace/surface targeting plus Linux inline notification action buttons through the existing freedesktop notification route, `limux hooks feed --source <agent>` CLI forwarding, CMUX-style Claude/Codex/generic hook event classification, blocking/nonblocking timeout selection, decision JSON rendering for permission, plan, and question replies, Codex hook setup wiring that installs CMUX Feed hook entries for `PreToolUse`, `PermissionRequest`, `PostToolUse`, `PreCompact`, `SubagentStart`, and `SubagentStop`, per-surface Codex wrapper shim injection for terminal tabs with `PATH` prepending plus `CMUX_CODEX_WRAPPER_SHIM`/`LIMUX_CODEX_WRAPPER_SHIM` exports, `LIMUX_CLI`/`CMUX_CLI` env wiring, wrapper-owned `hooks codex session-start` emission with surface/workspace/pid metadata, and launch metadata preservation for real Codex hook events, Claude hook setup wiring that installs blocking Feed entries for `PreToolUse` and `PermissionRequest` with Claude's matcher shape, Gemini hook setup wiring that installs blocking Feed `PreToolUse`, Cursor CLI flat JSON hook setup wiring that installs lifecycle entries plus Feed `beforeShellExecution`, Kiro CLI agent JSON hook setup wiring that installs lifecycle entries plus Feed `preToolUse` and `postToolUse`, Antigravity hook group setup wiring that installs lifecycle entries plus Feed `PreToolUse` and `PostToolUse`, Rovo Dev YAML setup wiring that installs lifecycle-only `on_complete`, `on_error`, and `on_tool_permission` hooks, OMP extension setup wiring that installs lifecycle-only `session_start`, `before_agent_start`, and `agent_end` hooks with CMUX-compatible base64 launch capture, Grok hook setup wiring that installs lifecycle entries plus blocking Feed `PreToolUse` with CMUX second-based timeout units, Pi extension setup wiring, Amp plugin setup wiring, Hermes Agent YAML/allowlist setup wiring, and Copilot, CodeBuddy, Factory, and Qoder nested JSON hook setup wiring that installs lifecycle entries plus blocking Feed `PreToolUse`; bare `limux hooks setup` now matches CMUX by considering every implemented agent integration and skipping unavailable agents by config-dir/binary checks instead of limiting defaults to Codex/Claude/Gemini. Retained `feed.item.received`, `feed.item.completed`, `feed.item.resolved`, `feed.cleared`, and `agent.hook.<HookEventName>` events, bounded CMUX-compatible `~/.cmuxterm/workstream.jsonl` audit persistence with rotation to `workstream.jsonl.1`, and a right-sidebar Feed preview while preserving `feed --help` and `hooks feed --source ...`. Verified by `cargo test -p limux-cli cmux_feed -- --nocapture`, `cargo test -p limux-cli codex_teams -- --nocapture`, `cargo test -p limux-cli antigravity -- --nocapture`, `cargo test -p limux-cli rovo -- --nocapture`, `cargo test -p limux-cli omp -- --nocapture`, `cargo test -p limux-cli cursor -- --nocapture`, `cargo test -p limux-cli kiro -- --nocapture`, `cargo test -p limux-cli grok -- --nocapture`, `cargo test -p limux-cli pi -- --nocapture`, `cargo test -p limux-cli amp -- --nocapture`, `cargo test -p limux-cli hermes -- --nocapture`, `cargo test -p limux-cli nested_json_agents -- --nocapture`, `cargo test -p limux-cli hook_install -- --nocapture`, `cargo test -p limux-cli default_hook_setup_covers_implemented_cmux_agents -- --nocapture`, direct isolated `limux-cli --json hooks setup` smokes for no available agents and fake available Codex, `cargo test -p limux-host-linux feed_actions -- --nocapture`, `cargo test -p limux-host-linux push_notifies_codex_app_server_permission_with_supported_actions -- --nocapture`, `cargo test -p limux-host-linux right_sidebar_codex_app_server_permission_actions_follow_capabilities -- --nocapture`, `cargo test -p limux-host-linux right_sidebar_feed -- --nocapture`, `cargo test -p limux-host-linux feed_sidebar_ -- --nocapture`, `cargo test -p limux-host-linux feed -- --nocapture`, `cargo test -p limux-host-linux notification -- --nocapture`, `cargo test -p limux-host-linux right_sidebar -- --nocapture`, `cargo test -p limux-host-linux event_bus -- --nocapture`, and `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`. Remaining: exact CMUX Bun/OpenTUI terminal UI parity and broader non-Codex agent-specific edge cases. |
| Right sidebar controls | `right-sidebar toggle/show/hide/focus/set/mode`, mode aliases `files/find/vault/sessions/feed/dock`; `sidebar validate/reload/select/open`; `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, `list-log`, `sidebar-state`; `right_sidebar`, `sidebar.*` | Partial | Added CMUX-compatible CLI parsing for `right-sidebar`, including `--workspace`, `--tab`, `--window`, `--no-focus`, mode aliases, silent success output for mutating commands, and JSON state output for `mode`. The live GTK bridge now exposes `right_sidebar`, validates modes, queues host-owned visibility/mode/focus state, and reports `visible`, `mode`, `focused`, `workspace_id`, and supported modes. Added workspace-scoped sidebar metadata parity for `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, `list-log`, and `sidebar-state`, backed by live GTK host state, CMUX alias bridge methods, retained `sidebar.metadata.*`, `sidebar.progress.*`, and `sidebar.log.*` events, bounded log retention, and a rendered GTK right-sidebar shell. The shell now renders mode-specific visible panels: Files uses a bounded non-recursive workspace-folder preview, Find shows the search root plus open surfaces, Vault shows non-secret metadata counts, Sessions shows pane and surface summaries, Feed shows newest retained Feed items, and Dock shows selected/open surfaces. Added custom-sidebar validation/docs/runtime JSON selection and pane-open parity: `sidebar validate` and `sidebar reload` validate `~/.config/cmux/sidebars`, `.swift` wins over `.json`, invalid JSON and empty Swift fail loudly, live `sidebar.custom.select` renders static declarative JSON sidebars in the right sidebar, live `sidebar.custom.open` opens/focuses static JSON sidebars as `customSidebar` pane tabs with CMUX-shaped refs and persisted tab state, JSON `log` plus `openURL` button actions are wired, no-parameter JSON dispatcher actions route through existing host helpers for `workspace.next`, `workspace.previous`, `workspace.last`, `window.focus`, `workspace.create`, and `notification.jump_to_unread`, documented parameterized JSON dispatcher actions route `workspace.select(workspace_id)`, `workspace.create(name?/cwd?/command?/focus?)`, `surface.focus(surface_id)`, `workspace.close(workspace_id)`, `surface.close(surface_id)`, `surface.run(surface_id?,command)`, `tab.create(workspace_id?)`, and `workspace.reorder(workspace_id,index|before_workspace_id|after_workspace_id)` through existing host helpers, static JSON style fields `spacing`, `alignment`, `padding`, `font`, `weight`, `color`, `background`, and `size` are parsed and applied in the GTK renderer, and runtime custom-sidebar validate/reload/select/open paths honor `customSidebars.beta.enabled` by hiding or rejecting sidebars while disabled. Verified by `cargo test -p limux-cli cmux_right_sidebar -- --nocapture`, `cargo test -p limux-cli cmux_sidebar -- --nocapture`, `cargo test -p limux-cli custom_sidebar -- --nocapture`, `cargo test -p limux-host-linux right_sidebar -- --nocapture`, `cargo test -p limux-host-linux sidebar_ -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar_open_route_accepts_focus_and_targets -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar_tab_round_trips_through_session_json -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, and `./scripts/check.sh`. Remaining: full interactive files/find/vault/sessions/feed/dock workflows, interpreted Swift runtime custom sidebars, hot reload/live data binding, broader parameterized dispatcher methods from sidebar buttons beyond the currently wired generic workspace/surface/tab/window methods, full picker UI, and multi-window routing beyond the current single GTK host window. |
| Agent session diagnostics | `sessions list`, `sessions debug`, `session-debug` | Partial | Added no-socket CLI inspection of saved Claude/Codex/Grok/OpenCode/Cursor/Kiro/Antigravity/RovoDev/OMP/Gemini/Copilot/CodeBuddy/Factory/Qoder hook-session stores, with `--agent`, `--session`, `--workspace`, `--surface`, `--cwd`, `--state-dir`, `--codex-home`, `--limit`, `--all`, and `--json` support; CMUX-shaped text/JSON rows; camelCase CMUX hook-store aliases for session and launch-command fields; Codex session-index/transcript diagnostics; Linux pid-anchored `/proc/<pid>/fd` transcript resolution for wrapper-started Codex sessions; native Codex session-id/path reporting without newest-file heuristics; stale saved-PID reporting so forkable sessions with dead PIDs remain visible instead of disappearing; out-of-range stored PID tolerance; concrete `fork_command` JSON/text diagnostics for agents with native fork forms, including Codex, Claude, OpenCode, Pi, and OMP, using preserved safe launch args and allowlisted environment while stripping credentials; and CMUX-compatible `hook_record_restorable`, `fork_unavailable_reason`, `fork_startup_input_available`, and `stale_pid_blocks_restore_in_0_64_17` diagnostics, including Claude transcript trust checks from direct `transcriptPath` files, `CLAUDE_CONFIG_DIR/projects/<encoded-cwd>/<session>.jsonl`, unambiguous workflow-container sibling transcript resolution, and OpenCode fork-support version probing with the CMUX minimum fork-capable version `1.14.50`, missing-executable rejection, remote-like context bypass, sanitized probe environment, and per-command probe caching. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-cli fork_command -- --nocapture`, `cargo test -p limux-cli codex_transcript -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, direct `cargo run -p limux-cli --bin limux-cli -- sessions list --agent codex --state-dir /tmp/limux-empty-sessions --json`, and direct `cargo run -p limux-cli --bin limux-cli -- session-debug --agent claude --state-dir /tmp/limux-empty-sessions`. Remaining: session diagnostics has no currently documented CMUX gap in this matrix; broader parity gaps remain in other rows. |
| Remote and SSH | `ssh`, `remote list/add/remove`, `remotes list/add/remove`, `remote-daemon-status`, `ssh-session-*`; `workspace.remote.*`; `cmuxd-remote` daemon RPC | Partial | Limux exposes explicit `not_supported` behavior for `workspace.remote.reconnect`/`workspace.remote.disconnect` live bridge methods and still fails SSH/remote-daemon CLI forms locally before socket routing when they require CMUX remote daemon support (`ssh`, `ssh-tmux`, `ssh-pty-attach`, `ssh-session-list`, `ssh-session-attach`, `ssh-session-cleanup`, `ssh-session-end`, `remote-daemon-status`, `vm-ssh-attach`) while preserving no-socket help probes. CMUX remote registry command parity is now parsed and routed through socket-backed `remotes.list`, `remotes.add`, and `remotes.remove` methods for top-level `remote`/`remotes`, including CMUX `list|ls`, `add <name> --route ... [--tag ...]`, and `remove|rm|delete <target>` aliases, command-position `--json`, local host:port/bracketed-IPv6 parsing, port-range validation, loopback/unspecified host rejection, sanitized text rendering, live bridge method advertisement, and a strict local persisted registry backend under the Limux config directory that stores non-secret display name, routes, tag, platform, generated device id, and update timestamp. Verified by `cargo test -p limux-cli cmux_remote_ssh -- --nocapture`, `cargo test -p limux-cli cmux_remotes -- --nocapture`, `cargo test -p limux-host-linux remote_registry -- --nocapture`, `cargo test -p limux-host-linux remote_workspace_methods_fail_before_dispatch -- --nocapture`, `cargo test -p limux-host-linux capabilities_include_remote_workspace_methods -- --nocapture`, and `./scripts/check.sh`. Remaining: CMUX-compatible remote daemon, SSH workspace proxy, lease/auth file protocol, remote PTY/session RPC, and cloud/team presence syncing beyond the local registry. |
| tmux compatibility | `capture-pane`, `resize-pane`, `pipe-pane`, `wait-for`, `swap-pane`, `break-pane`, `join-pane`, `next-window`, `previous-window`, `last-window`, `last-pane`, `find-window`, `clear-history`, `list-windows`, `list-panes`, buffers/hooks/respawn/display-message | Partial | Several compatibility wrappers exist in CLI. Live bridge now supports `workspace.next`/`next-window`, `workspace.previous`/`previous-window`, and `workspace.last`/`last-window` against the real GTK workspace stack with previous-workspace tracking, `pane.last`/`last-pane` using live control-bridge pane focus history, `surface.clear_history`/`clear-history` for addressed or focused terminal surfaces, `pane.resize`/`resize-pane` by mutating persisted GTK split ratios, `pane.swap`/`swap-pane` by exchanging selected surfaces between panes, `pane.join`/`join-pane` by moving an addressed or focused surface into a target pane, `pane.break`/`break-pane` by moving an addressed or focused surface into a newly-created workspace, `surface.respawn`/`respawn-pane` by replacing the addressed terminal tab process while preserving the surface ID, `split-window`/`splitw` by mapping tmux `-h`/`-v`/`-b` directions, `-d` detached focus preservation, exact live-geometry `-l` initial divider sizing, `-c` working directory, `-t` surface target, command injection, and `-P`/`-F` formatted output to `surface.split`, and `select-layout` by mapping main layouts to `workspace.equalize_splits` orientation. CLI-local tmux buffers, hooks, aliases, and display-message handling now match more CMUX defaults for the documented commands: default buffer name, `-b` buffer names, explicit missing-buffer errors for paste, empty output for missing show-buffer, size-reporting `list-buffers`, `set-hook --list/--unset`, `capturep`, `display/displayp`, `resizep`, `respawnp`, `setb`, `pasteb`, `show-buffer/showb`, lifecycle aliases `neww`, `select-window`/`selectw`, `select-pane`/`selectp`, `kill-window`/`killw`, `kill-pane`/`killp`, and `renamew`, `lsw`, `lsp`, `list-windows -F/--format`, `list-panes -F/--format`, `pipe-pane --command` and trailing shell-command parsing, `wait-for` 30-second/fractional timeout handling, `find-window` multi-match/no-match text plus `--content`/`--select` routing, real active terminal rows/columns/pixel/cell metrics in `pane.list`, explicit placeholder rejection for `popup`, `bind`/`bind-key`, `unbind`/`unbind-key`, and `copy-mode`, plus `display-message -p/-F/-t` parsing with known `#{...}` expansion, unknown-key stripping, trim, and empty-render fallback behavior backed by live workspace/pane/surface context. Verified by `cargo test -p limux-host-linux workspace_navigation -- --nocapture`, `cargo test -p limux-host-linux pane_last -- --nocapture`, `cargo test -p limux-host-linux surface_clear_history -- --nocapture`, `cargo test -p limux-host-linux pane_resize -- --nocapture`, `cargo test -p limux-host-linux pane_swap -- --nocapture`, `cargo test -p limux-host-linux pane_join -- --nocapture`, `cargo test -p limux-host-linux pane_break -- --nocapture`, `cargo test -p limux-host-linux surface_respawn -- --nocapture`, `cargo test -p limux-host-linux surface_split -- --nocapture`, `cargo test -p limux-core dispatcher_handles_pane_and_surface_flow -- --nocapture`, `cargo test -p limux-cli respawn_pane_request -- --nocapture`, `cargo test -p limux-cli tmux_pipe_pane -- --nocapture`, `cargo test -p limux-cli tmux_wait_for -- --nocapture`, `cargo test -p limux-cli tmux_find_window -- --nocapture`, `cargo test -p limux-cli tmux_ -- --nocapture`, `cargo test -p limux-cli cmux_ -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, `cargo test -p limux-host-linux ratio -- --nocapture`, and `./scripts/check.sh`. Remaining: full remote tmux daemon parity and exact advanced tmux layout state tracking. |
| tmux documented unsupported placeholders | `popup`, `bind`/`bind-key`, `unbind`/`unbind-key`, `copy-mode` | Not supported by CMUX | Upstream CLI contract marks these as placeholders/unsupported. Limux exposes no-socket help probes for the placeholder commands and routes each CLI spelling into one explicit `not supported` failure path rather than treating aliases as unknown commands or pretending the features work. Verified by `cargo test -p limux-cli tmux_ -- --nocapture`. |
| Config/settings/themes/docs | `settings`, `config doctor/check/validate/path/paths/docs/reload/get/set`, `themes list/set/clear`, `docs settings/shortcuts/api/browser/agents/dock/sidebars` | Partial | Implemented no-socket CLI parity for `docs`, `docs --json`, `docs list|all --json`, `docs <topic> --json`, `docs dock` plus dock aliases, `docs sidebars` plus custom-sidebar aliases, `settings path/paths/docs`, CMUX-shaped JSON output for `settings paths --json` and `settings docs --json`, strict CMUX arity failures for extra `settings path/docs` arguments, CMUX host-backed default `settings` open behavior, direct `settings <target>` aliases, `settings open [target]`, `shortcuts`, `config help`, `config path/paths/check/validate/doctor/docs/documentation`, CMUX-shaped JSON output for `config path --json`, `config paths --json`, and `config docs --json`, strict CMUX arity failures for extra `config path/docs` arguments, CMUX-style `config doctor --path <path>` custom target validation, default config doctor inclusion of nearest project `.cmux/cmux.json`/`cmux.json`, JSONC comment and trailing-comma preprocessing for `config doctor`, `config get/set sidebar-font-size`, `config get/set surface-tab-bar-font-size`, `config get/set notifications.*`, `config get/set app.appearance`, `config get/set app.newWorkspacePlacement`, `config get/set workspaceGroups.newWorkspacePlacement`, `config get/set app.workspaceInheritWorkingDirectory`, `config get/set app.focusPaneOnFirstClick`, `config get/set terminal.autoResumeAgentSessions`, upstream Account catalog keys for `account.piiDisplayMode`, `account.selectedTeamID`, and `account.welcomeShown`, upstream Integrations catalog keys for `integrations.claudeCode.*`, `integrations.codex.hooksEnabled`, `integrations.amp.hooksEnabled`, `integrations.cursor.hooksEnabled`, `integrations.gemini.hooksEnabled`, `integrations.kiro.*`, `integrations.ripgrep.customBinaryPath`, and `integrations.suppressSubagentNotifications`, and upstream Markdown catalog `config get/set` keys for `markdown.fontSize`, `markdown.fontFamily`, and `markdown.maxWidth`, upstream FileEditor and Canvas catalog keys for `fileEditor.wordWrap`, `canvas.paneGap`, and `canvas.snappingEnabled`, upstream Browser catalog `config get/set` keys for `browser.defaultSearchEngine`, custom search name/template, search suggestions, browser theme, hidden WebView discard settings, download prompt, terminal-link routing flags, embedded/external URL allowlists, React grab version, browser disabled override, and import-hint flags. Direct `config sidebar-font-size [points]` and `config surface-tab-bar-font-size [points]` aliases work. CMUX-compatible account/integrations/app/workspace/terminal/notification/markdown/fileEditor/canvas/browser settings JSON load/save preserves unrelated keys and rejects invalid values; CMUX-style `themes list/set/clear` manages a marked Ghostty `theme = ...` config block with light/dark conditional values and JSON output, including CMUX's strict `themes list` arity failure for positional extras; live socket `config reload`/`reload-config`/`reload_config`/`config.reload` reloads settings, shortcuts, and Ghostty config, applies appearance and terminal font-size changes, and emits retained `config.reloaded`; live host `settings open [target]` routes through `settings.open` with CMUX target aliases, bridge-side target validation, `activate` parsing, target echoing, and nearest-page GTK dialog presentation. Font-size values use CMUX implementation defaults/ranges from `/tmp/cmux-audit/CLI/CMUXCLI+Config.swift`; Browser values use upstream defaults/enums from `/tmp/cmux-audit/Packages/macOS/CmuxSettings/Sources/CmuxSettings/Keys/BrowserCatalogSection.swift`; Markdown values use upstream defaults from `/tmp/cmux-audit/Packages/macOS/CmuxSettings/Sources/CmuxSettings/Keys/MarkdownCatalogSection.swift`; Account values use upstream defaults from `/tmp/cmux-audit/Packages/macOS/CmuxSettings/Sources/CmuxSettings/Keys/AccountCatalogSection.swift` and `/tmp/cmux-audit/Packages/macOS/CmuxSettings/Sources/CmuxSettings/Values/PIIDisplayMode.swift`; Integrations values use upstream defaults from `/tmp/cmux-audit/Packages/macOS/CmuxSettings/Sources/CmuxSettings/Keys/IntegrationsCatalogSection.swift`; FileEditor and Canvas values use upstream defaults from `/tmp/cmux-audit/Packages/macOS/CmuxSettings/Sources/CmuxSettings/Keys/FileEditorCatalogSection.swift` and `/tmp/cmux-audit/Packages/macOS/CmuxSettings/Sources/CmuxSettings/Keys/CanvasCatalogSection.swift`. Verified by `cargo test -p limux-cli cmux_docs -- --nocapture`, `cargo test -p limux-cli local_docs_command -- --nocapture`, `cargo test -p limux-cli settings_open -- --nocapture`, `cargo test -p limux-cli config_path_outputs_cmux_json_shape -- --nocapture`, `cargo test -p limux-cli custom_sidebar -- --nocapture`, `cargo test -p limux-cli theme -- --nocapture`, `cargo test -p limux-cli config_browser_scalar_settings -- --nocapture`, `cargo test -p limux-cli config_account_scalar -- --nocapture`, `cargo test -p limux-host-linux account_settings -- --nocapture`, `cargo test -p limux-cli config_integrations_scalar -- --nocapture`, `cargo test -p limux-host-linux integrations_settings -- --nocapture`, `cargo test -p limux-host-linux malformed_integration_subsections -- --nocapture`, `cargo test -p limux-cli config_markdown -- --nocapture`, `cargo test -p limux-cli config_file_editor_and_canvas -- --nocapture`, `cargo test -p limux-host-linux file_editor_and_canvas -- --nocapture`, `cargo test -p limux-host-linux markdown_settings -- --nocapture`, `cargo test -p limux-host-linux markdown_response_metadata -- --nocapture`, direct isolated `limux-cli config get/set browser.defaultSearchEngine` and `browser.theme` smoke, isolated `XDG_CONFIG_HOME` direct `limux-cli --json themes set/list` smoke, `cargo test -p limux-cli config_font_size -- --nocapture`, `cargo test -p limux-cli config_notification_settings -- --nocapture`, `cargo test -p limux-cli config_app_appearance -- --nocapture`, `cargo test -p limux-cli config_workspace_placement -- --nocapture`, `cargo test -p limux-cli config_app_workspace_inherit -- --nocapture`, `cargo test -p limux-cli config_app_focus_pane_on_first_click -- --nocapture`, `cargo test -p limux-cli config_terminal_auto_resume_agent_sessions -- --nocapture`, `cargo test -p limux-cli config_reload -- --nocapture`, `cargo test -p limux-cli config_ -- --nocapture`, `cargo test -p limux-cli jsonc -- --nocapture`, host config/settings tests, and `./scripts/check.sh`. Remaining: bundled/interactive Ghostty theme picker parity, exact settings-page UI for every CMUX target beyond Limux's current General/Keybindings pages, and upstream `config get/set` keys beyond the implemented font-size, notification, account, integrations, markdown, fileEditor, canvas, browser, sound, app behavior, workspace-placement, and terminal behavior settings. |
| Sidebar display settings | `config get/set sidebar.hideAllDetails`, `sidebar.wrapWorkspaceTitles`, `sidebar.showWorkspaceDescription`, `sidebar.showNotificationMessage`, `sidebar.showBranchDirectory`, `sidebar.branchLayout`, `sidebar.showPullRequests`, `sidebar.watchGitStatus`, `sidebar.showPorts`, `sidebar.showSSH`, `sidebar.makePullRequestsClickable`, `sidebar.openPullRequestLinksInCmuxBrowser`, `sidebar.openPortLinksInCmuxBrowser`, `sidebar.showCustomMetadata`, `sidebar.showProgress`, `sidebar.showLog`, `sidebar.rightMaxWidth`; sidebar row title/detail visibility; right-sidebar metadata visibility; terminal git/PR watch env; sidebar-state port discovery/visibility/text rendering; right-sidebar width policy | Implemented | Added CMUX-compatible settings JSON load/save/defaults for `sidebar.hideAllDetails`, `sidebar.wrapWorkspaceTitles`, `sidebar.showWorkspaceDescription`, `sidebar.showNotificationMessage`, `sidebar.showBranchDirectory`, `sidebar.branchLayout`, `sidebar.showPullRequests`, `sidebar.watchGitStatus`, `sidebar.showPorts`, `sidebar.showSSH`, `sidebar.makePullRequestsClickable`, `sidebar.openPullRequestLinksInCmuxBrowser`, `sidebar.openPortLinksInCmuxBrowser`, `sidebar.showCustomMetadata`, `sidebar.showProgress`, `sidebar.showLog`, and optional `sidebar.rightMaxWidth`, local CLI `config get/set` support with strict boolean/string/numeric validation, live config-reload resync for open workspace rows and the right-sidebar width request, title wrapping through `wrapWorkspaceTitles`, workspace description detail rows through `showWorkspaceDescription`, unread sidebar notification text gating through `showNotificationMessage`, branch/directory detail rows gated through `showBranchDirectory`, branch/directory layout through `branchLayout` using direct `.git/HEAD` metadata reads instead of spawned `git` processes, terminal startup env policy for `CMUX_NO_GIT_WATCH` and `CMUX_NO_PR_WATCH` from `watchGitStatus`/`showPullRequests`, startup-env override protection for those managed vars, sidebar-state port rows discovered from workspace-attributed child listener sockets through one on-demand `/proc` scan and gated by `showPorts` plus the master `hideAllDetails` detail switch, `openPortLinksInCmuxBrowser` reflected in JSON port-row policy fields, non-JSON `sidebar-state` rendering of comma-separated `ports=...`, all detail rows suppressed by `hideAllDetails` while preserving unread dots, right-sidebar Status/Progress/Log section visibility gated by `showCustomMetadata`, `showProgress`, and `showLog`, and CMUX `rightMaxWidth` clamping semantics with min `276`, default max `1200`, settings max `4096`, and reserved terminal width `360`. Runtime clickable PR-link UI activation, clickable port-link UI activation, and SSH row rendering remain separate gaps because Limux now exposes real PR metadata through sidebar-state but does not yet provide GTK-clickable PR or port link widgets or SSH workspace metadata rows. Verified by `cargo test -p limux-cli config_sidebar_settings -- --nocapture`, `cargo test -p limux-cli cmux_sidebar_state_text_includes_metadata_sections -- --nocapture`, `cargo test -p limux-host-linux sidebar_preferences -- --nocapture`, `cargo test -p limux-host-linux sidebar_branch_directory -- --nocapture`, `cargo test -p limux-host-linux sidebar_git_branch -- --nocapture`, `cargo test -p limux-host-linux port_discovery -- --nocapture`, `cargo test -p limux-host-linux workspace_port_rows_discovers_child_listener -- --nocapture`, `cargo test -p limux-host-linux sidebar_ports_rows -- --nocapture`, `cargo test -p limux-host-linux git_watch_env -- --nocapture`, `cargo test -p limux-host-linux pane_create_contract_rejects_invalid_startup_fields -- --nocapture`, `cargo test -p limux-host-linux right_sidebar_width -- --nocapture`, `cargo test -p limux-host-linux right_sidebar_metadata -- --nocapture`, `cargo test -p limux-host-linux unread_visual_gate_respects_notification_setting -- --nocapture`, `cargo test -p limux-host-linux app_config -- --nocapture`, touched-crate clippy, and `./scripts/check.sh`. |
| Terminal auto-resume agent sessions setting | `config get/set terminal.autoResumeAgentSessions`; restored agent session launch policy | Implemented | `config get/set terminal.autoResumeAgentSessions` now uses CMUX's `true` default, writes nested terminal settings JSON, rejects invalid boolean values loudly, and host settings load/save preserves the key. When disabled, restored agent panes keep their saved agent metadata but `restore_on_startup` is cleared for hook-restored agents, so Codex/Claude/OpenCode-style resume commands are not auto-run on reopen. Verified by `cargo test -p limux-cli config_terminal_auto_resume_agent_sessions -- --nocapture`, `cargo test -p limux-host-linux terminal_auto_resume_agent_sessions -- --nocapture`, and `cargo test -p limux-host-linux restorable_agent_auto_resume -- --nocapture`. |
| Terminal/custom-sidebar/beta settings | `config get/set terminal.showScrollBar`, `terminal.copyOnSelect`, `terminal.agentHibernation.*`, `terminal.rendererRealization.*`, `terminal.titleUpdates.*`, `terminal.showTextBoxOnNewTerminals`, `terminal.focusTextBoxOnNewTerminals`, `terminal.textBoxMaxLines`, `terminal.textBoxDefaultSubmitAction`, `terminal.textBoxSubmitActions`, `terminal.resumeCommands`, `terminal.scrollSpeed`, `terminal.runawayMemoryGuardrail.*`, `customSidebars.renderer`, `customSidebars.beta.enabled`, `rightSidebar.beta.feed.enabled`, `rightSidebar.beta.dock.enabled`, `extensions.beta.enabled`, `remoteTmux.beta.enabled` | Implemented config parity | Local `config get/set` now supports the remaining Linux-meaningful CMUX terminal, custom-sidebar, and beta-feature settings with real nested JSON paths, CMUX defaults, strict boolean/integer/decimal/string/enum/JSON-array validation, decimal preservation for `terminal.scrollSpeed` and memory thresholds, and sibling-key preservation. Host settings load/save validates and round-trips the same keys, preserving unrelated terminal and right-sidebar siblings. Runtime effects remain limited to existing Limux behavior unless separately wired; hibernation, renderer realization, text-box, extension, and remote-tmux behavior are now explicit validated config parity rather than claimed runtime parity. Verified by `cargo test -p limux-cli config_terminal_scalar -- --nocapture`, `cargo test -p limux-cli config_custom_sidebar_and_beta -- --nocapture`, `cargo test -p limux-cli config_scalar_settings_reject -- --nocapture`, `cargo test -p limux-host-linux terminal_scalar -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, `cargo test -p limux-host-linux invalid_terminal_resume_commands -- --nocapture`, and `cargo test -p limux-host-linux save_to_path_writes_terminal_custom_sidebar -- --nocapture`. |
| App first-click pane focus setting | `config get/set app.focusPaneOnFirstClick`; terminal click-to-focus behavior | Implemented | `config get/set app.focusPaneOnFirstClick` now uses CMUX's runtime `false` default from `PaneFirstClickFocusSettings.defaultEnabled`, writes nested app settings JSON, rejects invalid boolean values loudly, and host settings load/save preserves the key. Terminal mouse press handling now requests terminal focus only when the setting is enabled. Verified by `cargo test -p limux-cli config_app_focus_pane_on_first_click -- --nocapture`, `cargo test -p limux-host-linux focus_pane_on_first_click -- --nocapture`, and `cargo test -p limux-host-linux terminal_click_focus -- --nocapture`. |
| App workspace cwd inheritance setting | `config get/set app.workspaceInheritWorkingDirectory`; host new-workspace cwd selection | Implemented | `config get/set app.workspaceInheritWorkingDirectory` now uses CMUX's `true` default, writes nested app settings JSON, rejects invalid boolean values loudly, and host settings load/save preserves the key. Runtime workspace creation now keeps explicit `cwd` authoritative, inherits the active workspace folder/cwd when enabled, and leaves the new workspace cwd unset when disabled so terminal defaults can apply. Verified by `cargo test -p limux-cli config_app_workspace_inherit -- --nocapture`, `cargo test -p limux-host-linux workspace_inherit -- --nocapture`, `cargo test -p limux-host-linux workspace_creation_directory -- --nocapture`, `cargo test -p limux-host-linux workspace_title_from_directory -- --nocapture`, `cargo test -p limux-host-linux save_writes_gtk_and_ghostty_color_schemes -- --nocapture`, and a direct temporary `XDG_CONFIG_HOME` `limux-cli config get/set` smoke. |
| App keep-workspace setting | `config get/set app.keepWorkspaceOpenWhenClosingLastSurface`; host behavior when the final surface closes | Implemented | `config get/set app.keepWorkspaceOpenWhenClosingLastSurface` now uses CMUX's `false` default, writes the nested `app` settings JSON, rejects invalid boolean values loudly, and the GTK host loads/saves the setting. When enabled, closing the final surface keeps the workspace alive by opening a fresh terminal surface in the now-empty pane, preserving Limux's non-empty-pane invariant while matching CMUX's keep-workspace-open intent. Verified by `cargo test -p limux-cli config_app_keep_workspace_open -- --nocapture`, `cargo test -p limux-host-linux keep_workspace_open -- --nocapture`, `cargo test -p limux-host-linux save_writes_gtk_and_ghostty_color_schemes -- --nocapture`, and a direct temporary `XDG_CONFIG_HOME` `limux-cli config get/set` smoke. |
| CLI globals and environment | `--socket`, `--password`, `--json`, `--id-format`, `--window`; `CMUX_SOCKET_PATH`, `CMUX_SOCKET`, `CMUX_SOCKET_PASSWORD`, `CMUX_WORKSPACE_ID`, `CMUX_SURFACE_ID`, `CMUX_TAB_ID` | Partial | Limux now supports `CMUX_SOCKET_PATH`/`CMUX_SOCKET` checked routing, global `--window` parsing for window aliases, global `--password` parsing, CMUX password precedence of explicit `--password`, `CMUX_SOCKET_PASSWORD`, then `~/.local/state/limux/socket-control-password`, `LIMUX_SOCKET_MODE=password`/`CMUX_SOCKET_MODE=password` startup validation, same-user peer gating before the password handshake, JSON `auth.login`/`auth.status`, CMUX legacy `auth <password>` text responses, no-socket `help`/`version`, no-socket CMUX command help probes for socket-backed and legacy alias commands, command-position `--json`/`--id-format` presentation flags outside browser commands, and CMUX workspace/surface/tab env aliases in CLI context lookup and spawned terminal env. Verified by `cargo test -p limux-cli client_sends_auth_login_before_request_when_password_is_set -- --nocapture`, `cargo test -p limux-control socket_password_resolver_prefers_explicit_then_env_then_file -- --nocapture`, `cargo test -p limux-host-linux password_mode_rejects_request_until_auth_login_succeeds -- --nocapture`, `cargo test -p limux-host-linux legacy_cmux_auth_command_returns_text_status -- --nocapture`, `cargo test -p limux-cli cmux_command_help -- --nocapture`, `cargo test -p limux-cli cmux_non_help_probe -- --nocapture`, `cargo test -p limux-cli cmux_command_position -- --nocapture`, direct `cargo run -q -p limux-cli --bin limux-cli -- sessions list --state-dir /tmp/limux-no-sessions --json`, direct `cargo run -p limux-cli --bin limux-cli -- list-panes --help`, direct `cargo run -p limux-cli --bin limux-cli -- list-windows --help`, focused touched-crate `cargo test -p limux-control -p limux-cli -p limux-host-linux`, and `./scripts/check.sh`. Remaining: full multi-window routing. |

Primary upstream references:

Recent verified parity updates:

- 2026-07-04: Added CMUX-compatible `sidebarAppearance.*` and `workspaceColors.*` settings parity from upstream CMUX `7f94c9352f5267c2c01319d1aaa73b1e2207890e`: local `config get/set` now supports `sidebarAppearance.matchTerminalBackground`, `sidebarAppearance.tintColor`, `sidebarAppearance.lightModeTintColor`, `sidebarAppearance.darkModeTintColor`, `sidebarAppearance.tintOpacity`, `sidebarAppearance.blurOpacity`, `sidebarAppearance.cornerRadius`, `sidebarAppearance.preset`, `sidebarAppearance.material`, `sidebarAppearance.blendMode`, `sidebarAppearance.state`, `workspaceColors.indicatorStyle`, `workspaceColors.selectionColor`, `workspaceColors.notificationBadgeColor`, `workspaceColors.colors`, `workspaceColors.paletteOverrides`, and `workspaceColors.customColors`. Values use CMUX defaults, legacy workspace indicator aliases normalize to modern values, colors normalize to uppercase `#RRGGBB`, structured palette arrays/maps are strict JSON, and host settings load/save round-trip the same sections. Verified by focused `limux-cli` config tests, focused `limux-host-linux` app-config tests, and touched-crate clippy.
- 2026-07-04: Added CMUX-compatible automation/mobile settings config parity for non-secret upstream catalog keys: `config get/set` now supports `automation.socketControlMode`, `automation.claudeCodeIntegration`, `automation.claudeBinaryPath`, `automation.workspaceAutoNaming`, `automation.autoNamingAgent`, `automation.ripgrepBinaryPath`, `automation.suppressSubagentNotifications`, `automation.ampIntegration`, `automation.cursorIntegration`, `automation.geminiIntegration`, `automation.kiroIntegration`, `automation.kiroNotificationLevel`, `automation.portBase`, `automation.portRange`, `mobile.iOSPairingHost.enabled`, `mobile.iOSPairingHost.port`, and `mobile.iOSPairingHost.displayName`; host settings load/save strictly validates and round-trips the same JSON sections. `automation.socketPassword` remains separate secret-file work, not a printable JSON scalar. Verified by focused `limux-cli` config tests and `limux-host-linux` app-config tests.
- 2026-07-03: Added CMUX-compatible pane chrome settings parity from upstream `PaneChromeCatalogSection.swift`: root `config get/set` now supports `paneBorderColor` and `activePaneBorderColor`, accepts CMUX hex colors with or without `#`, normalizes to uppercase `#RRGGBB`, treats JSON `null` as clear/default, rejects malformed colors loudly, host settings load/save round-trips both root keys, and the GTK pane root applies configured normal and focused pane border CSS. Verified by `cargo test -p limux-cli config_pane_chrome -- --nocapture`, `cargo test -p limux-host-linux pane_chrome -- --nocapture`, and `cargo test -p limux-host-linux build_window_css_uses_resolved_background_opacity -- --nocapture`.
- 2026-07-03: Added CMUX-compatible `trigger-flash` / `surface.trigger_flash` parity: CLI parsing maps workspace and surface/panel selectors to the live RPC, the GTK bridge advertises and routes the method, and the host marks the addressed or focused surface's workspace as needing attention while publishing `surface.flash_triggered` lifecycle events and returning `flashed` metadata. Verified by `cargo test -p limux-cli cmux_surface_aliases_map_to_limux_methods -- --nocapture` and `cargo test -p limux-host-linux surface_trigger_flash_route_accepts_cmux_surface_refs -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `markdown open <path>` / `markdown <path>` / `markdown.open` parity: CLI parsing accepts routing, direction, focus, and bounded font-size flags with loud validation, canonicalizes existing files, and the live GTK bridge opens the markdown file in a real WebKit browser split through an encoded `file://` URL while returning markdown path/URL metadata. Verified by `cargo test -p limux-cli cmux_markdown_open -- --nocapture` and `cargo test -p limux-host-linux markdown_open_route_queues_browser_pane -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `current-window` / `window.current` live bridge parity and made `new-window` / `window.create` plus `close-window` / `window.close` explicit single-host limitations instead of unknown methods: CLI parsing now rejects `close-window` without `--window`, the live GTK bridge advertises/routes all three window methods, `window.current` returns the current `window:1` metadata, and create/close return `not_supported` until true multi-window GTK support exists. Verified by `cargo test -p limux-cli cmux_window_and_workspace_aliases_map_to_limux_methods -- --nocapture` and `cargo test -p limux-host-linux window_current_create_and_close_routes_are_explicit -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `simulate-sidebar-drag` / `debug.sidebar.simulate_drag` parity for the current live Limux window: CLI parsing now accepts required `--window`, `--from`, and `--to` workspace refs plus positive `--duration-ms` and `--steps` controls, the live GTK bridge advertises/routes the CMUX debug method and alias with loud validation, and the host returns a non-mutating simulation summary with `simulated=true`, `committed=false`, edge, step, duration, window, and workspace metadata. Verified by `cargo test -p limux-cli cmux_simulate_sidebar_drag_builds_debug_params -- --nocapture` and `cargo test -p limux-host-linux simulate_sidebar_drag_route_queues_command -- --nocapture`.
- 2026-07-02: Added CMUX-compatible top-level `move-workspace-to-window` parity: CLI parsing now serializes `--workspace` and `--window` to `workspace.move_to_window` with loud missing-flag errors, the live GTK bridge advertises/routes `workspace.move_to_window` and `move-workspace-to-window`, and the host accepts the current single `window:1` target while rejecting unsupported windows explicitly until true multi-window GTK windows exist. Verified by `cargo test -p limux-cli cmux_window_and_workspace_aliases_map_to_limux_methods -- --nocapture`, `cargo test -p limux-host-linux workspace_move_to_window_route_queues_command -- --nocapture`, and `cargo test -p limux-core dispatcher_handles_workspace_and_window_flow -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `reorder-workspace`, `reorder-workspaces`, live `workspace.reorder`, and live/core `workspace.reorder_many` parity: CLI parsing now supports `--workspace` or positional workspace refs, `--index`, `--before`/`--before-workspace`, `--after`/`--after-workspace`, `--order`, `--window`, and `--dry-run`; the live GTK bridge advertises/routes both reorder RPCs with strict target and duplicate validation; single reorders reuse existing workspace-order mutation, event, and persistence paths; batch reorders place requested workspaces first while preserving unlisted workspace order. Verified by `cargo test -p limux-cli cmux_reorder_workspace_commands_build_params -- --nocapture`, `cargo test -p limux-host-linux workspace_reorder_routes_parse_cmux_params -- --nocapture`, and `cargo test -p limux-core dispatcher_workspace_reorder_many_moves_leading_order -- --nocapture`.
- 2026-07-02: Added CMUX-compatible split/new-tab cwd inheritance parity from upstream `5d1d14ce3d`: new terminal tabs, command-created tabs, `pane.create`, and `surface.split` now prefer the source pane's active terminal cwd before falling back to the pane/workspace cwd, while explicit launch `working_directory` remains authoritative. Verified by `cargo test -p limux-host-linux inherited_terminal_working_directory_prefers_active_terminal_cwd -- --nocapture`, `cargo test -p limux-host-linux pane_create_contract -- --nocapture`, and `cargo test -p limux-host-linux surface_split -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `workspace-action` and live `workspace.action` metadata parity: CLI parsing now accepts `--action` or positional actions plus `--workspace`, `--window`, `--title`, `--description`, and `--color` flags, the live GTK bridge advertises/routes `workspace.action`, and host mutations support `pin`, `unpin`, `rename`, `clear-name`, `set-description`, and `clear-description` with CMUX-shaped payloads, lifecycle events, and session persistence while unsupported sub-actions fail loudly. Verified by `cargo test -p limux-cli cmux_workspace_action -- --nocapture`, `cargo test -p limux-host-linux workspace_action -- --nocapture`, and touched-crate clippy.
- 2026-07-02: Completed the remaining CMUX `workspace.action` sub-actions for movement, close scopes, read state, and color: `move-up`, `move-down`, `move-top`, `close-others`, `close-above`, `close-below`, `mark-read`, `mark-unread`, `set-color`, and `clear-color` now route through the live GTK host with CMUX-shaped payloads, existing reorder/close/read paths, persisted workspace `customColor`, and visible sidebar title color. Verified by `cargo test -p limux-cli cmux_workspace_action -- --nocapture`, `cargo test -p limux-host-linux workspace_action -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar_tab_round_trips_through_session_json -- --nocapture`, and touched-crate clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar `workspace.sort(param: "unread"|"recent")` parity for the upstream cockpit sidebar sort menu: actions now validate strict sort modes, reorder live workspaces by pinned tier plus unread or latest retained notification activity, preserve active workspace selection, keep group runs normalized, emit existing workspace reorder events when changed, and persist changed order. Verified by `cargo test -p limux-host-linux custom_sidebar_dispatcher_actions_accept_documented_cmux_params -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar `workspace.copyBranch(workspace_id)` parity for upstream Swift activity sidebar buttons: actions now resolve CMUX workspace handles, read the workspace folder/cwd git branch through the existing no-spawn `.git/HEAD` parser, write GTK standard and primary clipboards, and fail loudly when no branch or GTK display exists. Verified by `cargo test -p limux-host-linux custom_sidebar_dispatcher_actions_accept_documented_cmux_params -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar `noop` action parity for upstream Swift design-system corpus buttons: actions now accept ignored payload fields such as `value`, show a supported `noop` tooltip, and execute without mutating host state or requiring dispatcher state. Verified by `cargo test -p limux-host-linux custom_sidebar_dispatcher_actions_accept_documented_cmux_params -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar `workspace.create` parity for upstream Swift sidebar buttons: actions support no-param background workspace creation, optional `name`/`title`/`template` title seeds, optional `cwd`/`working_directory`, optional `command` startup injection, strict boolean `focus`, real GTK workspace model creation, session persistence, CMUX workspace payloads, and loud validation for malformed focus params. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture` and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar `tab.create` parity for upstream Swift sidebar plus buttons: actions parse optional workspace targets from `workspace_id`, `workspace`, `param`, or `value`, default to the active workspace only when omitted, create a real terminal surface in the target workspace focused pane, optionally run `command`/`text` startup content, publish `surface.created` lifecycle events with `origin=tab.create`, persist session state, and fail loudly for explicit unknown workspaces or missing panes. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture` and host clippy.
- 2026-07-02: Added live GTK bridge and JSON custom-sidebar `window.focus` parity for upstream CMUX window focus actions: the bridge advertises/routes `window.focus`, explicit `window_id`/`window`/`id` values are validated against Limux's current single GTK host window, no-id sidebar calls focus the current host window for upstream Swift corpus context menus, unsupported window refs fail loudly, and responses include CMUX-shaped `window_id`/`window_ref` data. Verified by `cargo test -p limux-host-linux window_focus_route_queues_command -- --nocapture`, `cargo test -p limux-host-linux custom_sidebar -- --nocapture`, and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar `surface.run` parity for upstream command buttons: actions now parse required `command` text plus optional `surface_id`/`tab_id`, run the command in the addressed terminal or focused terminal when omitted, send a trailing newline through the existing Ghostty terminal injection path, publish redacted surface input events, and fail loudly for missing commands or missing terminal surfaces. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture` and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar clipboard action parity for upstream `clipboard.write(text:)`: sidebar actions now read copy text from `message`, `text`, `param`, or `value`, show copy previews in tooltips, write both GTK standard and primary clipboards when a display is available, and fail loudly when text or display is missing. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture` and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar URL action parity for upstream `openURL(param:)`, `open.url(url:)`, `link.open(url:)`, and `pr.open(url:)`: URL actions now read `message`, `url`, `param`, or `value`, show the resolved URL in button tooltips, and open through the existing GTK URI launcher with loud missing-URL feedback. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture` and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar alias parity for upstream `selectWorkspace(param)`, `tab.focus(param)`, and `tab.close(param)`: camel-case workspace selection now routes to the existing workspace selector, while tab focus/close resolve tab or surface handles through existing surface focus/close helpers. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture` and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar tab pin action parity for upstream `tab.togglePin(param: tab.id)`: sidebar buttons now parse `tab_id`, `surface_id`, `param`, or `value` tab/surface handles, resolve the owning workspace, toggle pinned state through the existing pane tab-action mutation path, persist state, and return CMUX-shaped tab action payloads. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture` and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar workspace action parity for corpus-backed dispatcher calls: `workspace.focus(param)` now aliases workspace selection, `workspace.pin`/`workspace.togglePin` toggle Limux pinned/favorite workspace order, `workspace.markRead` clears workspace notifications, `workspace.markAllRead` clears all unread notification state, and workspace/surface ids accept CMUX `param`/`value` aliases where applicable. Verified by `cargo test -p limux-host-linux custom_sidebar -- --nocapture` and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar dispatch for all single-target `workspace.reorder` forms: `index`, `before_workspace_id`, and `after_workspace_id`, including exactly-one-target validation, CMUX index resolution/clamping, live workspace order mutation, retained `workspace.reordered` event publication, and CMUX-shaped `plan`/`events` response metadata. Verified by focused host custom-sidebar tests and host clippy.
- 2026-07-02: Added CMUX-compatible parameter parsing for JSON custom-sidebar dispatcher actions and wired documented `workspace.select(workspace_id)` plus `surface.focus(surface_id)` actions through existing live host selection/focus helpers. Verified by focused host custom-sidebar tests.
- 2026-07-02: Added runtime enforcement for CMUX `customSidebars.beta.enabled`: live custom-sidebar validate/reload reports now expose the disabled state and hide sidebars when disabled, while select/open reject loudly with the beta-disabled reason. Verified by focused host custom-sidebar tests and host clippy.
- 2026-07-02: Added CMUX-compatible static styling fields for JSON custom sidebars: the GTK renderer now parses and applies `spacing`, `alignment`, `padding`, `font`, `weight`, `color`, `background`, and `size`, including CMUX font-size/weight/color token mappings where possible, spacer sizing, and loud rejection of malformed numeric style fields. Verified by focused host custom-sidebar tests and host clippy.
- 2026-07-02: Added CMUX-compatible no-parameter dispatcher actions for JSON custom-sidebar buttons: non-`log`/`openURL` JSON action types now route supported CMUX no-param methods through existing host helpers for `workspace.next`, `workspace.previous`, `workspace.last`, and `notification.jump_to_unread`; parameterized methods remain explicit unsupported paths. Verified by focused host custom-sidebar tests and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar pane opening: `sidebar.custom.open` now validates the runtime sidebar, opens or focuses a `customSidebar` pane tab with CMUX-shaped workspace/surface/tab refs, prefers a right split for focused opens, persists/restores the custom-sidebar tab kind by name, and keeps Swift interpreter sidebars on the explicit unsupported path. Verified by focused host custom-sidebar, route, persistence, and clippy checks.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar dispatcher close actions: `workspace.close(workspace_id)` and `surface.close(surface_id)` now parse strict required ids, route through existing host close helpers, publish close events, persist state, and expose correct dispatcher tooltips. Verified by focused host custom-sidebar tests and host clippy.
- 2026-07-02: Added CMUX-compatible JSON custom-sidebar runtime selection: `sidebar select` now routes to live `sidebar.custom.select`, validates `~/.config/cmux/sidebars`, renders static declarative JSON sidebars in the GTK right sidebar, and supports `log` plus `openURL` button actions while rejecting Swift runtime rendering loudly. Verified by focused CLI and host custom-sidebar tests.
- 2026-07-02: Added CMUX-compatible config parity for remaining Linux-meaningful terminal, custom-sidebar, and beta-feature settings: local `config get/set` now supports nested terminal performance/text-box/resume/memory keys, `customSidebars.renderer`, `customSidebars.beta.enabled`, `rightSidebar.beta.feed.enabled`, `rightSidebar.beta.dock.enabled`, `extensions.beta.enabled`, and `remoteTmux.beta.enabled` with strict type validation and host settings load/save round-tripping. Verified by focused CLI and host config tests.
- 2026-07-03: Added CMUX-compatible scalar App catalog settings parity from `/tmp/cmux-audit/Packages/macOS/CmuxSettings/Sources/CmuxSettings/Keys/AppCatalogSection.swift`: local `config get/set` now supports `app.windowTitleTemplate`, `app.menuBarOnly`, `app.preferredEditor`, `app.openSupportedFilesInCmux`, `app.openMarkdownInCmuxViewer`, `app.iMessageMode`, `app.reorderOnNotification`, `app.sendAnonymousTelemetry`, `app.warnBeforeQuit`, `app.warnBeforeClosingTab`, `app.warnBeforeClosingTabXButton`, `app.hideTabCloseButton`, `app.renameSelectsExistingName`, `app.commandPaletteSearchesAllSurfaces`, `app.workspaceButtonFade`, `app.workspaceTitlebarVisibility`, `app.systemWideHotkeyEnabled`, and `app.devWindowDisplay` with strict type/enum validation and host settings load/save round-tripping. Remaining App catalog keys that need separate enum/numeric/runtime policy work are `app.language`, `app.appIcon`, `app.minimalMode`, `app.globalFontMagnification`, `app.confirmQuit`, `app.fileDropDefaultBehavior`, and `app.titlebarControlsStyle`. Verified by `cargo test -p limux-cli config_app_scalar -- --nocapture`, `cargo test -p limux-host-linux app_scalar -- --nocapture`, and `cargo test -p limux-host-linux save_writes_gtk_and_ghostty_color_schemes -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `remote`/`remotes` registry parity: `list|ls`, `add <name> --route ... [--tag ...]`, and `remove|rm|delete <target>` now build `remotes.list`, `remotes.add`, and `remotes.remove` socket requests with CMUX route validation and sanitized text rendering, while the live bridge persists a strict local non-secret registry for those methods. Verified by focused CLI/host tests and `./scripts/check.sh`.
- 2026-07-02: Completed CMUX tab-action alias parity by adding the top-level `detach-tab` alias for `move-tab-to-new-workspace`, preserving strict rejection of caller-provided `--action`, exposing no-socket command help, and expanding live `tab.action` alias normalization coverage for canonical and CMUX spelling variants. Verified by `cargo test -p limux-cli detach_tab_alias -- --nocapture` and `cargo test -p limux-host-linux tab_action_aliases_route_to_live_command -- --nocapture`.
- 2026-07-02: Added CMUX-compatible clickable GTK sidebar PR/port row parity: workspace sidebar rows now render reported PRs and listening ports as action buttons, respect `sidebar.makePullRequestsClickable`, `sidebar.openPullRequestLinksInCmuxBrowser`, and `sidebar.openPortLinksInCmuxBrowser`, open in a Limux browser split when configured, otherwise launch the system URI handler, and refresh from the same in-memory metadata used by `sidebar-state` without polling or disk writes. Verified by focused host tests, host clippy, and `./scripts/check.sh`.
- 2026-07-02: Added CMUX-compatible shell cwd/state report parity: `surface.report_pwd` and `surface.report_shell_state` plus legacy aliases now update the addressed terminal cwd from normalized `file://` paths and record strict `prompt|running` state in pruned in-memory per-surface metadata, with CLI `report_pwd`/`report_shell_state` support for workspace/surface/panel flags plus env fallbacks. Verified by focused host/CLI tests and `./scripts/check.sh`.
- 2026-07-02: Added CMUX-compatible shell port metadata parity: `surface.report_tty`, `surface.report_ports`, and `surface.clear_ports` plus legacy CLI/socket aliases now record per-surface TTY and manual port reports in pruned in-memory host state, `sidebar-state` merges those reported ports with procfs-discovered rows without duplicates, and CLI `report_tty`/`report_ports`/`clear_ports` supports workspace/surface/panel flags plus Limux/CMUX env fallbacks. Verified by focused host/CLI tests and `./scripts/check.sh`.
- 2026-07-02: Added CMUX-compatible `surface.ports_kick` parity in the live GTK host and CLI: the bridge advertises/routes `surface.ports_kick`, `surface.ports-kick`, `ports_kick`, and `ports-kick`, validates workspace plus surface/panel refs, returns focused/selected surface metadata and current procfs-discovered port rows, and CLI `ports-kick` maps workspace/surface/panel/reason flags plus environment fallbacks. Verified by focused host/CLI tests and `./scripts/check.sh`.
- 2026-07-02: Added CMUX-compatible right-sidebar metadata visibility settings parity for `sidebar.showCustomMetadata`, `sidebar.showProgress`, and `sidebar.showLog`: local `config get/set` supports all three keys with CMUX true defaults, host settings load/save and default settings generation preserve them, invalid booleans fail loudly, and the right-sidebar Status, Progress, and Log sections follow the configured visibility flags. Verified by focused CLI and host tests, touched-crate clippy, and `./scripts/check.sh`.
- 2026-07-02: Added CMUX-compatible sidebar detail settings parity for `sidebar.hideAllDetails`, `sidebar.wrapWorkspaceTitles`, and `sidebar.showWorkspaceDescription`: local `config get/set` supports all three keys with CMUX defaults, host settings load/save and default settings generation preserve them, invalid booleans fail loudly, new and already-open workspace rows resync on config reload, title wrapping follows `wrapWorkspaceTitles`, workspace descriptions render according to `showWorkspaceDescription`, and `hideAllDetails` suppresses description/path/notification detail rows while preserving unread dots. Verified by focused CLI and host tests, touched-crate clippy, and `./scripts/check.sh`.
- 2026-07-02: Added CMUX-compatible sidebar display settings parity for `sidebar.showNotificationMessage` and `sidebar.showBranchDirectory`: local `config get/set` supports both keys with CMUX true defaults, host settings load/save and default settings generation preserve them, invalid booleans fail loudly, unread sidebar notification text follows `showNotificationMessage`, and workspace directory rows follow `showBranchDirectory` for new/restored workspaces. Verified by focused CLI and host tests, touched-crate clippy, and `./scripts/check.sh`.
- 2026-07-02: Added CMUX-compatible `notifications.command` and `notifications.hooksMode` parity in `limux-cli` and the live GTK host: local `config get/set` supports both keys with CMUX defaults, settings load/save and default settings generation preserve the nested values, invalid `hooksMode` values fail loudly, hook policy payloads/effects include `command`, and delivered notifications spawn the configured shell command with `CMUX_NOTIFICATION_*` and `LIMUX_NOTIFICATION_*` environment variables while reaping the child process. Verified by `cargo test -p limux-cli config_notification_settings -- --nocapture`, `cargo test -p limux-host-linux notification_preferences -- --nocapture`, `cargo test -p limux-host-linux load_from_path_rejects_invalid_notification_hooks_mode -- --nocapture`, `cargo test -p limux-host-linux notification_policy -- --nocapture`, `cargo test -p limux-host-linux notification_command_env_uses_cmux_and_limux_names -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, and `git diff --check`.

- 2026-07-02: Added CMUX-compatible notification visual settings parity in `limux-cli` and the live GTK host: local `config get/set` supports `notifications.dockBadge`, `notifications.showInMenuBar`, `notifications.unreadPaneRing`, and `notifications.paneFlash` with CMUX `true` defaults; settings load/save and default settings generation preserve the nested values; invalid `unreadPaneRing` values fail loudly; unread workspace visuals follow `unreadPaneRing`; and notification hook payloads/effects use the configured `paneFlash` value. Verified by `cargo test -p limux-cli config_notification_settings -- --nocapture`, `cargo test -p limux-host-linux notification_preferences -- --nocapture`, `cargo test -p limux-host-linux load_from_path_rejects_invalid_unread_pane_ring -- --nocapture`, `cargo test -p limux-host-linux notification_policy -- --nocapture`, `cargo test -p limux-host-linux unread_visual_gate_respects_notification_setting -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, and `git diff --check`.

- 2026-07-02: Added live CMUX advanced tab-action parity: `tab.action` now accepts CMUX aliases for `mark-read`, `mark-unread`, `reload`, `duplicate`, `close-left`, `close-right`, and `close-others`, routes browser-only duplicate/reload through real browser surfaces, applies relative close actions while skipping pinned tabs, and marks Limux workspaces read/unread for tab read-state actions. Verified by `cargo test -p limux-host-linux tab_action_aliases_route_to_live_command -- --nocapture` and `cargo test -p limux-host-linux tab_action_payload_includes_created_closed_and_unread_metadata -- --nocapture`.
- 2026-07-02: Added live CMUX tab metadata action parity: `tab.action` now routes `rename`, `clear-name`, `pin`, and `unpin` through the GTK host for addressed tabs, CLI `tab-action` serializes `--surface`, `--window`, and `--focus`, and `rename-tab` accepts surface aliases. Verified by `cargo test -p limux-cli cmux_tab_action_accepts_surface_window_and_focus_aliases -- --nocapture` and `cargo test -p limux-host-linux tab_action_metadata_routes_and_validates_live_command -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `move-tab-to-new-workspace` parity in `limux-cli`, `limux-core`, and the live GTK host: top-level `move-tab-to-new-workspace` wraps `tab.action move-to-new-workspace`, defaults `--focus` to false, accepts tab/surface/workspace/window/title flags, standalone control state moves the requested surface into a newly created workspace, and the live bridge routes the CMUX action through the existing tab-to-workspace host path with tab/surface alias normalization. Verified by `cargo test -p limux-core dispatcher_handles_surface_and_tab_actions_with_workspace_resolution -- --nocapture` and `cargo test -p limux-host-linux tab_action_move_to_new_workspace_routes_to_live_command -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `notifications.suppressOnlyFocusedSurface` parity in `limux-cli` and the live GTK host: local config get/set supports the CMUX boolean key with default false, settings load/save and default settings generation preserve the nested notification value, invalid values fail loudly, and desktop notification suppression switches between workspace-level and exact focused-surface policy. Verified by `cargo test -p limux-cli config_notification_settings -- --nocapture`, `cargo test -p limux-host-linux notification_preferences -- --nocapture`, `cargo test -p limux-host-linux load_from_path_rejects_invalid_focused_surface_suppression -- --nocapture`, `cargo test -p limux-host-linux ensure_default_config_file_writes_dark_appearance_and_notification_defaults -- --nocapture`, and `cargo test -p limux-host-linux desktop_notifications_ -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `terminal.autoResumeAgentSessions` parity in `limux-cli` and the live GTK host: local config get/set supports the CMUX boolean key with default true, settings load/save preserves the nested `terminal` value, invalid values fail loudly, and restored hook-managed agent panes keep metadata while suppressing automatic resume commands when disabled. Verified by `cargo test -p limux-cli config_terminal_auto_resume_agent_sessions -- --nocapture`, `cargo test -p limux-host-linux terminal_auto_resume_agent_sessions -- --nocapture`, and `cargo test -p limux-host-linux restorable_agent_auto_resume -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `app.focusPaneOnFirstClick` parity in `limux-cli` and the live GTK host: local config get/set supports the CMUX boolean key with the runtime false default from `PaneFirstClickFocusSettings.defaultEnabled`, settings load/save preserves the nested `app` value, invalid values fail loudly, and terminal click-to-focus now follows the setting. Verified by `cargo test -p limux-cli config_app_focus_pane_on_first_click -- --nocapture`, `cargo test -p limux-host-linux focus_pane_on_first_click -- --nocapture`, and `cargo test -p limux-host-linux terminal_click_focus -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `app.workspaceInheritWorkingDirectory` parity in `limux-cli` and the live GTK host: local config get/set supports the CMUX boolean key with the true default, settings load/save preserves the nested `app` value, invalid values fail loudly, explicit workspace cwd remains authoritative, enabled hosts inherit the active workspace folder/cwd for new workspaces, and disabled hosts leave new workspace cwd unset. Verified by `cargo test -p limux-cli config_app_workspace_inherit -- --nocapture`, `cargo test -p limux-host-linux workspace_inherit -- --nocapture`, `cargo test -p limux-host-linux workspace_creation_directory -- --nocapture`, `cargo test -p limux-host-linux workspace_title_from_directory -- --nocapture`, `cargo test -p limux-host-linux save_writes_gtk_and_ghostty_color_schemes -- --nocapture`, and a direct temporary `XDG_CONFIG_HOME` CLI smoke.
- 2026-07-02: Added CMUX-compatible `app.keepWorkspaceOpenWhenClosingLastSurface` parity in `limux-cli` and the live GTK host: local config get/set supports the CMUX boolean key, settings load/save preserves the nested `app` value, invalid values fail loudly, and enabled hosts preserve the workspace after closing the final surface by creating a fresh terminal surface. Verified by `cargo test -p limux-cli config_app_keep_workspace_open -- --nocapture`, `cargo test -p limux-host-linux keep_workspace_open -- --nocapture`, `cargo test -p limux-host-linux save_writes_gtk_and_ghostty_color_schemes -- --nocapture`, and a direct temporary `XDG_CONFIG_HOME` CLI smoke.
- 2026-07-02: Added CMUX-compatible `app.appearance` config parity: local `config get/set app.appearance` now reads the CMUX `system` default, writes nested settings JSON while preserving unrelated app and notification keys, rejects invalid values loudly, and the GTK host now prefers `app.appearance` for the app color scheme while keeping legacy Limux `appearance.color_scheme` in sync on save. Verified by `cargo test -p limux-cli config_app_appearance -- --nocapture`, `cargo test -p limux-host-linux app_appearance -- --nocapture`, `cargo test -p limux-host-linux save_writes_gtk_and_ghostty_color_schemes -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`.

- 2026-07-02: Added CMUX-compatible local `config get/set` and host runtime parity for notification sound settings: `notifications.sound` now accepts CMUX documented sound presets plus existing Linux aliases without silent fallback, `notifications.customSoundFilePath` is preserved, `custom_file` emits a Linux `sound-file` notification hint when configured, and invalid sound values fail loudly. Verified by `cargo test -p limux-cli config_notification_settings -- --nocapture`, `cargo test -p limux-host-linux notification_sound -- --nocapture`, and `cargo test -p limux-host-linux desktop_notification_hints_include_cmux_sound_and_custom_file -- --nocapture`.

- 2026-07-02: Added CMUX-compatible local `config get/set` parity for workspace placement settings: `app.newWorkspacePlacement` and `workspaceGroups.newWorkspacePlacement` now read the CMUX `afterCurrent` default, write nested settings JSON while preserving unrelated app, notification, and `workspaceGroups.byCwd` data, and reject invalid placement values loudly. Verified by `cargo test -p limux-cli config_workspace_placement -- --nocapture`, `cargo test -p limux-cli config_notification_settings -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.

- 2026-07-02: Added CMUX-compatible local `config get/set` parity for agent notification settings: `notifications.agentPermissionPrompt`, `notifications.agentTurnComplete`, and `notifications.agentIdleReminder` now read CMUX defaults when unset, write nested `notifications` JSON while preserving unrelated settings, and reject invalid boolean/turn-complete values loudly. Verified by `cargo test -p limux-cli config_notification_settings -- --nocapture` and `cargo test -p limux-cli config_font_size -- --nocapture`.

- 2026-07-02: Added CMUX-compatible agent notification gating parity from `128be4c1ac`: Limux now loads/saves `notifications.agentPermissionPrompt`, `notifications.agentTurnComplete`, and `notifications.agentIdleReminder`, classifies Claude hook notifications as turn-complete/needs-permission/idle-reminder, tracks pending Claude background work from `session_crons` and running `background_tasks`, accepts `agent_category`/`agentCategory` plus `agent_pending`/`agentPending` over `notification.create`, and suppresses or delivers agent notifications with the CMUX per-category decision table. Verified by `cargo test -p limux-cli claude_notification_gate -- --nocapture`, `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-host-linux agent_notification -- --nocapture`, `cargo test -p limux-host-linux notification_preferences -- --nocapture`, and `cargo test -p limux-host-linux notification_list_and_clear_routes_validate_params -- --nocapture`.

- 2026-07-02: Added CMUX-compatible OpenCode fork-support version probing for `sessions list`: local OpenCode records now keep `fork_command_available` separate from `fork_supported`, accept versions at or above `1.14.50`, reject missing absolute executables, bypass local probing for remote-like working directories, sanitize the probe environment, and cache probe results during a command. Verified by `cargo test -p limux-cli sessions -- --nocapture`.
- 2026-07-02: Added CMUX-compatible Claude workflow-container transcript resolution for `sessions list`: when a Claude hook record points `transcriptPath` at a workflow container directory, Limux now resolves the session to the single sibling transcript under the matching Claude project root and uses that resolved session id/path for fork diagnostics. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and SCPlus lint on `rust/limux-cli/src/sessions.rs`.
- 2026-07-02: Refreshed the CMUX audit target to `5eef9ac77dfebeeff1b8cd73f9328e8caea1879d` and added CMUX session-list trust diagnostics in `limux-cli`: native CMUX camelCase hook stores now deserialize, out-of-range saved PID values no longer break diagnostics, Claude `transcriptPath` and `CLAUDE_CONFIG_DIR/projects/<encoded-cwd>/<session>.jsonl` evidence gates `hook_record_restorable`, and JSON rows expose `fork_unavailable_reason`, `fork_startup_input_available`, and `stale_pid_blocks_restore_in_0_64_17`. Verified by `cargo test -p limux-cli sessions -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible session fork-command diagnostics in `limux-cli`: `sessions list/debug --json` now includes concrete `fork_command` text plus accurate `fork_command_available`/`fork_supported` booleans for agents with native fork forms, and text output now reports command availability from the rendered command. The builder preserves safe launch flags and allowlisted environment for Codex, Claude, OpenCode, Pi, and OMP while stripping credentials. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-cli fork_command -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added the public Codex Teams launcher in `limux-cli`: `limux codex-teams` now requires a Limux terminal surface context, validates Codex working-directory flags, starts a private `codex app-server --listen ws://127.0.0.1:<port>`, waits for websocket initialization, launches the root Codex process with CMUX-positioned `--remote`, starts the hidden `__codex-teams-watch` bridge, passes socket auth to the watcher, writes private helper logs, and terminates the app-server/watcher process groups on root exit or startup failure so wrapper-spawned app-server descendants do not leak. Verified by `cargo test -p limux-cli codex_teams -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, a bounded `LIMUX_WORKSPACE_ID=ws-smoke LIMUX_SURFACE_ID=surface-smoke ./target/debug/limux-cli --socket /tmp/limux-missing-codex-teams.sock codex-teams --version` smoke with no remaining `codex app-server --listen ws://127.0.0.1` process, and `./scripts/check.sh`. Remaining: fully interactive root/subagent smoke inside a live Limux GUI session.
- 2026-07-02: Added the hidden Codex Teams watcher stream in `limux-cli`: `__codex-teams-watch` now validates required watcher args, connects to the Codex app-server, initializes with Codex Teams notification opt-outs, backfills `thread/loaded/list`, subscribes with `thread/resume { excludeTurns: true }`, forwards app-server approval requests through `feed.push`, maps resolved Feed permission decisions back to Codex JSON-RPC responses, caches bounded related approval items, tracks subagent thread depth/open state, opens managed subagent panes through `surface.split`, and applies best-effort tab rename/equalize calls. Verified by `cargo test -p limux-cli codex_teams -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and `./scripts/check.sh`.
- 2026-07-02: Added the Codex Teams app-server websocket JSON-RPC client foundation in `limux-cli`: `rust/limux-cli/src/codex_teams.rs` now validates app-server websocket URLs, opens Codex app-server websockets with `tokio-tungstenite`, performs the experimental `initialize`/`initialized` handshake, handles interleaved notifications while waiting for request ids, returns app-server errors loudly, sends JSON-RPC success/error responses back to Codex, and handles ping/pong and invalid frame failures. Verified by `cargo test -p limux-cli codex_app_server_connection -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and a local websocket protocol test. Remaining: production process launch/cleanup, watcher loop, approval forwarding through Feed, and live subagent split orchestration.
- 2026-07-02: Added the Codex Teams watcher model and managed subagent split-plan foundation in `limux-cli`: `rust/limux-cli/src/codex_teams.rs` now parses Codex app-server thread/subagent-spawn metadata, rejects not-loaded thread status for attachment, builds CMUX-positioned root Codex `--remote` args, shell-quotes managed subagent `codex resume --remote` commands, writes one-shot startup scripts, emits CMUX Teams startup environment metadata, and constructs `surface.split` params for chained managed subagent panes. Verified by `cargo test -p limux-cli codex_teams -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`. Remaining: production websocket loop, process launch/cleanup, approval request forwarding, and live subagent open orchestration.
- 2026-07-02: Added the pure Rust CMUX Codex Teams app-server approval bridge foundation in `limux-cli`: `rust/limux-cli/src/codex_teams.rs` now builds CMUX-shaped Feed permission events from Codex app-server approval requests, extracts resolved Feed permission modes, maps Feed decisions back to `item/commandExecution/requestApproval`, `item/fileChange/requestApproval`, and `item/permissions/requestApproval` response payloads, bounds approval item snapshots, and resolves/validates Codex `-C`/`--cd`/`--cwd` arguments. Verified by `cargo test -p limux-cli codex_teams -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX Codex watcher startup-field parity in `limux-host-linux`: `pane.create` and `surface.split` now parse `initial_command`, `working_directory`, and strictly validated `startup_environment`, and live terminal creation uses those fields at launch time for managed panes instead of post-create text injection. Verified by `cargo test -p limux-host-linux pane_create_contract_accepts_cmux_startup_fields -- --nocapture`, `cargo test -p limux-host-linux pane_create_contract_rejects_invalid_startup_fields -- --nocapture`, and `cargo test -p limux-host-linux surface_split_route_queues_cmux_startup_fields -- --nocapture`.
- 2026-07-02: Added CMUX-compatible Codex app-server permission action capability policy in `limux-host-linux`: Feed right-sidebar buttons and native notification actions now filter Codex app-server approval modes from app-server `available_decisions`, `app_server_method`, and amendment payloads, while non-app-server Feed rows keep the existing action set. Verified by `cargo test -p limux-host-linux feed_actions -- --nocapture`, `cargo test -p limux-host-linux push_notifies_codex_app_server_permission_with_supported_actions -- --nocapture`, and `cargo test -p limux-host-linux right_sidebar_codex_app_server_permission_actions_follow_capabilities -- --nocapture`.
- 2026-07-02: Added explicit CMUX `codex-teams` CLI surface handling in `limux-cli`: `codex-teams --help` now renders the documented local usage text, and non-help execution routes to the socket-backed public launcher instead of the earlier `not_supported` stub. Verified by `cargo test -p limux-cli codex_teams -- --nocapture`, `cargo test -p limux-cli cmux_command_help -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and direct `limux-cli codex-teams` smoke commands.
- 2026-07-02: Added Linux inline Feed notification action parity in `limux-host-linux`: pending Feed permission, exit-plan, and question notifications now attach freedesktop action buttons and route selected actions through the existing shared Feed reply helpers. Verified by `cargo test -p limux-host-linux feed -- --nocapture`, `cargo test -p limux-host-linux notification -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible native Feed notification parity in `limux-host-linux`: pending actionable Feed permission, exit-plan, and question rows now dispatch host notifications through the existing workspace/surface-targeted notification pipeline before any blocking wait starts, while telemetry rows remain notification-free. Verified by `cargo test -p limux-host-linux feed -- --nocapture` and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible pid-anchored Codex transcript diagnostics in `limux-cli`: `sessions list/debug` now inspect live `/proc/<pid>/fd` targets for JSONL transcripts under the configured Codex home, accept only `sessions` or `archived_sessions` paths with UUID-shaped names, surface `codex_native_session_id`, `codex_pid_transcript_found`, and `codex_pid_transcript_path`, and fill transcript diagnostics from the live fd path when the saved wrapper session id is not indexed. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-cli codex_transcript -- --nocapture`, SCPlus lint on `rust/limux-cli/src/sessions.rs`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Codex wrapper-owned session-start parity in `limux-host-linux` and `limux-cli`: every terminal tab exports `LIMUX_CLI`/`CMUX_CLI`, the per-surface `codex` PATH shim emits `limux hooks codex session-start` with wrapper session id, surface, workspace, cwd, pid, and launch metadata before execing real Codex, and hook persistence accepts wrapper-provided session id and pid env metadata while still preferring real native session/transcript payloads when present. Verified by `cargo test -p limux-host-linux codex_wrapper -- --nocapture`, `cargo test -p limux-cli hook_session_id -- --nocapture`, `cargo test -p limux-cli hook_agent_pid -- --nocapture`, `cargo test -p limux-host-linux limux_cli_candidates -- --nocapture`, and `cargo clippy -p limux-host-linux -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Codex per-surface wrapper shim parity in `limux-host-linux`: every terminal tab now gets a private `codex` shim directory prepended to `PATH`, exports `CMUX_CODEX_WRAPPER_SHIM`/`LIMUX_CODEX_WRAPPER_SHIM` and root aliases, and preserves Codex launch metadata for real Codex hook events without inventing a fake native session id. Verified by `cargo test -p limux-host-linux codex_wrapper -- --nocapture`, `cargo test -p limux-cli codex -- --nocapture`, and `cargo clippy -p limux-host-linux -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Pi hook parity in `limux-cli`: `AgentKind::Pi` aliases, session-store coverage, native `pi --session <id>` restore command construction, `limux hooks pi install/uninstall`, protected `~/.pi/agent/extensions/cmux-session.ts` or `$PI_CODING_AGENT_DIR/extensions/cmux-session.ts` writes, generated TypeScript extension support for `session_start`, `before_agent_start`, `tool_execution_start`, `tool_execution_end`, `agent_end`, and `session_shutdown`, CMUX/Limux base64 launch capture, Pi Feed forwarding for `PreToolUse` and `PostToolUse`, and resume-binding set/clear calls. Verified by `cargo test -p limux-cli pi -- --nocapture`, `cargo test -p limux-cli feed_hook -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Amp hook parity in `limux-cli`: `AgentKind::Amp` aliases, session-store coverage, native `amp threads continue <id>` restore command construction, `limux hooks amp install/uninstall`, protected `~/.config/amp/plugins/cmux-session.ts` or `$AMP_CONFIG_DIR/plugins/cmux-session.ts` writes, generated TypeScript plugin support for `session.start`, `agent.start`, `tool.call`, `tool.result`, and `agent.end`, CMUX/Limux base64 launch capture with `AMP_API_KEY` stripped from child hook/status/log processes, and live `set-status`/`clear-status`/`log` bridge calls. Verified by `cargo test -p limux-cli amp -- --nocapture`, an isolated `AMP_CONFIG_DIR` install/uninstall smoke, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Hermes Agent hook parity in `limux-cli`: `AgentKind::HermesAgent` aliases, session-store coverage, native `hermes --resume <id>` restore command construction, `limux hooks hermes-agent install/uninstall`, protected marked YAML hook blocks in `$HERMES_HOME/config.yaml` or `~/.hermes/config.yaml`, `shell-hooks-allowlist.json` approval install/uninstall preserving unrelated approvals, lifecycle events `on_session_start`, `pre_llm_call`, `post_llm_call`, `pre_approval_request`, `post_approval_response`, `on_session_end`, `on_session_finalize`, and `on_session_reset`, Feed events `pre_tool_call`, `post_tool_call`, `pre_approval_request`, and `post_approval_response`, and nonblocking Hermes tool telemetry classification. Verified by `cargo test -p limux-cli hermes -- --nocapture`, `cargo test -p limux-cli feed_hook -- --nocapture`, an isolated `HERMES_HOME` install/uninstall smoke, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Kiro Feed deny-status parity in `limux-cli`: generated Kiro hook entries preserve Feed denial status instead of masking it with the generic `|| echo '{}'` fallback, resolved Kiro permission `deny` decisions exit with status 2, `once`/`always`/`all`/`bypass` allow decisions print `{}` and exit 0, and malformed Kiro permission modes fail closed with status 2. Verified by `cargo test -p limux-cli kiro -- --nocapture`.
- 2026-07-02: Added CMUX-compatible workspace-create title flag parity in `limux-cli`: `new-workspace --name <title>`, `new-workspace --title <title>`, and `workspace create --name/--title` now serialize the title into `workspace.create` params while preserving cwd, command, and workspace environment handling. Verified by `cargo test -p limux-cli new_workspace_params -- --nocapture` and `cargo test -p limux-cli workspace_env -- --nocapture`.
- 2026-07-02: Added CMUX-compatible project workspace-env loading in `limux-cli`: `new-workspace` and `workspace create` now search upward from the current directory for `.cmux/cmux.json` before `cmux.json`, stop at `$HOME`, read the `workspace.env` definition or the configured `newWorkspaceCommand` workspace env, reject protected `CMUX_*`/`LIMUX_*` keys, and keep explicit `--env-file`/`--env` values higher precedence. Verified by `cargo test -p limux-cli project_workspace_env -- --nocapture`, `cargo test -p limux-cli project_cmux_config -- --nocapture`, `cargo test -p limux-cli workspace_env -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible `config doctor --path` and project-target parity in `limux-cli`: doctor accepts repeated `--path` / `--path=<path>` custom targets, reports CMUX-style OK/MISSING/ERROR text with bytes and keys, and default doctor targets now include the nearest project `.cmux/cmux.json`/`cmux.json` when present. Verified by `cargo test -p limux-cli config_ -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible JSONC preprocessing for `config doctor` in `limux-cli`: doctor now accepts line comments, block comments, and trailing commas while preserving comment-like text inside strings, then validates the sanitized top-level object and reports `JSONC syntax is valid`. Verified by `cargo test -p limux-cli config_doctor -- --nocapture`, `cargo test -p limux-cli jsonc -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace-create focus parity in `limux-cli` and the live GTK bridge: `new-workspace --focus <true|false>` and `workspace create --focus <true|false>` serialize CMUX boolean aliases, invalid CLI focus values fail loudly, live `workspace.create` accepts boolean `focus`, defaults to background creation, and only activates the new workspace when `focus` is true. Verified by `cargo test -p limux-cli new_workspace_params -- --nocapture`, `cargo test -p limux-host-linux workspace_create_route_accepts_focus_bool -- --nocapture`, and `cargo test -p limux-host-linux workspace_env_and_create_routes_validate_environment -- --nocapture`.
- 2026-07-02: Added CMUX/tmux lifecycle alias parity in `limux-cli` for `neww`, `select-window`/`selectw`, `select-pane`/`selectp`, `kill-window`/`killw`, `kill-pane`/`killp`, and `renamew`, including no-socket help probes, `-t`/`--target` parsing, pane focus routing, surface close routing, workspace select/close routing, and rename title parsing that skips target values. Verified by `cargo test -p limux-cli tmux -- --nocapture` and `cargo test -p limux-cli cmux_ -- --nocapture`.
- 2026-07-02: Added CMUX/tmux detached split focus parity in `limux-cli` and the live GTK bridge: `split-window -d` now serializes `focus:false`, `surface.split` parses and preserves the focus flag, and launch-created terminal tabs honor that activation setting. Verified by `cargo test -p limux-cli tmux -- --nocapture` and `cargo test -p limux-host-linux surface_split -- --nocapture`.
- 2026-07-02: Added CMUX/tmux split sizing request parity in `limux-cli` and the live GTK bridge: `split-window -l <cells>` now serializes a bounded `initial_divider_position`, `surface.split` parses and preserves the divider field, and host pane splitting applies it as the initial split ratio. Verified by `cargo test -p limux-cli tmux -- --nocapture` and `cargo test -p limux-host-linux surface_split -- --nocapture`.
- 2026-07-02: Added CMUX/tmux `split-window -P -F` print-format parity in `limux-cli`: formatted split output now renders pane, surface, and workspace fields from the created split payload, falls back to the new pane handle, and treats `--format` as a value flag instead of command text. Verified by `cargo test -p limux-cli tmux_split_window -- --nocapture`, `cargo test -p limux-cli tmux -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX/tmux terminal geometry exposure foundation: Ghostty cell pixel dimensions now flow through terminal health, `surface-health` and `pane.list` expose active terminal rows/columns/pixel/cell metrics, and CLI tmux row-format rendering consumes real `pane_width`, `pane_height`, `pane_cell_width_px`, and `pane_cell_height_px` fields when available. Verified by `cargo test -p limux-cli tmux_list_rows_render_format_strings -- --nocapture` and `cargo check -p limux-host-linux`.
- 2026-07-02: Added exact CMUX/tmux `split-window -l` live sizing in `limux-cli`: explicit `-l` sizing now resolves workspace/pane context, reads the target pane's real rows/columns from `pane.list`, computes the divider with CMUX's current-cells clamp formula, and fails loudly if geometry cannot be resolved instead of using the old 100-cell approximation. Verified by `cargo test -p limux-cli tmux_split_window -- --nocapture`, `cargo test -p limux-cli tmux -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added additional CMUX/tmux local command parity in `limux-cli`: `pipe-pane` accepts trailing shell command text as well as `--command`, `wait-for` now uses CMUX's 30-second default with fractional non-negative timeout parsing, and `find-window` emits CMUX-shaped multi-match/no-match text with case-insensitive title matching plus `--content` and `--select` routing. Verified by `cargo test -p limux-cli tmux_pipe_pane -- --nocapture`, `cargo test -p limux-cli tmux_wait_for -- --nocapture`, `cargo test -p limux-cli tmux_find_window -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Ghostty theme catalog parity in `limux-cli`: `themes list` discovers Ghostty theme files from Linux resource, XDG data, repo, and user theme directories, text/JSON output reports CMUX-style theme rows with current light/dark badges, and `themes set` canonicalizes or rejects names against the discovered catalog while keeping arbitrary-name behavior when no catalog is available. Verified by `cargo test -p limux-cli theme -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible top-level `tree` and partial `top` routing in `limux-cli`: `tree` now assembles a window/workspace/pane/surface topology from existing list APIs and renders text/JSON output, while `top` uses Limux `system.memory` diagnostics and explicitly rejects scoped/sorted/TSV CMUX `system.top` flags instead of silently faking them. Verified by `cargo test -p limux-cli cmux_tree -- --nocapture`, `cargo test -p limux-cli cmux_top -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added native live-bridge topology parity: the GTK bridge now advertises and routes `window.list` and `system.tree`, and top-level `tree` calls `system.tree` instead of reconstructing topology through multiple client-side list calls. Verified by `cargo test -p limux-host-linux system_tree -- --nocapture`, `cargo test -p limux-cli cmux_tree -- --nocapture`, and `cargo clippy -p limux-host-linux -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added additional local CMUX `top` parity in `limux-cli`: `top --sort mem|proc` reorders local process groups, and `top --flat` / `top --format tsv` render CMUX-shaped TSV rows from Limux's current process-group diagnostics while scoped top and CPU sorting remain loud unsupported paths. Verified by `cargo test -p limux-cli cmux_top -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added native live-bridge `system.top` parity for Limux process diagnostics: the bridge advertises/routes `system.top`, top-level `top` calls it directly, `--workspace` filters process groups using Limux child-process attribution, `--window window:1` validates the current GTK host window, and `--all` requests the unfiltered local process view. Verified by `cargo test -p limux-host-linux system_top -- --nocapture`, `cargo test -p limux-host-linux memory_diagnostics -- --nocapture`, and `cargo test -p limux-cli cmux_top -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `top --sort cpu` local diagnostics: the host now extracts cumulative user+system CPU ticks from `/proc/<pid>/stat`, aggregates ticks per direct-child process group, includes `cpu_ticks` in process diagnostics, and TSV top output reports the measured tick value instead of a placeholder. Verified by `cargo test -p limux-host-linux memory_diagnostics -- --nocapture`, `cargo test -p limux-cli cmux_top -- --nocapture`, and `cargo clippy -p limux-host-linux -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible sampled CPU-percent `top` parity: live `system.top` accepts bounded `sample_ms`, samples `/proc/stat` plus descendant process ticks twice, emits `cpu_percent` for app and process-group diagnostics, and `top --sort cpu` / `top --flat` now use sampled percent when present. Verified by `cargo test -p limux-host-linux memory_diagnostics -- --nocapture`, `cargo test -p limux-host-linux system_top -- --nocapture`, `cargo test -p limux-cli cmux_top -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX docs dock topic parity in `limux-cli`: `docs` now lists `dock`, `docs dock` prints dock/right-sidebar custom-control guidance, and `doc`/`controls`/`right-sidebar`/`dock-json` aliases normalize like CMUX. Verified by `cargo test -p limux-cli cmux_docs -- --nocapture`.
- 2026-07-02: Added CMUX/tmux local split/layout parity in `limux-cli`: `split-window`/`splitw` now route to `surface.split` with tmux `-h`/`-v`/`-b` direction mapping, `-c` working directory, `-t` surface target, command injection, and `-P` surface-handle output, while `select-layout` routes to `workspace.equalize_splits` with `main-vertical`/`main-horizontal` orientation mapping. Verified by `cargo test -p limux-cli tmux -- --nocapture`, `cargo test -p limux-cli cmux_ -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace-create layout parity in `limux-cli` and the live GTK bridge: `new-workspace --layout <json>` and `workspace create --layout <json>` require JSON objects, serialize CMUX layout params, translate CMUX split/pane/surface layout topology into Limux `LayoutNodeState`, run terminal startup commands from layout surface entries, seed browser surface URLs, and skip top-level `--command` when a layout is present. Verified by `cargo test -p limux-cli new_workspace_params -- --nocapture`, `cargo test -p limux-host-linux workspace_create_route -- --nocapture`, `cargo test -p limux-host-linux terminal_tab_state_round_trips_restorable_agent_metadata -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace-create description and group-placement parity in `limux-cli` and the live GTK bridge: `new-workspace --description`, `--group`, `--group-placement top|end|afterCurrent`, and `--group-reference` serialize into `workspace.create`, live host creation validates group/reference targets before mutation, persists workspace descriptions, includes descriptions in workspace payloads/events, and places new grouped workspaces at top, end, or after the reference/current workspace. Verified by `cargo test -p limux-cli new_workspace_params -- --nocapture`, `cargo test -p limux-host-linux workspace_create_route -- --nocapture`, `cargo test -p limux-host-linux workspace_group_state_round_trips_through_session_json -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible `workspace.group.new_workspace` placement parity: live bridge rejects invalid placement values, host grouped workspace creation applies top/end/afterCurrent insertion order, and the default uses the group anchor as the CLI/header reference. Verified by `cargo test -p limux-host-linux workspace_group_mutation_routes_parse_cmux_params -- --nocapture`, `cargo test -p limux-host-linux workspace_group_insert_index_matches_cmux_placements -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace group placement config parity in the live host: `workspaceGroups.newWorkspacePlacement` and longest-match `workspaceGroups.byCwd[*].newWorkspacePlacement` are loaded from Limux settings JSON, invalid placement values fail loudly, supported byCwd fields are preserved on settings save, and `workspace.group.new_workspace` uses the configured default when no explicit placement is supplied. Verified by `cargo test -p limux-host-linux workspace_group_placement -- --nocapture`, `cargo test -p limux-host-linux save_to_path_writes_workspace_group_default_and_preserves_by_cwd -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Cmd-N folder workspace group placement parity in the live host: folder-dialog workspace creation now detects the active workspace group, resolves configured placement from the group anchor cwd, uses the active workspace as the `afterCurrent` reference, and leaves ungrouped active workspaces unchanged. Verified by `cargo test -p limux-host-linux workspace_group_insert_index_matches_cmux_placements -- --nocapture`, `cargo test -p limux-host-linux workspace_group_placement -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace group sidebar header rendering in the live GTK host: grouped sidebar order now emits group header rows, anchor workspaces are represented by their group header instead of a duplicate workspace row, collapsed groups hide child rows through the shared render plan, restored sessions rebuild group headers, and sidebar selection maps header rows back to their anchor workspace. Verified by `cargo test -p limux-host-linux workspace_sidebar_render_items -- --nocapture` and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace group header controls in the live GTK host: the header chevron dispatches collapse/expand through the existing `workspace.group.*` host action path, the header plus button dispatches `workspace.group.new_workspace` with host-configured placement resolution, and action failures surface as explicit runtime errors. Verified by `cargo test -p limux-host-linux workspace_group_header -- --nocapture` and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible grouped sidebar drag-order planning in the live GTK host: group headers now carry workspace drag payloads, header drops route through top-level workspace row planning, grouped drops keep group runs contiguous with anchor-first ordering, and grouped child rows promote out of the group when dropped into top-level space. Verified by `cargo test -p limux-host-linux workspace_reorder_uses_top_level_rows -- --nocapture`, `cargo test -p limux-host-linux workspace_top_level_ids -- --nocapture`, `cargo test -p limux-host-linux workspace_group -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace group delete confirmation UX in the live GTK host: right-clicking a group header exposes a destructive `Delete Group (Close Workspaces)` action, shows the CMUX-shaped member-count confirmation dialog, deletes only after explicit confirmation, and leaves socket/CLI `workspace.group.delete` unconditional. Verified by `cargo test -p limux-host-linux workspace_group_delete_confirmation -- --nocapture`, `cargo test -p limux-host-linux workspace_group -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible `app.newWorkspacePlacement` parity in the live host: settings JSON now loads/saves the app-level placement value, invalid placement values fail loudly, unrelated app settings are preserved on save, and normal Cmd-N/sidebar-button folder workspace creation honors top/end/afterCurrent insertion with CMUX pinned-prefix behavior. Verified by `cargo test -p limux-host-linux app_new_workspace_placement -- --nocapture`, `cargo test -p limux-host-linux save_to_path_writes_app_workspace_placement_and_preserves_app_keys -- --nocapture`, `cargo test -p limux-host-linux workspace_insert_index_matches_cmux_app_placements_with_pins -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.

- `/tmp/cmux-audit/docs/cli-contract.md`
- `/tmp/cmux-audit/docs/events.md`
- `/tmp/cmux-audit/docs/feed.md`
- `/tmp/cmux-audit/docs/notifications.md`
- `/tmp/cmux-audit/docs/workspace-groups.md`
- `/tmp/cmux-audit/docs/remote-daemon-spec.md`
- `/tmp/cmux-audit/daemon/remote/README.md`
- `/tmp/cmux-audit/skills/cmux-browser/references/commands.md`
