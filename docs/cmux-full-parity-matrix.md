# CMUX Full Parity Matrix

Source CMUX commit: `128be4c1ac6f7fc5ed1943e518ef5908b79340e5` in `/tmp/cmux-audit` `origin/main`.

Status values:

- Implemented: Limux has a verified equivalent in this branch.
- Partial: Limux has a usable subset, but named CMUX behavior remains missing.
- Missing: no equivalent found in Limux.
- Blocked: implementation needs external infrastructure or a platform facility not available in this local verification pass.
- Not supported by CMUX: upstream CMUX documents the API as unsupported.

| Area | CMUX commands/APIs | Limux status | Evidence and remaining gap |
| --- | --- | --- | --- |
| Notification lifecycle | `notify`, `list-notifications`, `dismiss-notification`, `mark-notification-read`, `open-notification`, `jump-to-unread`, `clear-notifications`; `notification.create/list/dismiss/mark_read/open/jump_to_unread/clear` | Implemented | Implemented live host store, CLI/API aliases, retained event publication for `notification.created`, `notification.read`, `notification.removed`, and `notification.cleared`, CMUX-style `created_at`, surface/pane refs, `tab_title` metadata for surface-targeted notifications, configured notification hooks, hook-controlled `record`, `markUnread`, `desktop`, and `sound` policy effects, CMUX agent notification category metadata (`turn-complete`, `needs-permission`, `idle-reminder`, `other`), pending background-work gating, and Claude hook classification for turn-complete/permission/idle notifications in `rust/limux-host-linux/src/app_config.rs`, `rust/limux-host-linux/src/control_bridge.rs`, `rust/limux-host-linux/src/window.rs`, `rust/limux-host-linux/src/event_bus.rs`, and `rust/limux-cli/src/main.rs`. Verified by `cargo test -p limux-host-linux notification -- --nocapture`, `cargo test -p limux-host-linux app_config -- --nocapture`, `cargo test -p limux-host-linux agent_notification -- --nocapture`, `cargo test -p limux-host-linux notification_preferences -- --nocapture`, `cargo test -p limux-host-linux notification_list_and_clear_routes_validate_params -- --nocapture`, `cargo test -p limux-cli claude_notification_gate -- --nocapture`, and `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`. |
| Browser open split | `browser open`, `browser open-split`, `browser new`; `browser.open_split` | Implemented | Live bridge accepts `pane.create type=browser` and `browser.open_split`, seeded through WebKit browser pane state and CLI browser open/new aliases. Verified by `cargo test -p limux-host-linux browser_open_split_route_queues_browser_pane -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, and `./scripts/check.sh`. |
| Browser navigation and automation | `goto/navigate`, `back/forward/reload`, `snapshot`, `eval`, `wait`, `click`, `fill`, `type`, `select`, `get`, `find`, `frame`, `dialog`, `download`, `cookies`, `storage`, `tab`, `console`, `errors`, `highlight`, `state` | Implemented | Live bridge now supports `browser.navigate`, `browser.url.get`, `browser.back`, `browser.forward`, `browser.reload`, DOM actions `browser.click/dblclick/fill/type/select/hover/focus/check/uncheck/press/keydown/keyup/scroll/scroll_into_view`, `browser.focus_webview`, `browser.is_webview_focused`, `browser.eval`, getters `browser.get.title/text/html/value/attr/count/box/styles`, DOM state checks `browser.is.visible/enabled/checked`, locator methods `browser.find.role/text/label/placeholder/alt/title/testid/first/last/nth`, persisted `@eN` element refs from snapshot/find through later actions and getters, `browser.frame.select/main` with persistent selected-frame evaluation context, `browser.dialog.accept/dismiss` for queued WebKit JavaScript dialogs, `browser.download.wait` for bounded local download path waits, real WebKit PNG capture through `browser.screenshot`, `browser.snapshot`, bounded-polling `browser.wait`, injection commands `browser.addscript/addinitscript/addstyle`, retained diagnostic buffers for `browser.console.list/clear` and `browser.errors.list/clear`, `browser.highlight`, `browser.cookies.get/set/clear`, `browser.storage.get/set/clear`, browser tab commands `browser.tab.list/new/switch/close`, state save/load commands `browser.state.save/load`, and retained CMUX browser events for successful navigation, interaction, and redacted input actions for addressed WebKit browser surfaces. CLI routes serialize the matching browser namespace and legacy focus/url aliases in `rust/limux-cli/src/main.rs`. Verified by `cargo test -p limux-host-linux browser_action_routes_validate_surface_and_url -- --nocapture`, `cargo test -p limux-host-linux browser_element_ref_scripts_store_and_resolve_refs -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, `cargo test -p limux-cli cmux_ -- --nocapture`, `cargo test -p limux-core dispatcher_browser_screenshot_writes_requested_png_path -- --nocapture`, `cargo test -p limux-host-linux browser_find -- --nocapture`, `cargo test -p limux-host-linux browser_state -- --nocapture`, `cargo test -p limux-host-linux browser_stateful -- --nocapture`, `cargo test -p limux-host-linux browser_diagnostics -- --nocapture`, `cargo test -p limux-host-linux browser_tab -- --nocapture`, `cargo test -p limux-host-linux event_bus -- --nocapture`, `cargo test -p limux-host-linux -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, `cargo test -p limux-control -- --nocapture`, and `./scripts/check.sh`. |
| Browser documented unsupported APIs | `browser.viewport.set`, `browser.geolocation.set`, `browser.offline.set`, `browser.trace.start/stop`, `browser.network.route/unroute/requests`, `browser.screencast.start/stop`, `browser.input_mouse/input_keyboard/input_touch` | Not supported by CMUX | Upstream documents these as WKWebView gaps in `/tmp/cmux-audit/skills/cmux-browser/references/commands.md`. Limux now returns explicit `not_supported` errors for the CMUX CLI forms and live bridge RPC methods instead of silently passing them through or misreporting them as unknown. Verified by `cargo test -p limux-cli cmux_browser_unsupported -- --nocapture` and `cargo test -p limux-host-linux browser_unsupported -- --nocapture`. |
| Workspace lifecycle | `workspace list/create/env/close/rename/select/reconnect/disconnect`, `list-workspaces`, `new-workspace`, `select-workspace`, `close-workspace`, `rename-workspace`, `current-workspace` | Partial | Core workspace list/create/select/rename/close exists. Added workspace environment storage, `new-workspace --env/--env-file`, `workspace.create workspace_env`, project `cmux.json` workspace env loading for `new-workspace`/`workspace create`, `new-workspace --name/--title`, `workspace create --name/--title`, CMUX-compatible `--focus <true|false>` parsing for `new-workspace`/`workspace create`, live `workspace.create focus` handling with background creation as the default, CMUX `--layout <json>` CLI validation, live CMUX layout translation for split/pane/surface topology, layout terminal startup commands, layout browser URLs, CMUX `--description` persistence/API payloads, CMUX `--group`, `--group-placement top|end|afterCurrent`, and `--group-reference` creation placement, app-level `app.newWorkspacePlacement` loading/saving for normal Cmd-N/sidebar-button folder creation with CMUX pinned-prefix placement semantics, terminal inheritance for workspace env, `workspace env [--mask]` inspection with protected `CMUX_*`/`LIMUX_*` rejection, retained CMUX event emission for `workspace.created`, `workspace.selected`, `workspace.closed`, `workspace.renamed`, and `workspace.reordered` from live host model mutation paths, and explicit CMUX `workspace reconnect`/`disconnect` CLI-to-RPC mapping with live `workspace.remote.reconnect`/`workspace.remote.disconnect` `not_supported` errors until the remote daemon exists. Verified by `cargo test -p limux-cli project_workspace_env -- --nocapture`, `cargo test -p limux-cli project_cmux_config -- --nocapture`, `cargo test -p limux-cli new_workspace_params -- --nocapture`, `cargo test -p limux-cli workspace_env -- --nocapture`, `cargo test -p limux-cli workspace_remote_namespace_requests_map_to_cmux_methods -- --nocapture`, `cargo test -p limux-host-linux workspace_create_route -- --nocapture`, `cargo test -p limux-host-linux workspace_insert_index_matches_cmux_app_placements_with_pins -- --nocapture`, `cargo test -p limux-host-linux app_new_workspace_placement -- --nocapture`, `cargo test -p limux-host-linux workspace_group_state_round_trips_through_session_json -- --nocapture`, `cargo test -p limux-host-linux workspace_lifecycle -- --nocapture`, `cargo test -p limux-host-linux workspace_reordered -- --nocapture`, `cargo test -p limux-host-linux remote_workspace_methods_fail_before_dispatch -- --nocapture`, and `./scripts/check.sh`. Remaining: actual remote reconnect/disconnect behavior, moving/reordering across windows, exact multi-window routing semantics, and broader CMUX argument compatibility. |
| Workspace groups | `workspace-group list/create/ungroup/delete/rename/collapse/expand/pin/unpin/add/remove/set-anchor/new-workspace/set-color/set-icon/move/focus`; `workspace.group.*` | Partial | Added CMUX-style persisted group model, per-workspace `group_id`, live `workspace.group.*` list and mutation APIs, `list-workspace-groups`, `workspace-group <subcommand>`, and `workspace group <subcommand>` CLI routes. Collapsed groups now resync the GTK sidebar and hide inactive non-anchor member workspaces while keeping the anchor and active workspace visible. `workspace.group.new_workspace` now validates `placement`, applies CMUX top/end/afterCurrent insertion order, and resolves omitted placement from `workspaceGroups.newWorkspacePlacement` plus longest-match `workspaceGroups.byCwd[*].newWorkspacePlacement` settings. Cmd-N/sidebar-button folder creation now joins the active workspace group, resolves placement from the group anchor cwd, and uses the active member as the `afterCurrent` reference. Verified by focused workspace-group tests, `cargo test -p limux-host-linux workspace_group -- --nocapture`, `cargo test -p limux-host-linux workspace_group_mutation_routes_parse_cmux_params -- --nocapture`, `cargo test -p limux-host-linux workspace_group_insert_index_matches_cmux_placements -- --nocapture`, `cargo test -p limux-host-linux workspace_group_placement -- --nocapture`, `cargo test -p limux-host-linux collapsed_workspace_groups -- --nocapture`, `cargo clippy -p limux-host-linux --all-targets -- -D warnings`, and `./scripts/check.sh`. Remaining: true grouped sidebar/header rendering, drag-order semantics across grouped rows, delete confirmation UX, and group-header `+` UI placement parity after grouped header rendering exists. |
| Pane and surface lifecycle | `new-split`, `list-panes`, `list-pane-surfaces`, `focus-pane`, `new-pane`, `new-surface`, `close-surface`, `move-surface`, `split-off`, `reorder-surface`, `drag-surface-to-split`, `refresh-surfaces`, `clear-history`, `surface-health` | Partial | Live bridge supports list/create/focus, terminal surface creation, `surface.split`/`new-split` to create a real split from a source surface, CMUX watcher startup fields for split-created terminal panes (`initial_command`, `working_directory`, and strictly validated `startup_environment`) so managed Codex subagent panes can launch with startup cwd/env instead of post-create text injection, `surface.drag_to_split`/`split-off`/`drag-surface-to-split` to move a surface into a new split, `surface.close`/`close-surface` tab removal, `surface.move`/`move-surface` to another pane with optional index, `surface.reorder`/`reorder-surface` within a pane by index or before/after target, `surface.refresh`/`refresh-surfaces` terminal redraws, `surface.clear_history`/`clear-history` terminal scrollback clearing through Ghostty's live clear action, retained CMUX `surface.created`, `surface.focused`, `surface.closed`, `surface.moved`, and `surface.reordered` lifecycle events from successful live host mutations, and tmux pane resize/swap/join/break selected-surface movement, all with session-save scheduling where state mutates. Verified by `cargo test -p limux-host-linux pane_create_contract_accepts_cmux_startup_fields -- --nocapture`, `cargo test -p limux-host-linux pane_create_contract_rejects_invalid_startup_fields -- --nocapture`, `cargo test -p limux-host-linux surface_split_route_queues_cmux_startup_fields -- --nocapture`, `cargo test -p limux-host-linux surface_split -- --nocapture`, `cargo test -p limux-host-linux surface_drag_to_split -- --nocapture`, `cargo test -p limux-host-linux surface_close -- --nocapture`, `cargo test -p limux-host-linux surface_move -- --nocapture`, `cargo test -p limux-host-linux surface_reorder -- --nocapture`, `cargo test -p limux-host-linux surface_refresh -- --nocapture`, `cargo test -p limux-host-linux surface_clear_history -- --nocapture`, `cargo test -p limux-host-linux surface_lifecycle -- --nocapture`, `cargo test -p limux-host-linux pane_resize -- --nocapture`, `cargo test -p limux-host-linux pane_swap -- --nocapture`, `cargo test -p limux-host-linux pane_join -- --nocapture`, `cargo test -p limux-host-linux pane_break -- --nocapture`, `cargo test -p limux-cli cmux_surface_aliases -- --nocapture`, and `./scripts/check.sh`. Remaining: broader cross-window pane move semantics outside the current live host scope. |
| Terminal I/O | `read-screen`, `capture-pane`, `send`, `send-key`, `send-panel`, `send-key-panel` | Partial | Terminal read/send/key paths exist. Live socket text/key injection now emits retained CMUX `surface.input_sent` and `surface.key_sent` events with terminal text redacted from event payloads. `read-screen`/`capture-pane` now serialize CMUX `--lines` and `--scrollback` options, validate positive line counts, apply last-N line limiting to the terminal text Limux can currently read, and return explicit `scrollback_requested`, `scrollback_included`, and `source` metadata instead of silently claiming scrollback support. Verified by `cargo test -p limux-host-linux surface_io -- --nocapture`, `cargo test -p limux-host-linux surface_input -- --nocapture`, `cargo test -p limux-host-linux read_text_route_accepts_lines_and_scrollback_flags -- --nocapture`, `cargo test -p limux-host-linux limit_text_to_last_lines_preserves_line_endings -- --nocapture`, `cargo test -p limux-cli read_screen_params_include_lines_and_scrollback -- --nocapture`, and `cargo test -p limux-cli cmux_surface_aliases -- --nocapture`. Remaining: true beyond-viewport scrollback capture from Ghostty. |
| Topology and resource diagnostics | `tree`, `top`, `memory`; `system.tree`, `system.top`, `system.memory` | Partial | Limux now exposes live GTK bridge `window.list`, native `system.tree`, and native `system.top`; top-level `tree` routes to `system.tree`, which snapshots the current GTK host window with workspace, pane, and surface children and returns JSON tagged with `source=limux_live_system_tree`. Top-level `top` routes to native `system.top`, supports `--workspace` filtering against Limux child-process workspace attribution, validates `--window window:1` for the current single GTK host window, accepts `--all`, supports local text sorting by cumulative CPU ticks, memory, or process count with `--sort cpu|mem|proc`, and supports CMUX-shaped TSV text output through `--flat`/`--format tsv` for the current local process-group diagnostics. Verified by `cargo test -p limux-host-linux system_top -- --nocapture`, `cargo test -p limux-host-linux memory_diagnostics -- --nocapture`, `cargo test -p limux-host-linux system_tree -- --nocapture`, `cargo test -p limux-cli cmux_tree -- --nocapture`, `cargo test -p limux-cli cmux_top -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`. Remaining: sampled CPU-percent windows and broader multi-window process attribution if Limux gains multiple live GTK windows. |
| Events stream | `events.stream`; CLI `events --after --cursor-file --name --category --reconnect --limit --no-ack --no-heartbeat` | Implemented | Added a host-side retained event bus with 4,096-frame replay, ack resume metadata, category/name filters, heartbeat frames, live socket takeover, bounded event frames, live subscriber `slow_consumer` cutoff at 1,024 pending matching events, durable CMUX-compatible `~/.cmuxterm/events.jsonl` append, 16 MiB rotation to `events.jsonl.1`, Feed/agent/notification/workspace lifecycle/surface lifecycle/surface I/O/config reload/browser navigation/browser interaction/redacted browser input event publication, and CLI reconnect resume from the last processed `seq`. Verified by `cargo test -p limux-host-linux event_bus -- --nocapture`, `cargo test -p limux-host-linux live_stream_closes_slow_consumers_after_pending_queue_limit -- --nocapture`, `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`, `cargo test -p limux-host-linux workspace_lifecycle -- --nocapture`, `cargo test -p limux-host-linux workspace_reordered -- --nocapture`, `cargo test -p limux-host-linux surface_lifecycle -- --nocapture`, `cargo test -p limux-host-linux config_reload -- --nocapture`, `cargo test -p limux-host-linux browser_ -- --nocapture`, `cargo test -p limux-host-linux surface_input -- --nocapture`, and `cargo test -p limux-cli events_ -- --nocapture`. |
| Feed and approvals | `feed.push`, `feed.permission.reply`, `feed.question.reply`, `feed.exit_plan.reply`; `feed tui`, `feed clear`, feed hooks | Partial | Added a live host Feed backend with bounded in-memory item ring, direct socket handling for `feed.push`, `feed.list`, `feed.clear`, `feed.permission.reply`, `feed.question.reply`, and `feed.exit_plan.reply`, CMUX-shaped resolved/acknowledged/timed_out result payloads, reply mode validation, blocking wait support beyond the normal 5s control-command timeout, `limux feed clear [--yes|-y]` local deletion of `~/.cmuxterm/workstream.jsonl` and `workstream.jsonl.1`, `limux feed tui [--opentui|--legacy]` routing to the live Feed right-sidebar UI with explicit `limux-right-sidebar` implementation reporting, direct right-sidebar permission controls for pending Codex/CMUX Feed requests (`once`, `always`, `bypass`, `deny`), CMUX-compatible Codex app-server permission action capability filtering for right-sidebar and native notification action buttons (`once`/`always`/`all` only when app-server `available_decisions` and amendment payloads can map back to valid approval responses), public `limux codex-teams` launcher/watcher orchestration for Codex app-server approvals and managed subagent splits, direct right-sidebar exit-plan controls (`manual`, `autoAccept`, `bypassPermissions`, `ultraplan`, `deny`), direct right-sidebar question choice controls from CMUX/Claude question option payloads, native host notifications for pending actionable Feed permission, exit-plan, and question rows with workspace/surface targeting plus Linux inline notification action buttons through the existing freedesktop notification route, `limux hooks feed --source <agent>` CLI forwarding, CMUX-style Claude/Codex/generic hook event classification, blocking/nonblocking timeout selection, decision JSON rendering for permission, plan, and question replies, Codex hook setup wiring that installs CMUX Feed hook entries for `PreToolUse`, `PermissionRequest`, `PostToolUse`, `PreCompact`, `SubagentStart`, and `SubagentStop`, per-surface Codex wrapper shim injection for terminal tabs with `PATH` prepending plus `CMUX_CODEX_WRAPPER_SHIM`/`LIMUX_CODEX_WRAPPER_SHIM` exports, `LIMUX_CLI`/`CMUX_CLI` env wiring, wrapper-owned `hooks codex session-start` emission with surface/workspace/pid metadata, and launch metadata preservation for real Codex hook events, Claude hook setup wiring that installs blocking Feed entries for `PreToolUse` and `PermissionRequest` with Claude's matcher shape, Gemini hook setup wiring that installs blocking Feed `PreToolUse`, Cursor CLI flat JSON hook setup wiring that installs lifecycle entries plus Feed `beforeShellExecution`, Kiro CLI agent JSON hook setup wiring that installs lifecycle entries plus Feed `preToolUse` and `postToolUse`, Antigravity hook group setup wiring that installs lifecycle entries plus Feed `PreToolUse` and `PostToolUse`, Rovo Dev YAML setup wiring that installs lifecycle-only `on_complete`, `on_error`, and `on_tool_permission` hooks, OMP extension setup wiring that installs lifecycle-only `session_start`, `before_agent_start`, and `agent_end` hooks with CMUX-compatible base64 launch capture, Grok hook setup wiring that installs lifecycle entries plus blocking Feed `PreToolUse` with CMUX second-based timeout units, and Copilot, CodeBuddy, Factory, and Qoder nested JSON hook setup wiring that installs lifecycle entries plus blocking Feed `PreToolUse`, retained `feed.item.received`, `feed.item.completed`, `feed.item.resolved`, `feed.cleared`, and `agent.hook.<HookEventName>` events, bounded CMUX-compatible `~/.cmuxterm/workstream.jsonl` audit persistence with rotation to `workstream.jsonl.1`, and a right-sidebar Feed preview while preserving `feed --help` and `hooks feed --source ...`. Verified by `cargo test -p limux-cli cmux_feed -- --nocapture`, `cargo test -p limux-cli codex_teams -- --nocapture`, `cargo test -p limux-cli antigravity -- --nocapture`, `cargo test -p limux-cli rovo -- --nocapture`, `cargo test -p limux-cli omp -- --nocapture`, `cargo test -p limux-cli cursor -- --nocapture`, `cargo test -p limux-cli kiro -- --nocapture`, `cargo test -p limux-cli grok -- --nocapture`, `cargo test -p limux-cli nested_json_agents -- --nocapture`, `cargo test -p limux-cli hook_install_writes_feed_hooks -- --nocapture`, `cargo test -p limux-host-linux feed_actions -- --nocapture`, `cargo test -p limux-host-linux push_notifies_codex_app_server_permission_with_supported_actions -- --nocapture`, `cargo test -p limux-host-linux right_sidebar_codex_app_server_permission_actions_follow_capabilities -- --nocapture`, `cargo test -p limux-host-linux right_sidebar_feed -- --nocapture`, `cargo test -p limux-host-linux feed_sidebar_ -- --nocapture`, `cargo test -p limux-host-linux feed -- --nocapture`, `cargo test -p limux-host-linux notification -- --nocapture`, `cargo test -p limux-host-linux right_sidebar -- --nocapture`, `cargo test -p limux-host-linux event_bus -- --nocapture`, and `cargo test -p limux-host-linux event_stream_takeover -- --nocapture`. Remaining: exact CMUX Bun/OpenTUI terminal UI parity, hook setup wiring for additional CMUX agents beyond Codex/Claude/Grok/Gemini/OpenCode/Cursor/Kiro/Antigravity/RovoDev/OMP/Copilot/CodeBuddy/Factory/Qoder, and broader non-Codex agent-specific edge cases. |
| Right sidebar controls | `right-sidebar toggle/show/hide/focus/set/mode`, mode aliases `files/find/vault/sessions/feed/dock`; `sidebar validate/reload/select/open`; `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, `list-log`, `sidebar-state`; `right_sidebar`, `sidebar.*` | Partial | Added CMUX-compatible CLI parsing for `right-sidebar`, including `--workspace`, `--tab`, `--window`, `--no-focus`, mode aliases, silent success output for mutating commands, and JSON state output for `mode`. The live GTK bridge now exposes `right_sidebar`, validates modes, queues host-owned visibility/mode/focus state, and reports `visible`, `mode`, `focused`, `workspace_id`, and supported modes. Added workspace-scoped sidebar metadata parity for `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, `list-log`, and `sidebar-state`, backed by live GTK host state, CMUX alias bridge methods, retained `sidebar.metadata.*`, `sidebar.progress.*`, and `sidebar.log.*` events, bounded log retention, and a rendered GTK right-sidebar shell. The shell now renders mode-specific visible panels: Files uses a bounded non-recursive workspace-folder preview, Find shows the search root plus open surfaces, Vault shows non-secret metadata counts, Sessions shows pane and surface summaries, Feed shows newest retained Feed items, and Dock shows selected/open surfaces. Added local custom-sidebar validation/docs parity for `sidebar validate` and `sidebar reload` against `~/.config/cmux/sidebars`, including `.swift` over `.json` precedence, JSON object validation, empty Swift rejection, text/JSON reports, and explicit `not_supported` renderer failures for `sidebar select/open` after the requested source validates. Verified by `cargo test -p limux-cli cmux_right_sidebar -- --nocapture`, `cargo test -p limux-cli cmux_sidebar -- --nocapture`, `cargo test -p limux-cli custom_sidebar -- --nocapture`, `cargo test -p limux-host-linux right_sidebar -- --nocapture`, `cargo test -p limux-host-linux sidebar_ -- --nocapture`, `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`, and `./scripts/check.sh`. Remaining: full interactive files/find/vault/sessions/feed/dock workflows, runtime custom-sidebar SwiftUI/JSON rendering for `sidebar select/open`, and window-specific routing beyond the current single GTK host window. |
| Agent session diagnostics | `sessions list`, `sessions debug`, `session-debug` | Partial | Added no-socket CLI inspection of saved Claude/Codex/Grok/OpenCode/Cursor/Kiro/Antigravity/RovoDev/OMP/Gemini/Copilot/CodeBuddy/Factory/Qoder hook-session stores, with `--agent`, `--session`, `--workspace`, `--surface`, `--cwd`, `--state-dir`, `--codex-home`, `--limit`, `--all`, and `--json` support; CMUX-shaped text/JSON rows; camelCase CMUX hook-store aliases for session and launch-command fields; Codex session-index/transcript diagnostics; Linux pid-anchored `/proc/<pid>/fd` transcript resolution for wrapper-started Codex sessions; native Codex session-id/path reporting without newest-file heuristics; stale saved-PID reporting so forkable sessions with dead PIDs remain visible instead of disappearing; out-of-range stored PID tolerance; concrete `fork_command` JSON/text diagnostics for agents with native fork forms, including Codex, Claude, OpenCode, Pi, and OMP, using preserved safe launch args and allowlisted environment while stripping credentials; and CMUX-compatible `hook_record_restorable`, `fork_unavailable_reason`, `fork_startup_input_available`, and `stale_pid_blocks_restore_in_0_64_17` diagnostics, including Claude transcript trust checks from direct `transcriptPath` files, `CLAUDE_CONFIG_DIR/projects/<encoded-cwd>/<session>.jsonl`, unambiguous workflow-container sibling transcript resolution, and OpenCode fork-support version probing with the CMUX minimum fork-capable version `1.14.50`, missing-executable rejection, remote-like context bypass, sanitized probe environment, and per-command probe caching. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-cli fork_command -- --nocapture`, `cargo test -p limux-cli codex_transcript -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, direct `cargo run -p limux-cli --bin limux-cli -- sessions list --agent codex --state-dir /tmp/limux-empty-sessions --json`, and direct `cargo run -p limux-cli --bin limux-cli -- session-debug --agent claude --state-dir /tmp/limux-empty-sessions`. Remaining: session diagnostics has no currently documented CMUX gap in this matrix; broader parity gaps remain in other rows. |
| Remote and SSH | `ssh`, `remote list/add/remove`, `remotes list/add/remove`, `remote-daemon-status`, `ssh-session-*`; `workspace.remote.*`; `cmuxd-remote` daemon RPC | Partial | Limux exposes explicit `not_supported` behavior for `workspace.remote.reconnect`/`workspace.remote.disconnect` live bridge methods and now fails top-level Remote/SSH CLI forms locally before socket routing when they require CMUX remote daemon support or CMUX cloud/team device-registry infrastructure (`ssh`, `ssh-tmux`, `ssh-pty-attach`, `ssh-session-list`, `ssh-session-attach`, `ssh-session-cleanup`, `ssh-session-end`, `remote-daemon-status`, `remote`, `remotes`, `vm-ssh-attach`) while preserving no-socket help probes. Verified by `cargo test -p limux-cli cmux_remote_ssh -- --nocapture`, `cargo test -p limux-host-linux remote_workspace_methods_fail_before_dispatch -- --nocapture`, and `./scripts/check.sh`. Remaining: CMUX-compatible remote daemon, SSH workspace proxy, lease/auth file protocol, remote PTY/session RPC, and cloud/team device registry-backed remote list/add/remove management. |
| tmux compatibility | `capture-pane`, `resize-pane`, `pipe-pane`, `wait-for`, `swap-pane`, `break-pane`, `join-pane`, `next-window`, `previous-window`, `last-window`, `last-pane`, `find-window`, `clear-history`, `list-windows`, `list-panes`, buffers/hooks/respawn/display-message | Partial | Several compatibility wrappers exist in CLI. Live bridge now supports `workspace.next`/`next-window`, `workspace.previous`/`previous-window`, and `workspace.last`/`last-window` against the real GTK workspace stack with previous-workspace tracking, `pane.last`/`last-pane` using live control-bridge pane focus history, `surface.clear_history`/`clear-history` for addressed or focused terminal surfaces, `pane.resize`/`resize-pane` by mutating persisted GTK split ratios, `pane.swap`/`swap-pane` by exchanging selected surfaces between panes, `pane.join`/`join-pane` by moving an addressed or focused surface into a target pane, `pane.break`/`break-pane` by moving an addressed or focused surface into a newly-created workspace, `surface.respawn`/`respawn-pane` by replacing the addressed terminal tab process while preserving the surface ID, `split-window`/`splitw` by mapping tmux `-h`/`-v`/`-b` directions, `-d` detached focus preservation, exact live-geometry `-l` initial divider sizing, `-c` working directory, `-t` surface target, command injection, and `-P`/`-F` formatted output to `surface.split`, and `select-layout` by mapping main layouts to `workspace.equalize_splits` orientation. CLI-local tmux buffers, hooks, aliases, and display-message handling now match more CMUX defaults for the documented commands: default buffer name, `-b` buffer names, explicit missing-buffer errors for paste, empty output for missing show-buffer, size-reporting `list-buffers`, `set-hook --list/--unset`, `capturep`, `display/displayp`, `resizep`, `respawnp`, `setb`, `pasteb`, `show-buffer/showb`, lifecycle aliases `neww`, `select-window`/`selectw`, `select-pane`/`selectp`, `kill-window`/`killw`, `kill-pane`/`killp`, and `renamew`, `lsw`, `lsp`, `list-windows -F/--format`, `list-panes -F/--format`, `pipe-pane --command` and trailing shell-command parsing, `wait-for` 30-second/fractional timeout handling, `find-window` multi-match/no-match text plus `--content`/`--select` routing, real active terminal rows/columns/pixel/cell metrics in `pane.list`, explicit placeholder rejection for `popup`, `bind-key`, `unbind-key`, and `copy-mode`, plus `display-message -p/-F/-t` parsing with known `#{...}` expansion, unknown-key stripping, trim, and empty-render fallback behavior backed by live workspace/pane/surface context. Verified by `cargo test -p limux-host-linux workspace_navigation -- --nocapture`, `cargo test -p limux-host-linux pane_last -- --nocapture`, `cargo test -p limux-host-linux surface_clear_history -- --nocapture`, `cargo test -p limux-host-linux pane_resize -- --nocapture`, `cargo test -p limux-host-linux pane_swap -- --nocapture`, `cargo test -p limux-host-linux pane_join -- --nocapture`, `cargo test -p limux-host-linux pane_break -- --nocapture`, `cargo test -p limux-host-linux surface_respawn -- --nocapture`, `cargo test -p limux-host-linux surface_split -- --nocapture`, `cargo test -p limux-core dispatcher_handles_pane_and_surface_flow -- --nocapture`, `cargo test -p limux-cli respawn_pane_request -- --nocapture`, `cargo test -p limux-cli tmux_pipe_pane -- --nocapture`, `cargo test -p limux-cli tmux_wait_for -- --nocapture`, `cargo test -p limux-cli tmux_find_window -- --nocapture`, `cargo test -p limux-cli tmux -- --nocapture`, `cargo test -p limux-cli cmux_ -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, `cargo test -p limux-host-linux ratio -- --nocapture`, and `./scripts/check.sh`. Remaining: full remote tmux daemon parity and exact advanced tmux layout state tracking. |
| tmux documented unsupported placeholders | `popup`, `bind-key`, `unbind-key`, `copy-mode` | Not supported by CMUX | Upstream CLI contract marks these as placeholders/unsupported. Limux should report explicit unsupported behavior if exposed. |
| Config/settings/themes/docs | `settings`, `config doctor/check/validate/path/paths/docs/reload/get/set`, `themes list/set/clear`, `docs settings/shortcuts/api/browser/agents/dock/sidebars` | Partial | Implemented no-socket CLI parity for `docs`, `docs dock` plus dock aliases, `docs sidebars` plus custom-sidebar aliases, `settings path/docs`, `shortcuts`, `config path/paths/check/validate/doctor/docs/documentation`, CMUX-style `config doctor --path <path>` custom target validation, default config doctor inclusion of nearest project `.cmux/cmux.json`/`cmux.json`, JSONC comment and trailing-comma preprocessing for `config doctor`, `config get/set sidebar-font-size`, `config get/set surface-tab-bar-font-size`, `config get/set notifications.sound`, `config get/set notifications.customSoundFilePath`, `config get/set notifications.agentPermissionPrompt`, `config get/set notifications.agentTurnComplete`, `config get/set notifications.agentIdleReminder`, `config get/set app.appearance`, `config get/set app.newWorkspacePlacement`, `config get/set workspaceGroups.newWorkspacePlacement`, direct `config sidebar-font-size [points]`, direct `config surface-tab-bar-font-size [points]`, CMUX-compatible `app.appearance` settings JSON load/save and host appearance application with invalid-value rejection and legacy Limux appearance-section sync, CMUX-compatible `app.newWorkspacePlacement` settings JSON load/save with invalid-value rejection and unrelated app-key preservation, CMUX-compatible `workspaceGroups.newWorkspacePlacement` settings JSON load/save with existing byCwd override preservation, CMUX notification sound settings JSON load/save for `notifications.sound` and `notifications.customSoundFilePath`, CMUX agent notification settings JSON load/save for `notifications.agentPermissionPrompt`, `notifications.agentTurnComplete`, and `notifications.agentIdleReminder`, CMUX-style `themes list/set/clear` management of a marked Ghostty `theme = ...` config block with light/dark conditional values and JSON output, Ghostty theme catalog discovery from Linux resource/XDG/repo/user theme directories, CMUX-shaped theme list rows with current light/dark badges, catalog-backed set validation with canonical case matching, live socket `config reload`/`reload-config`/`reload_config`/`config.reload` support that reloads settings, shortcuts, and Ghostty config, applies appearance and terminal font-size changes, and emits retained `config.reloaded`, and live host `settings open [target]` routing through `settings.open` with CMUX target aliases, bridge-side target validation, `activate` parsing, target echoing, and nearest-page GTK dialog presentation. Font-size values use CMUX implementation defaults/ranges from `/tmp/cmux-audit/CLI/CMUXCLI+Config.swift` and preserve unrelated Limux settings JSON. Verified by `cargo test -p limux-cli cmux_docs -- --nocapture`, `cargo test -p limux-cli custom_sidebar -- --nocapture`, `cargo test -p limux-cli theme -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, isolated `XDG_CONFIG_HOME` direct `limux-cli --json themes set/list` smoke, `cargo test -p limux-cli config_font_size -- --nocapture`, `cargo test -p limux-cli config_notification_settings -- --nocapture`, `cargo test -p limux-cli config_app_appearance -- --nocapture`, `cargo test -p limux-cli config_workspace_placement -- --nocapture`, `cargo test -p limux-cli config_reload -- --nocapture`, `cargo test -p limux-cli config_ -- --nocapture`, `cargo test -p limux-cli jsonc -- --nocapture`, `cargo test -p limux-cli settings_open -- --nocapture`, `cargo test -p limux-host-linux app_appearance -- --nocapture`, `cargo test -p limux-host-linux app_new_workspace_placement -- --nocapture`, `cargo test -p limux-host-linux notification_preferences -- --nocapture`, `cargo test -p limux-host-linux notification_sound -- --nocapture`, `cargo test -p limux-host-linux desktop_notification_hints_include_cmux_sound_and_custom_file -- --nocapture`, `cargo test -p limux-host-linux workspace_group_placement -- --nocapture`, `cargo test -p limux-host-linux config_reload -- --nocapture`, `cargo test -p limux-host-linux settings_open -- --nocapture`, and `./scripts/check.sh`. Remaining: bundled/interactive Ghostty theme picker parity, exact settings-page UI for every CMUX target beyond Limux's current General/Keybindings pages, and upstream `config get/set` keys beyond the font-size, notification, sound, app appearance, and workspace-placement settings found in current CMUX. |
| CLI globals and environment | `--socket`, `--password`, `--json`, `--id-format`, `--window`; `CMUX_SOCKET_PATH`, `CMUX_SOCKET`, `CMUX_SOCKET_PASSWORD`, `CMUX_WORKSPACE_ID`, `CMUX_SURFACE_ID`, `CMUX_TAB_ID` | Partial | Limux now supports `CMUX_SOCKET_PATH`/`CMUX_SOCKET` checked routing, global `--window` parsing for window aliases, global `--password` parsing, CMUX password precedence of explicit `--password`, `CMUX_SOCKET_PASSWORD`, then `~/.local/state/limux/socket-control-password`, `LIMUX_SOCKET_MODE=password`/`CMUX_SOCKET_MODE=password` startup validation, same-user peer gating before the password handshake, JSON `auth.login`/`auth.status`, CMUX legacy `auth <password>` text responses, no-socket `help`/`version`, no-socket CMUX command help probes for socket-backed and legacy alias commands, command-position `--json`/`--id-format` presentation flags outside browser commands, and CMUX workspace/surface/tab env aliases in CLI context lookup and spawned terminal env. Verified by `cargo test -p limux-cli client_sends_auth_login_before_request_when_password_is_set -- --nocapture`, `cargo test -p limux-control socket_password_resolver_prefers_explicit_then_env_then_file -- --nocapture`, `cargo test -p limux-host-linux password_mode_rejects_request_until_auth_login_succeeds -- --nocapture`, `cargo test -p limux-host-linux legacy_cmux_auth_command_returns_text_status -- --nocapture`, `cargo test -p limux-cli cmux_command_help -- --nocapture`, `cargo test -p limux-cli cmux_non_help_probe -- --nocapture`, `cargo test -p limux-cli cmux_command_position -- --nocapture`, direct `cargo run -q -p limux-cli --bin limux-cli -- sessions list --state-dir /tmp/limux-no-sessions --json`, direct `cargo run -p limux-cli --bin limux-cli -- list-panes --help`, direct `cargo run -p limux-cli --bin limux-cli -- list-windows --help`, focused touched-crate `cargo test -p limux-control -p limux-cli -p limux-host-linux`, and `./scripts/check.sh`. Remaining: full multi-window routing. |

Primary upstream references:

Recent verified parity updates:

- 2026-07-02: Added CMUX-compatible `app.appearance` config parity: local `config get/set app.appearance` now reads the CMUX `system` default, writes nested settings JSON while preserving unrelated app and notification keys, rejects invalid values loudly, and the GTK host now prefers `app.appearance` for the app color scheme while keeping legacy Limux `appearance.color_scheme` in sync on save. Verified by `cargo test -p limux-cli config_app_appearance -- --nocapture`, `cargo test -p limux-host-linux app_appearance -- --nocapture`, `cargo test -p limux-host-linux save_writes_gtk_and_ghostty_color_schemes -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`.

- 2026-07-02: Added CMUX-compatible local `config get/set` and host runtime parity for notification sound settings: `notifications.sound` now accepts CMUX documented sound presets plus existing Linux aliases without silent fallback, `notifications.customSoundFilePath` is preserved, `custom_file` emits a Linux `sound-file` notification hint when configured, and invalid sound values fail loudly. Verified by `cargo test -p limux-cli config_notification_settings -- --nocapture`, `cargo test -p limux-host-linux notification_sound -- --nocapture`, and `cargo test -p limux-host-linux desktop_notification_hints_include_cmux_sound_and_custom_file -- --nocapture`.

- 2026-07-02: Added CMUX-compatible local `config get/set` parity for workspace placement settings: `app.newWorkspacePlacement` and `workspaceGroups.newWorkspacePlacement` now read the CMUX `afterCurrent` default, write nested settings JSON while preserving unrelated app, notification, and `workspaceGroups.byCwd` data, and reject invalid placement values loudly. Verified by `cargo test -p limux-cli config_workspace_placement -- --nocapture`, `cargo test -p limux-cli config_notification_settings -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.

- 2026-07-02: Added CMUX-compatible local `config get/set` parity for agent notification settings: `notifications.agentPermissionPrompt`, `notifications.agentTurnComplete`, and `notifications.agentIdleReminder` now read CMUX defaults when unset, write nested `notifications` JSON while preserving unrelated settings, and reject invalid boolean/turn-complete values loudly. Verified by `cargo test -p limux-cli config_notification_settings -- --nocapture` and `cargo test -p limux-cli config_font_size -- --nocapture`.

- 2026-07-02: Added CMUX-compatible agent notification gating parity from `128be4c1ac`: Limux now loads/saves `notifications.agentPermissionPrompt`, `notifications.agentTurnComplete`, and `notifications.agentIdleReminder`, classifies Claude hook notifications as turn-complete/needs-permission/idle-reminder, tracks pending Claude background work from `session_crons` and running `background_tasks`, accepts `agent_category`/`agentCategory` plus `agent_pending`/`agentPending` over `notification.create`, and suppresses or delivers agent notifications with the CMUX per-category decision table. Verified by `cargo test -p limux-cli claude_notification_gate -- --nocapture`, `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-host-linux agent_notification -- --nocapture`, `cargo test -p limux-host-linux notification_preferences -- --nocapture`, and `cargo test -p limux-host-linux notification_list_and_clear_routes_validate_params -- --nocapture`.

- 2026-07-02: Added CMUX-compatible OpenCode fork-support version probing for `sessions list`: local OpenCode records now keep `fork_command_available` separate from `fork_supported`, accept versions at or above `1.14.50`, reject missing absolute executables, bypass local probing for remote-like working directories, sanitize the probe environment, and cache probe results during a command. Verified by `cargo test -p limux-cli sessions -- --nocapture`.
- 2026-07-02: Added CMUX-compatible Claude workflow-container transcript resolution for `sessions list`: when a Claude hook record points `transcriptPath` at a workflow container directory, Limux now resolves the session to the single sibling transcript under the matching Claude project root and uses that resolved session id/path for fork diagnostics. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and SCPlus lint on `rust/limux-cli/src/sessions.rs`.
- 2026-07-02: Refreshed the CMUX audit target to `5eef9ac77dfebeeff1b8cd73f9328e8caea1879d` and added CMUX session-list trust diagnostics in `limux-cli`: native CMUX camelCase hook stores now deserialize, out-of-range saved PID values no longer break diagnostics, Claude `transcriptPath` and `CLAUDE_CONFIG_DIR/projects/<encoded-cwd>/<session>.jsonl` evidence gates `hook_record_restorable`, and JSON rows expose `fork_unavailable_reason`, `fork_startup_input_available`, and `stale_pid_blocks_restore_in_0_64_17`. Verified by `cargo test -p limux-cli sessions -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible session fork-command diagnostics in `limux-cli`: `sessions list/debug --json` now includes concrete `fork_command` text plus accurate `fork_command_available`/`fork_supported` booleans for agents with native fork forms, and text output now reports command availability from the rendered command. The builder preserves safe launch flags and allowlisted environment for Codex, Claude, OpenCode, Pi, and OMP while stripping credentials. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-cli fork_command -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added the public Codex Teams launcher in `limux-cli`: `limux codex-teams` now requires a Limux terminal surface context, validates Codex working-directory flags, starts a private `codex app-server --listen ws://127.0.0.1:<port>`, waits for websocket initialization, launches the root Codex process with CMUX-positioned `--remote`, starts the hidden `__codex-teams-watch` bridge, passes socket auth to the watcher, writes private helper logs, and terminates the app-server/watcher process groups on root exit or startup failure so wrapper-spawned app-server descendants do not leak. Verified by `cargo test -p limux-cli codex_teams -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, a bounded `LIMUX_WORKSPACE_ID=ws-smoke LIMUX_SURFACE_ID=surface-smoke ./target/debug/limux-cli --socket /tmp/limux-missing-codex-teams.sock codex-teams --version` smoke with no remaining `codex app-server --listen ws://127.0.0.1` process, and `./scripts/check.sh`. Remaining: fully interactive root/subagent smoke inside a live Limux GUI session.
- 2026-07-02: Added the hidden Codex Teams watcher stream in `limux-cli`: `__codex-teams-watch` now validates required watcher args, connects to the Codex app-server, initializes with Codex Teams notification opt-outs, backfills `thread/loaded/list`, subscribes with `thread/resume { excludeTurns: true }`, forwards app-server approval requests through `feed.push`, maps resolved Feed permission decisions back to Codex JSON-RPC responses, caches bounded related approval items, tracks subagent thread depth/open state, opens managed subagent panes through `surface.split`, and applies best-effort tab rename/equalize calls. Verified by `cargo test -p limux-cli codex_teams -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and `./scripts/check.sh`.
- 2026-07-02: Added the Codex Teams app-server websocket JSON-RPC client foundation in `limux-cli`: `rust/limux-cli/src/codex_teams.rs` now validates app-server websocket URLs, opens Codex app-server websockets with `tokio-tungstenite`, performs the experimental `initialize`/`initialized` handshake, handles interleaved notifications while waiting for request ids, returns app-server errors loudly, sends JSON-RPC success/error responses back to Codex, and handles ping/pong and invalid frame failures. Verified by `cargo test -p limux-cli codex_app_server_connection -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and a local websocket protocol test. Remaining: production process launch/cleanup, watcher loop, approval forwarding through Feed, and live subagent split orchestration.
- 2026-07-02: Added the Codex Teams watcher model and managed subagent split-plan foundation in `limux-cli`: `rust/limux-cli/src/codex_teams.rs` now parses Codex app-server thread/subagent-spawn metadata, rejects not-loaded thread status for attachment, builds CMUX-positioned root Codex `--remote` args, shell-quotes managed subagent `codex resume --remote` commands, writes one-shot startup scripts, emits CMUX Teams startup environment metadata, and constructs `surface.split` params for chained managed subagent panes. Verified by `cargo test -p limux-cli codex_teams -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`. Remaining: production websocket loop, process launch/cleanup, approval request forwarding, and live subagent open orchestration.
- 2026-07-02: Added the pure Rust CMUX Codex Teams app-server approval bridge foundation in `limux-cli`: `rust/limux-cli/src/codex_teams.rs` now builds CMUX-shaped Feed permission events from Codex app-server approval requests, extracts resolved Feed permission modes, maps Feed decisions back to `item/commandExecution/requestApproval`, `item/fileChange/requestApproval`, and `item/permissions/requestApproval` response payloads, bounds approval item snapshots, and resolves/validates Codex `-C`/`--cd`/`--cwd` arguments. Verified by `cargo test -p limux-cli codex_teams -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX Codex watcher startup-field parity in `limux-host-linux`: `pane.create` and `surface.split` now parse `initial_command`, `working_directory`, and strictly validated `startup_environment`, and live terminal creation uses those fields at launch time for managed panes instead of post-create text injection. Verified by `cargo test -p limux-host-linux pane_create_contract_accepts_cmux_startup_fields -- --nocapture`, `cargo test -p limux-host-linux pane_create_contract_rejects_invalid_startup_fields -- --nocapture`, and `cargo test -p limux-host-linux surface_split_route_queues_cmux_startup_fields -- --nocapture`.
- 2026-07-02: Added CMUX-compatible Codex app-server permission action capability policy in `limux-host-linux`: Feed right-sidebar buttons and native notification actions now filter Codex app-server approval modes from app-server `available_decisions`, `app_server_method`, and amendment payloads, while non-app-server Feed rows keep the existing action set. Verified by `cargo test -p limux-host-linux feed_actions -- --nocapture`, `cargo test -p limux-host-linux push_notifies_codex_app_server_permission_with_supported_actions -- --nocapture`, and `cargo test -p limux-host-linux right_sidebar_codex_app_server_permission_actions_follow_capabilities -- --nocapture`.
- 2026-07-02: Added explicit CMUX `codex-teams` CLI surface handling in `limux-cli`: `codex-teams --help` now renders the documented local usage text, and non-help execution routes to the socket-backed public launcher instead of the earlier `not_supported` stub. Verified by `cargo test -p limux-cli codex_teams -- --nocapture`, `cargo test -p limux-cli cmux_command_help -- --nocapture`, `cargo clippy -p limux-cli --all-targets -- -D warnings`, and direct `limux-cli codex-teams` smoke commands.
- 2026-07-02: Added Linux inline Feed notification action parity in `limux-host-linux`: pending Feed permission, exit-plan, and question notifications now attach freedesktop action buttons and route selected actions through the existing shared Feed reply helpers. Verified by `cargo test -p limux-host-linux feed -- --nocapture`, `cargo test -p limux-host-linux notification -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible native Feed notification parity in `limux-host-linux`: pending actionable Feed permission, exit-plan, and question rows now dispatch host notifications through the existing workspace/surface-targeted notification pipeline before any blocking wait starts, while telemetry rows remain notification-free. Verified by `cargo test -p limux-host-linux feed -- --nocapture` and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible pid-anchored Codex transcript diagnostics in `limux-cli`: `sessions list/debug` now inspect live `/proc/<pid>/fd` targets for JSONL transcripts under the configured Codex home, accept only `sessions` or `archived_sessions` paths with UUID-shaped names, surface `codex_native_session_id`, `codex_pid_transcript_found`, and `codex_pid_transcript_path`, and fill transcript diagnostics from the live fd path when the saved wrapper session id is not indexed. Verified by `cargo test -p limux-cli sessions -- --nocapture`, `cargo test -p limux-cli codex_transcript -- --nocapture`, SCPlus lint on `rust/limux-cli/src/sessions.rs`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Codex wrapper-owned session-start parity in `limux-host-linux` and `limux-cli`: every terminal tab exports `LIMUX_CLI`/`CMUX_CLI`, the per-surface `codex` PATH shim emits `limux hooks codex session-start` with wrapper session id, surface, workspace, cwd, pid, and launch metadata before execing real Codex, and hook persistence accepts wrapper-provided session id and pid env metadata while still preferring real native session/transcript payloads when present. Verified by `cargo test -p limux-host-linux codex_wrapper -- --nocapture`, `cargo test -p limux-cli hook_session_id -- --nocapture`, `cargo test -p limux-cli hook_agent_pid -- --nocapture`, `cargo test -p limux-host-linux limux_cli_candidates -- --nocapture`, and `cargo clippy -p limux-host-linux -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Codex per-surface wrapper shim parity in `limux-host-linux`: every terminal tab now gets a private `codex` shim directory prepended to `PATH`, exports `CMUX_CODEX_WRAPPER_SHIM`/`LIMUX_CODEX_WRAPPER_SHIM` and root aliases, and preserves Codex launch metadata for real Codex hook events without inventing a fake native session id. Verified by `cargo test -p limux-host-linux codex_wrapper -- --nocapture`, `cargo test -p limux-cli codex -- --nocapture`, and `cargo clippy -p limux-host-linux -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Pi hook parity in `limux-cli`: `AgentKind::Pi` aliases, session-store coverage, native `pi --session <id>` restore command construction, `limux hooks pi install/uninstall`, protected `~/.pi/agent/extensions/cmux-session.ts` or `$PI_CODING_AGENT_DIR/extensions/cmux-session.ts` writes, generated TypeScript extension support for `session_start`, `before_agent_start`, `tool_execution_start`, `tool_execution_end`, `agent_end`, and `session_shutdown`, CMUX/Limux base64 launch capture, Pi Feed forwarding for `PreToolUse` and `PostToolUse`, and resume-binding set/clear calls. Verified by `cargo test -p limux-cli pi -- --nocapture`, `cargo test -p limux-cli feed_hook -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Amp hook parity in `limux-cli`: `AgentKind::Amp` aliases, session-store coverage, native `amp threads continue <id>` restore command construction, `limux hooks amp install/uninstall`, protected `~/.config/amp/plugins/cmux-session.ts` or `$AMP_CONFIG_DIR/plugins/cmux-session.ts` writes, generated TypeScript plugin support for `session.start`, `agent.start`, `tool.call`, `tool.result`, and `agent.end`, CMUX/Limux base64 launch capture with `AMP_API_KEY` stripped from child hook/status/log processes, and live `set-status`/`clear-status`/`log` bridge calls. Verified by `cargo test -p limux-cli amp -- --nocapture`, an isolated `AMP_CONFIG_DIR` install/uninstall smoke, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Hermes Agent hook parity in `limux-cli`: `AgentKind::HermesAgent` aliases, session-store coverage, native `hermes --resume <id>` restore command construction, `limux hooks hermes-agent install/uninstall`, protected marked YAML hook blocks in `$HERMES_HOME/config.yaml` or `~/.hermes/config.yaml`, `shell-hooks-allowlist.json` approval install/uninstall preserving unrelated approvals, lifecycle events `on_session_start`, `pre_llm_call`, `post_llm_call`, `pre_approval_request`, `post_approval_response`, `on_session_end`, `on_session_finalize`, and `on_session_reset`, Feed events `pre_tool_call`, `post_tool_call`, `pre_approval_request`, and `post_approval_response`, and nonblocking Hermes tool telemetry classification. Verified by `cargo test -p limux-cli hermes -- --nocapture`, `cargo test -p limux-cli feed_hook -- --nocapture`, an isolated `HERMES_HOME` install/uninstall smoke, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Kiro Feed deny-status parity in `limux-cli`: generated Kiro hook entries preserve Feed denial status instead of masking it with the generic `|| echo '{}'` fallback, resolved Kiro permission `deny` decisions exit with status 2, `once`/`always`/`all`/`bypass` allow decisions print `{}` and exit 0, and malformed Kiro permission modes fail closed with status 2. Verified by `cargo test -p limux-cli kiro -- --nocapture`.
- 2026-07-02: Added CMUX-compatible workspace-create title flag parity in `limux-cli`: `new-workspace --name <title>`, `new-workspace --title <title>`, and `workspace create --name/--title` now serialize the title into `workspace.create` params while preserving cwd, command, and workspace environment handling. Verified by `cargo test -p limux-cli new_workspace_params -- --nocapture` and `cargo test -p limux-cli workspace_env -- --nocapture`.
- 2026-07-02: Added CMUX-compatible project workspace-env loading in `limux-cli`: `new-workspace` and `workspace create` now search upward from the current directory for `.cmux/cmux.json` before `cmux.json`, stop at `$HOME`, read the `workspace.env` definition or the configured `newWorkspaceCommand` workspace env, reject protected `CMUX_*`/`LIMUX_*` keys, and keep explicit `--env-file`/`--env` values higher precedence. Verified by `cargo test -p limux-cli project_workspace_env -- --nocapture`, `cargo test -p limux-cli project_cmux_config -- --nocapture`, `cargo test -p limux-cli workspace_env -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible `config doctor --path` and project-target parity in `limux-cli`: doctor accepts repeated `--path` / `--path=<path>` custom targets, reports CMUX-style OK/MISSING/ERROR text with bytes and keys, and default doctor targets now include the nearest project `.cmux/cmux.json`/`cmux.json` when present. Verified by `cargo test -p limux-cli config_ -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible JSONC preprocessing for `config doctor` in `limux-cli`: doctor now accepts line comments, block comments, and trailing commas while preserving comment-like text inside strings, then validates the sanitized top-level object and reports `JSONC syntax is valid`. Verified by `cargo test -p limux-cli config_doctor -- --nocapture`, `cargo test -p limux-cli jsonc -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace-create focus parity in `limux-cli` and the live GTK bridge: `new-workspace --focus <true|false>` and `workspace create --focus <true|false>` serialize CMUX boolean aliases, invalid CLI focus values fail loudly, live `workspace.create` accepts boolean `focus`, defaults to background creation, and only activates the new workspace when `focus` is true. Verified by `cargo test -p limux-cli new_workspace_params -- --nocapture`, `cargo test -p limux-host-linux workspace_create_route_accepts_focus_bool -- --nocapture`, and `cargo test -p limux-host-linux workspace_env_and_create_routes_validate_environment -- --nocapture`.
- 2026-07-02: Added CMUX/tmux lifecycle alias parity in `limux-cli` for `neww`, `select-window`/`selectw`, `select-pane`/`selectp`, `kill-window`/`killw`, `kill-pane`/`killp`, and `renamew`, including no-socket help probes, `-t`/`--target` parsing, pane focus routing, surface close routing, workspace select/close routing, and rename title parsing that skips target values. Verified by `cargo test -p limux-cli tmux -- --nocapture` and `cargo test -p limux-cli cmux_ -- --nocapture`.
- 2026-07-02: Added CMUX/tmux detached split focus parity in `limux-cli` and the live GTK bridge: `split-window -d` now serializes `focus:false`, `surface.split` parses and preserves the focus flag, and launch-created terminal tabs honor that activation setting. Verified by `cargo test -p limux-cli tmux -- --nocapture` and `cargo test -p limux-host-linux surface_split -- --nocapture`.
- 2026-07-02: Added CMUX/tmux split sizing request parity in `limux-cli` and the live GTK bridge: `split-window -l <cells>` now serializes a bounded `initial_divider_position`, `surface.split` parses and preserves the divider field, and host pane splitting applies it as the initial split ratio. Verified by `cargo test -p limux-cli tmux -- --nocapture` and `cargo test -p limux-host-linux surface_split -- --nocapture`.
- 2026-07-02: Added CMUX/tmux `split-window -P -F` print-format parity in `limux-cli`: formatted split output now renders pane, surface, and workspace fields from the created split payload, falls back to the new pane handle, and treats `--format` as a value flag instead of command text. Verified by `cargo test -p limux-cli tmux_split_window -- --nocapture`, `cargo test -p limux-cli tmux -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX/tmux terminal geometry exposure foundation: Ghostty cell pixel dimensions now flow through terminal health, `surface-health` and `pane.list` expose active terminal rows/columns/pixel/cell metrics, and CLI tmux row-format rendering consumes real `pane_width`, `pane_height`, `pane_cell_width_px`, and `pane_cell_height_px` fields when available. Verified by `cargo test -p limux-cli tmux_list_rows_render_format_strings -- --nocapture` and `cargo check -p limux-host-linux`.
- 2026-07-02: Added exact CMUX/tmux `split-window -l` live sizing in `limux-cli`: explicit `-l` sizing now resolves workspace/pane context, reads the target pane's real rows/columns from `pane.list`, computes the divider with CMUX's current-cells clamp formula, and fails loudly if geometry cannot be resolved instead of using the old 100-cell approximation. Verified by `cargo test -p limux-cli tmux_split_window -- --nocapture`, `cargo test -p limux-cli tmux -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added additional CMUX/tmux local command parity in `limux-cli`: `pipe-pane` accepts trailing shell command text as well as `--command`, `wait-for` now uses CMUX's 30-second default with fractional non-negative timeout parsing, and `find-window` emits CMUX-shaped multi-match/no-match text with case-insensitive title matching plus `--content` and `--select` routing. Verified by `cargo test -p limux-cli tmux_pipe_pane -- --nocapture`, `cargo test -p limux-cli tmux_wait_for -- --nocapture`, `cargo test -p limux-cli tmux_find_window -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Ghostty theme catalog parity in `limux-cli`: `themes list` discovers Ghostty theme files from Linux resource, XDG data, repo, and user theme directories, text/JSON output reports CMUX-style theme rows with current light/dark badges, and `themes set` canonicalizes or rejects names against the discovered catalog while keeping arbitrary-name behavior when no catalog is available. Verified by `cargo test -p limux-cli theme -- --nocapture`, `cargo test -p limux-cli -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible top-level `tree` and partial `top` routing in `limux-cli`: `tree` now assembles a window/workspace/pane/surface topology from existing list APIs and renders text/JSON output, while `top` uses Limux `system.memory` diagnostics and explicitly rejects scoped/sorted/TSV CMUX `system.top` flags instead of silently faking them. Verified by `cargo test -p limux-cli cmux_tree -- --nocapture`, `cargo test -p limux-cli cmux_top -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added native live-bridge topology parity: the GTK bridge now advertises and routes `window.list` and `system.tree`, and top-level `tree` calls `system.tree` instead of reconstructing topology through multiple client-side list calls. Verified by `cargo test -p limux-host-linux system_tree -- --nocapture`, `cargo test -p limux-cli cmux_tree -- --nocapture`, and `cargo clippy -p limux-host-linux -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added additional local CMUX `top` parity in `limux-cli`: `top --sort mem|proc` reorders local process groups, and `top --flat` / `top --format tsv` render CMUX-shaped TSV rows from Limux's current process-group diagnostics while scoped top and CPU sorting remain loud unsupported paths. Verified by `cargo test -p limux-cli cmux_top -- --nocapture` and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added native live-bridge `system.top` parity for Limux process diagnostics: the bridge advertises/routes `system.top`, top-level `top` calls it directly, `--workspace` filters process groups using Limux child-process attribution, `--window window:1` validates the current GTK host window, and `--all` requests the unfiltered local process view. Verified by `cargo test -p limux-host-linux system_top -- --nocapture`, `cargo test -p limux-host-linux memory_diagnostics -- --nocapture`, and `cargo test -p limux-cli cmux_top -- --nocapture`.
- 2026-07-02: Added CMUX-compatible `top --sort cpu` local diagnostics: the host now extracts cumulative user+system CPU ticks from `/proc/<pid>/stat`, aggregates ticks per direct-child process group, includes `cpu_ticks` in process diagnostics, and TSV top output reports the measured tick value instead of a placeholder. Verified by `cargo test -p limux-host-linux memory_diagnostics -- --nocapture`, `cargo test -p limux-cli cmux_top -- --nocapture`, and `cargo clippy -p limux-host-linux -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX docs dock topic parity in `limux-cli`: `docs` now lists `dock`, `docs dock` prints dock/right-sidebar custom-control guidance, and `doc`/`controls`/`right-sidebar`/`dock-json` aliases normalize like CMUX. Verified by `cargo test -p limux-cli cmux_docs -- --nocapture`.
- 2026-07-02: Added CMUX/tmux local split/layout parity in `limux-cli`: `split-window`/`splitw` now route to `surface.split` with tmux `-h`/`-v`/`-b` direction mapping, `-c` working directory, `-t` surface target, command injection, and `-P` surface-handle output, while `select-layout` routes to `workspace.equalize_splits` with `main-vertical`/`main-horizontal` orientation mapping. Verified by `cargo test -p limux-cli tmux -- --nocapture`, `cargo test -p limux-cli cmux_ -- --nocapture`, and `cargo clippy -p limux-cli --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace-create layout parity in `limux-cli` and the live GTK bridge: `new-workspace --layout <json>` and `workspace create --layout <json>` require JSON objects, serialize CMUX layout params, translate CMUX split/pane/surface layout topology into Limux `LayoutNodeState`, run terminal startup commands from layout surface entries, seed browser surface URLs, and skip top-level `--command` when a layout is present. Verified by `cargo test -p limux-cli new_workspace_params -- --nocapture`, `cargo test -p limux-host-linux workspace_create_route -- --nocapture`, `cargo test -p limux-host-linux terminal_tab_state_round_trips_restorable_agent_metadata -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace-create description and group-placement parity in `limux-cli` and the live GTK bridge: `new-workspace --description`, `--group`, `--group-placement top|end|afterCurrent`, and `--group-reference` serialize into `workspace.create`, live host creation validates group/reference targets before mutation, persists workspace descriptions, includes descriptions in workspace payloads/events, and places new grouped workspaces at top, end, or after the reference/current workspace. Verified by `cargo test -p limux-cli new_workspace_params -- --nocapture`, `cargo test -p limux-host-linux workspace_create_route -- --nocapture`, `cargo test -p limux-host-linux workspace_group_state_round_trips_through_session_json -- --nocapture`, and `cargo clippy -p limux-cli -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible `workspace.group.new_workspace` placement parity: live bridge rejects invalid placement values, host grouped workspace creation applies top/end/afterCurrent insertion order, and the default uses the group anchor as the CLI/header reference. Verified by `cargo test -p limux-host-linux workspace_group_mutation_routes_parse_cmux_params -- --nocapture`, `cargo test -p limux-host-linux workspace_group_insert_index_matches_cmux_placements -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible workspace group placement config parity in the live host: `workspaceGroups.newWorkspacePlacement` and longest-match `workspaceGroups.byCwd[*].newWorkspacePlacement` are loaded from Limux settings JSON, invalid placement values fail loudly, supported byCwd fields are preserved on settings save, and `workspace.group.new_workspace` uses the configured default when no explicit placement is supplied. Verified by `cargo test -p limux-host-linux workspace_group_placement -- --nocapture`, `cargo test -p limux-host-linux save_to_path_writes_workspace_group_default_and_preserves_by_cwd -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible Cmd-N folder workspace group placement parity in the live host: folder-dialog workspace creation now detects the active workspace group, resolves configured placement from the group anchor cwd, uses the active workspace as the `afterCurrent` reference, and leaves ungrouped active workspaces unchanged. Verified by `cargo test -p limux-host-linux workspace_group_insert_index_matches_cmux_placements -- --nocapture`, `cargo test -p limux-host-linux workspace_group_placement -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.
- 2026-07-02: Added CMUX-compatible `app.newWorkspacePlacement` parity in the live host: settings JSON now loads/saves the app-level placement value, invalid placement values fail loudly, unrelated app settings are preserved on save, and normal Cmd-N/sidebar-button folder workspace creation honors top/end/afterCurrent insertion with CMUX pinned-prefix behavior. Verified by `cargo test -p limux-host-linux app_new_workspace_placement -- --nocapture`, `cargo test -p limux-host-linux save_to_path_writes_app_workspace_placement_and_preserves_app_keys -- --nocapture`, `cargo test -p limux-host-linux workspace_insert_index_matches_cmux_app_placements_with_pins -- --nocapture`, and `cargo clippy -p limux-host-linux --all-targets -- -D warnings`.

- `/tmp/cmux-audit/docs/cli-contract.md`
- `/tmp/cmux-audit/docs/events.md`
- `/tmp/cmux-audit/docs/feed.md`
- `/tmp/cmux-audit/docs/notifications.md`
- `/tmp/cmux-audit/docs/workspace-groups.md`
- `/tmp/cmux-audit/docs/remote-daemon-spec.md`
- `/tmp/cmux-audit/daemon/remote/README.md`
- `/tmp/cmux-audit/skills/cmux-browser/references/commands.md`
